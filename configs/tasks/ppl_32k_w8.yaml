name: ppl_32k_w8
generator: ppl
ctx: 32768
doc: >-
  Teacher-forced perplexity at 32K, 8 sampled windows: prefill the context with the arm's
  compression active, then score a frozen 512-token continuation window against the compressed
  cache, averaged over 8 sampled windows (not the 4 of ppl_32k). The Week-18 G4 pod's protocol --
  perplexity on the SAME pod as the RULER cells, which is what kills the cross-week splice the
  review panel objected to. v1 read WikiText-103 TRAIN (a known defect recorded in CODE_AUDIT).
n_trials: 1
seeds: [0]
n_samples: 8
