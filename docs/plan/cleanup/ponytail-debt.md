# ponytail-debt ledger — kv-dlra

Scan 2026-09-11 on week7 @ 2719a50: `grep -rnE '(#|//) ?ponytail:' .` (excluding .git, .venv, .claude, caches).

No ponytail: debt. Clean ledger. (0 markers, 0 with no trigger.)

Convention from here on (CLAUDE.md → ponytail full): every deliberate shortcut carries
`# ponytail: <ceiling>, <upgrade path>`. L0's after-report and `/ponytail-debt` in the
cleanup lane regenerate this file; a marker with no trigger is tagged `no-trigger`.
