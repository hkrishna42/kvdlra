# L4 lane ledger — copied verbatim from the lane SDD workspace (at the finishing menu, 935f745)

Source: `.claude/worktrees/L4-kernel/.superpowers/sdd/2026-09-20-L4-kernel/progress.md`, copied 2026-09-21 08:53 EDT. Rulings R-L4-0..35 with every review verdict; the plan scan (plan-scan.md), the whole-branch review (final-review-report.md), the ponytail review and the amendment draft live beside it in that workspace; the amendment draft is also committed as docs/plan/reports/kernel-smoke-amendment2-draft.md.

---

# SDD ledger — plan: docs/plan/plans/2026-09-20-L4-kernel.md

Lane lane/L4-kernel in .claude/worktrees/L4-kernel, branched off origin/lane/L3-gate1-tracker-swap-v2 @ 91d149d (NOT main — main @ 278bd01 is behind the lane until the L3 session merges; L4's first task edits bug_cache.py, which the lane changed; `git rebase main` in this worktree after that merge lands). Own .venv (make env, 2 s from the uv cache; `import kvdlra` resolves to this tree). Baseline make test here: 622 passed / 112.9 s under load (the L6 warm figure is 60.8–70.3 s; the 90 s CI gate is read warm).
Session started 2026-09-20 23:30 EDT. The L3 session (kv-dlra-5e) owns the pre-flight re-run harvest, Amendment 1b, the L6 gate, the --no-ff merge into main and the Stage-1 launch — this lane never touches them and never runs git in the main checkout.
Spec: docs/adr/0001-factored-attention-kernel.md (ACCEPTED, D-002), docs/plan/lanes/L4_kernel.md, GATES §G4, prereg/kernel_smoke.md, ICML2027_PLAN §2 Gate 3, SESSION_PROMPT_L4 "Lane L4-kernel".
Mode: one implementer per worktree at a time (R-L3-17); reviewers may overlap; opus for implementers and reviewers by default, sonnet for mechanical fix rounds and scoped re-reviews, the most capable model for the plan and the whole-branch review.

Ruling R-L4-0 (session setup): each worktree gets its OWN .venv via `make env` instead of symlinking the shared one — the shared editable install points at exactly one tree and the L3 session may run its gates in its tree at any moment; cost if wrong: seconds of make env and hardlinked disk.
Ruling R-L4-1: the lane plan is written by a dedicated plan-writer agent (most capable model) from the ADR + prereg + lane brief + the code, then pre-flight-scanned here before Task 1 — the orchestrator's context is kept for coordination; cost if wrong: one plan review round.

2026-09-20 23:41 EDT: plan writer dispatched (fable) → docs/plan/plans/2026-09-20-L4-kernel.md + .superpowers/sdd/plan-writer-report.md.
2026-09-21 00:45 EDT: PLAN LANDED @ 384eaf8 (fable plan writer; 3,622 lines, 9 tasks; report .superpowers/sdd/plan-writer-report.md). Read by the orchestrator: header, Global Constraints, the design decisions, the verified transformers call path, Task 1 in full (self-consistent: the row fan-out, every reader it lists, five tests calling what it defines). Whole-plan pre-flight scan (fable, read-only → plan-scan.md) dispatched in parallel: the conflict table for every task pair, the spec coverage, the call-path claims, the numerics contract, the amendment + prereg arithmetic.
Rulings on the plan writer's questions/contradictions (each surfaced to the owner at the next report):
- Ruling R-L4-2: ONE pod — the kernel_check axis runs first, then the latency cells (prereg §4's own amendment clause); a failed check refuses the Week-3 gate while the latency rows still land as measurements. Cost if wrong: one extra ≈ $1 check pod.
- Ruling R-L4-3: gpu_budget_h 5.0 for cell list A + the check (2.3× the 2.2 h point; the deviation from §9's 4.0 named in Amendment 1 A1.4). Cost if wrong: ≈ $0.7 at the bar.
- Ruling R-L4-4: the 8B single-layer half of the correctness precondition is met by the on-pod per-layer max|Δ| on live 8B K/V and queries (§4 lets the launch entry name what unblocked it; stronger than one dumped layer); the 1B-dump half runs locally. Cost if wrong: an amendment sentence + a dump pod.
- Ruling R-L4-5: the renderer (Task 7) lands one commit BEFORE the pod YAML (Task 8) — at least as strict as §10's "same commit" for citability. Cost if wrong: none.
- Ruling R-L4-6: postrope_r128 = a single-arm pod paired against the Stage-1 Llama pod by prompt_sha256/window_idx (the pre-flight re-run's precedent); not launched in this lane. Cost if wrong: a prereg re-plan before any launch.
- Ruling R-L4-7 (provisional): the Triton kernel's first execution is `pytest -m gpu` on the pod before `pod.py run` (no GPU here); if it needs more than two fix-and-relaunch iterations (≈ $0.35–1 each), a dedicated GPU dev loop is set up instead. Cost if wrong: ≈ $2 of boots.
- Owner's: cell list B's card (H100-or-max-batch, D-002 concern 2) — not decided here.
- Noted: commit form is `git commit -o -m "..." -- <paths>` (the -m must precede --); test-count baseline on this worktree is 622 (the plan's "603" is STATE's L3 figure) → Task 1 expects 626.
Task 1 BASE = 384eaf8d2201bae5d68c66847fd0a0d322408c3f (dispatched 2026-09-21 00:42 EDT, opus implementer, brief task-1-brief.md + global-constraints.md, report task-1-report.md).
2026-09-21 01:07 EDT: Task 1 implementer DONE_WITH_CONCERNS @ 09b9103 (opus; 626 passed / 79 s; new module 0.39 s; golden bit-identity green; RED 5 × NotImplementedError first). Concerns: (1) three eval-side readers of layer tensors do not fan out at batch > 1 — frontier._footprint (:355), frontier.py:559 _tracked_rank, persist.py:48-68/:131 — none reached by run_latency (the only batched caller) → passed to the reviewer as a named risk, ruling after the review; (2) ruff SIM117 / mypy no-untyped-call on Cache.reset forced cosmetic deviations from the brief's test text; (3) bare pytest without .venv/bin on PATH fails test_w10_ruler_quant (ninja) — environmental, pre-existing; use make test; (4) line drift 293-297. Task reviewer (opus) dispatched on 384eaf8..09b9103.
2026-09-21 01:25 EDT: Task 1 review (opus): APPROVED, no Critical/Important. Verified: all 13 sites verbatim (test file diffed mechanically against the brief — three cosmetic lint deltas only); batch-1 path untouched by construction (every new reader short-circuits on falsy _rows; reset()/drain_diag() reduce to the old bodies); _ctor_kwargs = exactly the 30 constructor params, alias overwrite suppresses the row warning (run, not read); consolidate fan-out load-bearing and covered; shared _QuantBank data-oblivious; Cache base fan-out complete. Minors (deferred): (a) frontier._footprint (:350-365), frontier.py:559 _tracked_rank and persist.state_tensors (:44-65, :127-133) silently ZERO at batch > 1 (u_k None → rank 0 / empty persisted state) — not reached by run_latency, the only batched caller; (b) the 1→B transition on a populated parent silently builds fresh rows (bug_cache.py:1279-1287) — build rows only when cumulative_length == 0, else raise; (c) the report overstates the ragged-path guarantee (rows are half-advanced before the raise — harmless abort path); (d) the footprint identity assertion is quant-off-only (comment); (e) drain order is not chronological across a reset (no contract); (f) rows share basis_seed/layer_idx (the right call; recorded); (g) transformers' batch-manipulation Cache APIs (reorder/crop/repeat/select) still address the parent — pre-existing.
Ruling R-L4-8: minors (a) and (b) are one-line guards on the citability path and are folded into Task 4's dispatch (the next task that edits bug_cache.py and frontier.py): a NotImplementedError in _footprint/_tracked_rank/state_tensors when a layer has _rows, and the 1→B refusal in _update_rows, each with a one-assert test; (c)–(g) go to the final review's deferred list. Cost if wrong: ≈ 10 lines.
Task 1: complete (commits 384eaf8..09b9103, review clean)
Task 2 BASE = 09b91030e4aba0225cb94961e7e0b808c9504b90 (dispatched 2026-09-21 01:16 EDT, sonnet implementer — the brief carries the complete code; brief task-2-brief.md + global-constraints.md, report task-2-report.md). Note for the dispatch: baseline 626 → expected 629; `pytest -m gpu` with nothing marked exits 5 ('no tests ran'), which is the expected outcome, not a failure.
2026-09-21 01:40 EDT: PLAN SCAN (fable, read-only; plan-scan.md): 35 rows (26 pairs + 9 per-task), 1 Critical / 10 Important / 10 Minor; every bug_cache.py cite verified at HEAD; the transformers call-path claims verified by execution (mask None/bool by registration; rope bit-identity); Task 3's reference + 6 tests RUN as written (6 passed; 1B layer 8 max|Δ| 2.75e-3 at |out| ≤ 0.84); Task 7's renderer run through a shim (2 of 4 tests fail on a mkdir); the amendment's GiB figures re-derived; no placeholders; forbidden words clean in every file the plan writes.
Rulings (each an exact plan edit, applied by a plan-editor agent once Task 2's implementer has committed — one writer per worktree):
- Ruling R-L4-9 (C-1): the five prompt constants (PROMPT_CORPUS, PROMPT_TOKENS, N_NEW, PROMPT_STRIDE, PROMPT_STARTS) live in kvdlra.eval.config (torch-free) and kvdlra.kernel.prompts re-exports them; Task 6's config.py drops its `from kvdlra.kernel.prompts import …` — otherwise every pod.py/tables.py subprocess imports torch + transformers (5–12 s × ~68 subprocesses ≈ +5–6 min of CI). Cost if wrong: two import lines. If Task 2 lands before this reaches it, the change is a plan-mandated item of its fix round.
- Ruling R-L4-10 (I-1): Task 4's mask-size assertion reads attended_length() + 1 (get_mask_sizes reports the returned block incl. the new token; measured 70 vs 69).
- Ruling R-L4-11 (I-2): Task 7's `_results()` rmtree-then-mkdir (the second render raised FileExistsError; 4 passed with it).
- Ruling R-L4-12 (I-3): the on-pod kernel_compare reference is fp32 sdpa over the bf16 stored representation (query/k_full/v_full .float()), the same rounding point as the CPU tests; A1.2 says so. Cost if wrong: one fp32 copy of one layer for one step.
- Ruling R-L4-13 (I-4): the ≤ 8 s constraint is enforced by restructuring the tests, not waived — Task 8's pod test uses one module-scoped dry run + in-process harvest/check/latency_table with a single `launch --dry-run` subprocess; Task 7's CLI test calls tables.main() in-process; Task 4's exact-tier variant runs on 4 of the 16 tiny prompts; per-task budgets restated (T1 1.5 landed, T2 0.2, T3 1.0 CI, T4 2.0, T6 0.8, T7 0.5, T8 1.5, T9 0.5 = 8.0).
- Ruling R-L4-14 (I-5/I-6): the full-suite command is `make test` (the Makefile prepends .venv/bin; bare pytest fails one quant test on ninja); the baseline is 626 at 09b9103 with the corrected deltas (T2 629, T3 635, T4 642, T5 642 + 8 skipped, T6 648, T7 654, T8 658, T9 663).
- Ruling R-L4-15 (I-7/I-8/I-9, B.6): the corrected arithmetic — A1.4/pod doc: the check ≈ 0.78 min per prefill (¼ of the r64 3.1), ≈ 27 min ≈ 0.45 h, point 2.25 h, the 5.0 bar = 2.2×, $1.0–1.7 expected; postrope §8 ≈ 416 [diag] rows/sample ≈ 53,000 rows (bursts of 416 inside the 30,000-line poll); postrope §9's three-arm alternative ≈ 39 h with boot (34 h before it).
- Ruling R-L4-16 (I-10): Task 3's synthetic case scales dense_v and c_v to O(1) (× 8) so the absolute 1e-2 bar bites in CI; the bar itself stays absolute and pre-registered; the implementer reports the measured diff after scaling (expected ≈ 5e-3, under the bar with ≥ 2× margin — a larger number is a finding, not a reason to move the bar).
- Minors taken (M-2 cd prefix; M-3 dispatcher falls back to the reference unless CUDA ∧ bf16 ∧ triton, and FactoredMiddle.cat raises a clear ValueError on differing live ranks; M-4 the no-tier comment; M-6 the exit-5 note; M-7 "8 skipped"; M-9 the navigational note that bug_cache.py/records.py line numbers are re-located by symbol at dispatch). M-1/M-5/M-8/M-10 need no edit.
- The author's questions: 1 one pod check-first (R-L4-2 stands); 2 bar 5.0 at the corrected 2.2× (R-L4-3 stands); 3 postrope cross-pod pairing + the launch entry names the Stage-1 Llama harvest commit (R-L4-6 + one §10 sentence); 4 cell list B's card = owner's; 5 fingerprints verified here (sum 133202, sha c0d9…b659f); 6 the constraint-list wording is fine.
Dispatch order from here: Task 2 review → plan edits (sonnet, exact replacement texts from plan-scan.md Part D) → Task 3 (+ the M-3 dispatcher change in its brief) → Task 4 (+ R-L4-8's guards) → …
2026-09-21 01:35 EDT: Task 2 implementer DONE_WITH_CONCERNS @ e22b661 (sonnet; 629 passed; 3 new tests; -m gpu exits 5 as expected). Concerns: the brief's zip(STARTS, STARTS[1:], strict=True) always raises → itertools.pairwise (RUF007 agreed); a 103-col wrap; SIM300 reorder; every fingerprint matched first run. Task reviewer (sonnet) dispatched on 09b9103..e22b661 with R-L4-9 (constants → config.py, re-exported) as a plan-level ruling to verify against; plan-editor (sonnet) dispatched in parallel to apply plan-scan.md Part D's edits to the plan file only.
2026-09-21 01:43 EDT: Task 2 review (sonnet): APPROVED — five files verbatim, three tests re-run (3 passed / 0.08 s), the zip(strict=True) defect confirmed real and pairwise exact, marker/hook verified live (-m gpu exit 5, no strict-markers complaint). Plan-mandated follow-up (R-L4-9): the five prompt constants must move to kvdlra.eval.config and be re-exported (prompt_windows and the pin test unaffected; prompts.py:5's docstring sentence reverses direction). Minor: the short-stream test's try/except → pytest.raises (inherited from the brief). Fix round 1 (sonnet) queued behind the plan editor (one writer per worktree).
2026-09-21 02:04 EDT: PLAN EDITS landed @ f0eedc4 (sonnet plan editor; 13/13 items of plan-scan.md Part D applied; 57 fenced blocks parse; the stale self-review sentence at the plan's tail left as is — a note, not a requirement). Task 2 fix round 1 (sonnet) dispatched: R-L4-9 constants move + docstring + pytest.raises. Task 3 brief extracted from the edited plan.
2026-09-21 02:16 EDT: Task 2 fix round 1/5 DONE @ 3bf4a0c (sonnet; constants → config.py torch-free (verified 'torch' not in sys.modules), re-export via __all__; docstring; pytest.raises; 629 passed; lint clean). Scoped re-review (sonnet) dispatched on e22b661..3bf4a0c (the range also carries the plan-edit commit f0eedc4).
Task 3 BASE = 3bf4a0cb6cef745a20ec777942d1aed29396bf54 (dispatched 2026-09-21 02:16 EDT, opus implementer; brief task-3-brief.md + global-constraints.md; report task-3-report.md; the scan ran this task's code as written: 6 passed, 1B layer 8 max|Δ| 2.75e-3 pre-scaling).
2026-09-21 02:20 EDT: Task 2 re-review of round 1 (sonnet): all 3 ADDRESSED (config torch-free verified live: '1966080 False'; kernel.prompts 'True False'); no new breakage. Deferred minor: the four-line window comment in prompts.py now floats above the TINY_* block (its constants moved to config.py).
Task 2: fix round 1/5 (3 addressed, 0 open; commits e22b661..3bf4a0c)
Task 2: complete (commits 09b9103..3bf4a0c, review clean after one fix round)
2026-09-21 02:27 EDT: Task 3 implementer DONE @ 6fe058c (opus; 635 passed / 67.9 s; module 6 passed / 0.95 s incl. the 0.65 s dump test; max|Δ| random 4.66e-3 / 5.15e-3 after ×8, fp32 1.1e-6, no-middle 2.4e-6, 1B layer 8 2.75e-3 — all under the 1e-2 bar). Concerns: six lint/mypy-forced deviations (rope_theta inside rope_parameters — inv_freq bit-identical; typed _mid_to helper; importlib for the triton import); Task 5's signature contract; 'auto' silently takes the reference for fp32 on CUDA (pod runs assert their backend). Task reviewer (opus) dispatched on 3bf4a0c..6fe058c.
2026-09-21 02:37 EDT: Task 3 review (opus): APPROVED, no Critical — every rounding point cited (U/C per tile on use with fp32 accumulate; in-tile fp32 RoPE at true positions, bit-identical to LlamaRotaryEmbedding; K̂/V̂ rounded before the dots; running sum over unrounded p, PV over rounded p; dense tiles in the same softmax; head-major U, GQA h*G+j → h); the test reference mirrors _ensure_mid_cache op for op; the dump test drives the production layer (293 dense + 3803 middle = 4096 = _decode_peek's length); rope_theta relocation bit-identical; measured numbers reproduced to all digits; mutation study: layout/GQA/RoPE-position defects blow the bar 100×, but the rounded-p-sum and unrounded-K̂/V̂ mutations are invisible at 1e-2 (5.151e-3 / 4.590e-3 vs 5.151e-3). Important (plan-mandated, for Task 5): a second 1e-2 comparison against reconstruct cannot gate the Triton kernel's rounding points. Minors (deferred): FactoredMiddle.cat untested until Task 4; five raise-paths uncovered; finish() nan with no tokens (unreachable); the 8B rotary has no external bit-pin; importlib hides Task 5's signature from mypy; U's per-call fp32 transient (brief-verbatim); reference.py:35 forward-references _mid_coords (Task 4).
Ruling R-L4-17: Task 5's gpu-marked tests add a DIRECT triton-vs-reference comparison on the random cases at max|Δ| ≤ 2e-4 (25× under the pre-registered bar and ≈ 3× under the ≈ 5.6e-4 the rounding-point mutations move the output by), printed with the measured value; the pre-registered 1e-2 vs reconstruct stays as written. If a kernel that passes 1e-2 fails only the 2e-4 bound on the pod, the review decides whether the kernel or the bound is wrong — never the implementer alone. Cost if wrong: one extra pod boot (≈ $0.35).
Task 3: complete (commits 3bf4a0c..6fe058c, review clean)
Task 4 BASE = 6fe058c5fe9dbc8d3c34bc6c6680afb3b02572ed (dispatched 2026-09-21 02:37 EDT, opus implementer; brief task-4-brief.md + global-constraints.md + R-L4-8's two guards as additional items; report task-4-report.md; expected 642 + 2 = 644 passed on this Mac).
2026-09-21 02:58 EDT: Task 4 implementer DONE_WITH_CONCERNS @ 99842f2 + 05e8c40 (opus; 644 passed / 66.9 s; test_kernel_path 7 passed / 1.60 s; 16/16 token-exact, max |logit diff| 1.64e-7; 512 factored calls + 64 sdpa delegations counted; kernel_compare smoke 1.1e-8/1.5e-8). Concerns: run_ppl's tracked-rank guard unasserted (unreachable independently; _footprint raises first); kernel_compare untested until Task 6; the glue is Llama-shaped (single rotary_emb, module.layer_idx/scaling); lint wraps + one unused type-ignore removed. Task reviewer (opus) dispatched on 6fe058c..05e8c40.
2026-09-21 03:10 EDT: Task 4 review (opus): APPROVED — brief verbatim (attention.py diffed against the brief: one unused type-ignore removed; the test file line-wrapped only); reconstruct path provably untouched; transformers mechanism re-verified (register/get_interface/mask None/output layout); the kernel's middle bit-identical to _ensure_mid_cache's in fifo, surprise + tier, and the two-tier quantized case (0.0); 32 factored calls + 4 delegations per prompt reproduced; workspace 0 after kernel decode; R-L4-8 guards placed before the first tensor read, two tests non-vacuous. Important 1: _factored_middle's positions diverge from _ensure_mid_cache on fifo + exact tier (0.76 max abs K; unreachable in committed arms) → Ruling R-L4-18 (shared position helper + a fifo/tier identity test); Important 2: the run_ppl guard untested → Ruling R-L4-19 (documented deviation accepted; no helper). Minors: attach registrations outside try; nested attach unsupported (docstring); false lazy-import comment; redundant assert; counters not asserted (workspace_numel()==0 taken instead); kernel_compare re-materializes the workspace (Task 6 must not read it on a compare step); _mid_coords duplication by design. Fix round 1 (sonnet) dispatched with task-4-fix1-findings.md.
2026-09-21 03:38 EDT: Task 4 fix round 1/5 DONE @ a2f0d10 (sonnet; 645 passed / 71.5 s; test_kernel_path 8 passed / 1.64 s; the fifo+tier test reproduces k_diff 0.7512 pre-fix, 0.0 post-fix; _mid_positions()'s fifo branch now includes _hh_attended_len and _ensure_mid_cache sources mid_start from it — the golden green). Scoped re-review (sonnet) dispatched on 05e8c40..a2f0d10 — named risk: the reconstruct path's output must be unchanged in every configuration (the helper change touches _ensure_mid_cache).
Task 5 BASE = a2f0d101864b8be57a46cf42a6ddb56e3f704030 (dispatched 2026-09-21 03:38 EDT, opus implementer; brief task-5-brief.md + global-constraints.md + R-L4-17; report task-5-report.md; expected 645 passed + 8 skipped; reviewed-not-run — first execution is pytest -m gpu on the pod).
2026-09-21 03:41 EDT: CROSS-LANE — the L3 session (local_334d7519, 'L3/L5 orchestration weeks 0–3 (fork)') is idle: its transcript ends with the Monitor's ALL_DONE notification at 02:24 EDT and no turn after it; the watchdog harvested results/gate1_preflight_rerun/ to disk (uncommitted). Ruling R-L4-20: the harvest commit / Amendment 1b / merge / Stage-1 launch stay the L3 session's (the handover rule: STATE or the owner releases them); this session sent it one status message via the desktop app's send_message asking it to continue its own sequence, and does not touch those items. Cost if wrong: the L3 session resumes — which is what the owner asked of it. If it stays idle into the morning, the owner decides at the next report.
2026-09-21 03:51 EDT: Task 4 re-review of round 1 (sonnet): all 6 items ADDRESSED; the reconstruct path proven unchanged (a) track_positions True untouched, (b)/(c) _mid_len() = q + f + hh_attended so old and new mid_start are identical integers; the old fifo branch of _mid_positions() was DEAD before Task 4 (its one caller sat inside the track_positions branch); the new test would fail on a revert (k_diff 0.7512). Out of scope: make test in the shared worktree shows test_reachability failing on Task 5's in-progress triton_kernel.py (a lazily imported module) — Task 5's implementer owns it.
Task 4: fix round 1/5 (6 addressed, 0 open; commits 05e8c40..a2f0d10)
Task 4: complete (commits 6fe058c..a2f0d10, review clean after one fix round)
2026-09-21 03:58 EDT: Task 5 implementer DONE_WITH_CONCERNS @ c710ba6 (opus; reviewed-not-run: 645 passed / 8 skipped; ruff/mypy clean; import path lazy; -m gpu collects 8). Disagreements with the brief, all mechanical: test_reachability needed a function-level import of triton_kernel inside a gpu test; mypy codes untyped-decorator/no-untyped-def on @triton.jit; C408 dict literal; N803/N806 noqa outside the tests waiver; one ADDITION: dense_v.shape != dense_k.shape raises (DV addressed with DK's strides). R-L4-17 applied (REF_TOL = 2e-4, measured value printed and in the assert message). Concerns: the legitimate kernel-vs-reference difference is estimated low-1e-4 (thin headroom under 2e-4); probable register spills at num_warps=4 on the 8B shape (latency, not correctness). Task reviewer (opus, static derivation) dispatched on a2f0d10..c710ba6.
Task 6 BASE = c710ba6127d275ccecedad394ae5b74bbbc2209b (dispatched 2026-09-21 03:58 EDT, opus implementer; brief task-6-brief.md + global-constraints.md + the Task-4 note that kernel_compare re-materializes the workspace on the compare step; report task-6-report.md; expected 651 passed + 8 skipped).
2026-09-21 04:13 EDT: Task 6 implementer DONE @ 27d3593 (opus; 651 passed / 8 skipped / 70.9 s; module 6 tests / 0.45 s; the log line round-trips incl. '-' placeholders and the error= tail; CHECK FAIL message verbatim; config.py and pod.py torch-free; nine files). Deviations gate-forced (bool() wrap; wraps; runner docstring). Concern: the axis's on-pod wall clock (2 runs × 16 prompts × kernel arms) is unmeasured — Task 8's budget. Task reviewer (opus) dispatched on c710ba6..27d3593.
Task 7 BASE = 27d359334fff8ebe74bf978b899852aaa0f5ed1f (dispatched 2026-09-21 04:13 EDT, opus implementer; brief task-7-brief.md + global-constraints.md; report task-7-report.md; expected 657 passed + 8 skipped; make tables must stay diff-clean).
2026-09-21 04:16 EDT: Task 5 review (opus, static): NEEDS FIXES — the kernel derives clean (44/44 mechanical checks of the pointer expressions at h=0 and h=H_kv−1 incl. ragged tails; six rounding points mapped line by line; the split partition exhaustive; bit-identical at n_splits=1 on fp32 operands; emulated split vs reconstruct 5.17e-3/5.15e-3 under the 1e-2 bar). Critical 1+2: the two direct-comparison tests cannot pass — bf16 outputs quantize at one ulp 3.9e-3 and the split re-draws the P rounding (1.8e-3 in fp32) → Ruling R-L4-21 amends R-L4-17: fp32 inputs (bit-identical after the cast), the 2e-4 bar at n_splits == 1 via B = 16 (full 32-tile loop), 4e-3 for the 16-split and the batch-independence cases. Important 3 → Ruling R-L4-22: 'auto' falls back to the reference for non-pow2 r/D or G > 16 (a testable eligibility predicate). Important 4: the headroom estimate corrected in the record. Minors: three Python-side refusals taken (inv_freq length, mid/dense device, B cross-check); num_warps left for the pod. Fix round 1 (opus — nothing can run here) queued behind Task 7's writer slot; findings in task-5-fix1-findings.md.
2026-09-21 04:23 EDT: Task 6 review (opus): APPROVED, no Critical/Important — every contract verified by execution (line format round trip incl. the error= tail; companion lines match neither the regex nor replayable; the CHECK FAIL message; the three validation branches; config.py and pod.py torch-free; both paths feed their own greedy tokens; error rows never dropped; the tracker's two RNG draws seeded). Only the pod can show the bf16 8B magnitude and the axis's wall clock. Minors (deferred → the next writer round as a small 'L4.6 minors' commit): docstring drift (pod.py:642-643 'four'→five, :902 'three'→four; runner.py:21 stdout contract omits [kernel_check]); a regression test that the [kernel_check layers …] companion line is NOT a record (the higher-value pin) + a pytest.raises on a bad task config; n_new/chunk unpinned (plan-mandated); no layer count recorded (Task 7's contract; leave).
Task 6: complete (commits c710ba6..27d3593, review clean)
2026-09-21 04:28 EDT: Task 7 implementer DONE_WITH_CONCERNS @ b44ad40 (opus; 657 passed / 8 skipped / 68 s; module 0.25–0.36 s; make tables diff-clean; footprints 0.302/0.556/1.064 GiB reproduced). Concerns: one load_arm per arm instead of per grid row (the brief's form measured 0.48–0.59 s); hand line splits under fmt: skip; three mypy accommodations; the speed ratio still prints under a REFUSED precondition (the brief's behaviour) → Ruling R-L4-23: keep printing — the verdict line governs citability and prereg §4's 'not measured for speed' means not READ as a result, which the REFUSED verdict states; A1.2 says so. Prereg §3's 'resident ratio not in LatencyRecord' is stale → Task 8's A1.3 already records the field. Task reviewer (opus) dispatched on 27d3593..b44ad40. Task 5 fix round 1 (opus, writer) dispatched with task-5-fix1-findings.md + the L4.6 minors as a second commit; fix base for its re-review = b44ad40.
2026-09-21 04:39 EDT: Task 7 review (opus): APPROVED — footprints 0.3023/0.5562/1.0640 GiB reproduced in-process from the arm's resolved kwargs (full → n/a); the nine archived lines parse (batch 1, kv_resident None); §4 arithmetic/precedence verified incl. equal peaks (FAIL), batch-4-only (REFUSED), unmet precondition (REFUSED, ratio printed per R-L4-23); no '--' can render; make tables diff-clean; Task 8's four strings render. Important (plan-mandated): spikes > 8 outside (reconstruct|kernel, 32K) never reported → Ruling R-L4-24 (report every such row; refusal scope unchanged). Minors taken: absent precondition on a kernel-arm pod → REFUSED (Ruling R-L4-25); duplicate-role guard; latency.py docstring; three-context footprint pin. Deferred: the real print untested vs the regex; the archive seam; coord_budget fallback; quant unmodelled; dry_run ignored. Handoff: prereg §3/§4/§11 'not in LatencyRecord' superseded by A1.3 — Task 8's amendment states it. Fix round 1 (sonnet) queued behind the Task 5 fixer's writer slot; findings in task-7-fix1-findings.md.
2026-09-21 04:46 EDT: Task 5 fix round 1/5 DONE @ 6330274 (opus; fp32-output comparisons: 2e-4 at n_splits == 1 via B = 16 (U (16,1024,64), 32 tiles, one split), 4e-3 across splits and for batch independence; _triton_eligible/_shapes_eligible + n_splits_for helpers CPU-tested; nine pointers' device guard; inv_freq/batch refusals; the jit body untouched) + L4.6 minors @ 725fcf1 (docstrings; companion lines pinned as non-records; a bad config raises). 661 passed / 8 skipped. Scoped re-review (opus, static) dispatched on b44ad40..725fcf1. Task 7 fix round 1 (sonnet, writer) dispatched with task-7-fix1-findings.md; fix base for its re-review = 725fcf1.
2026-09-21 05:02 EDT: Task 5 re-review of round 1 (opus, static): Critical 2, Important 3/4, M-5/6/7 ADDRESSED; the jit body untouched; the L4.6 minors commit verified (counts right; companion lines pinned; bad config raises); Critical 1 OPEN — the mechanism is exactly as ruled (fp32 outputs, B = 16 one split full loop, n_splits_for as the one source) but the 2e-4 MAX bar was set from a one-row residual: at B = 16 the emulated max is 3.63e-4 / 9.17e-4 (rms 1.0e-5 / 1.2e-5). → Ruling R-L4-26 amends R-L4-21: the tight case asserts max ≤ 2e-3 AND rms ≤ 1e-4, both printed; the other two cases stay at 4e-3. Pickups: an r-48 auto-fallback gpu equality test; two stale 'all three axes' phrases. Fix round 2 (sonnet) queued behind the Task 7 fixer; findings in task-5-fix2-findings.md.
Task 5: fix round 1/5 (6 addressed, 1 open — the tight bar; commits b44ad40..6330274)
2026-09-21 05:02 EDT: Task 7 fix round 1/5 DONE @ 95d0b2c (sonnet; 663 passed / 8 skipped; make tables diff-clean; the duplicate-role test fabricates the second arm via a load_arm monkeypatch). Scoped re-review (sonnet) dispatched on 725fcf1..95d0b2c. Task 5 fix round 2 (sonnet, writer) dispatched with task-5-fix2-findings.md; fix base for its re-review = 95d0b2c.
2026-09-21 05:08 EDT: Task 7 re-review of round 1 (sonnet): all 5 ADDRESSED (the NOT STEADY STATE pass unfiltered by role/ctx; the 32K refusal block byte-identical; R-L4-25 scoped to 'kernel-role arm present'; the duplicate-role test fabricates a distinct second key; footprints pinned and produced); 7 passed / 0.29 s; no new breakage.
Task 7: fix round 1/5 (5 addressed, 0 open; commits 725fcf1..95d0b2c)
Task 7: complete (commits 27d3593..95d0b2c, review clean after one fix round)
2026-09-21 05:22 EDT: Task 5 fix round 2/5 DONE @ c3071ce (sonnet; tight case max ≤ 2e-3 AND rms ≤ 1e-4 printed; r = 48 'auto' equals the reference on CUDA) + L4.6 minors 2 @ 48d5b65; 663 passed / 8 skipped. Scoped re-review (sonnet) dispatched on 95d0b2c..48d5b65.
Task 8 BASE = 48d5b654b1e7b1470d27778585d92e65c2f5f9dd (dispatched 2026-09-21 05:22 EDT, opus implementer; brief task-8-brief.md + global-constraints.md; the amendment additionally names the on-pod gpu test bars (R-L4-21/26), the axis wall-clock allowance, and the superseded 'not in LatencyRecord' sentences; report task-8-report.md; expected 667 passed + 8 skipped).
2026-09-21 05:23 EDT: LAUNCH PREP (read-only): vast.ai credit $80.80, no running instances; cheapest A100 SXM4 40 GB offer with reliability ≥ 0.99 right now = 36878329 at $0.734/h (R 99.9, 4.7 Gbps down, 41 GB VRAM — the reconstruct arm's 64K b1 peak 12.8 GB + 15 GB weights fits); the kernel-smoke bar 5.0 h ≈ $3.7. Main still 278bd01; the L3 harvest still uncommitted (the owner has not reopened that session). The launch waits for Task 8's review (the pod YAML + Amendment 1 committed and pushed) and follows D-011: DECISIONS line with the prereg's first-commit SHA (71c9be6, on the L3 lane — a strict ancestor of this lane), the launch SHA, the offer, the rate, the bar, the credit, and the precondition's evidence paths (tests/test_kernel_reference.py, tests/test_kernel_path.py; the on-pod gpu run + kernel_check.jsonl per A1.2). NOTE for the merge: DECISIONS/STATE appends on this lane will conflict at EOF with the L3 session's Stage-1 appends on main — resolved by keeping both, in date order.
2026-09-21 05:28 EDT: Task 5 re-review of round 2 (sonnet): R-L4-26, P-1, P-2 all SATISFIED (dual bars over the whole B = 16 fp32 diff; the r = 48 construction passes __post_init__ and the wrapper's refusal order reaches the pow2 check first; the remaining 'three axes' in records.py:671 is drained()'s three call sites — a different concept); 8 passed / 8 skipped; kernel body untouched. Deferred: nothing has executed on CUDA — the pod's pytest -m gpu is the first run.
Task 5: fix round 2/5 (3 addressed, 0 open; commits 95d0b2c..c3071ce)
Task 5: complete (commits a2f0d10..c3071ce, review clean after two fix rounds; reviewed-not-run)
2026-09-21 05:38 EDT: Task 8 implementer DONE @ 17b4e7f (opus; 667 passed / 8 skipped / 72.2 s; module 1.36 s of 1.5; prereg +148/−0; <L4 tokens filled; launch --dry-run prints MAX_HOURS=5 and REFUSEs on the unpushed HEAD as designed). Concerns: module time near its bar (pod.check's git subprocesses); suite 72 s of 90; a '--' vs em dash in A1.4's first bullet (verbatim); §8's watchdog polls re-checked (169 within 145/241). Task reviewer (opus, as a scientist) dispatched on 48d5b65..17b4e7f.
Task 9 BASE = 17b4e7f71eeb3d722023ee7ce51bed32c7258347 (dispatched 2026-09-21 05:39 EDT, opus implementer; brief task-9-brief.md + global-constraints.md; report task-9-report.md; expected 672 passed + 8 skipped; the pod is NOT launched by this lane).
2026-09-21 05:58 EDT: Task 8 review (opus, as a scientist): APPROVED, no Critical/Important — the pod loads and pins §3's design (check task first; 39 pod hashes distinct; the reference arm/task files untouched over the whole lane); Amendment 1 append-only (the first 458 lines byte-identical; 148 insertions, 0 deletions); every number re-derived: 0.78 min, 26.5 min ≈ 0.45 h, 2.25 h point, 5.0 = 2.22×, $1.0–1.7 / $2.3–3.7, ≤ 86 ≈ 90 log lines, BUDGET_ITERS 169 < the 600 floor (25 h of polls), footprints 0.3023/0.5562/1.0640 GiB = ADR's 0.151/0.139/0.133×, the archived lines parse 9/9; the A1.2 bars match the shipped tests; the prereg's first commit 71c9be6 is a strict ancestor; all six cited SHAs real and right; the launch guards fire (dirty tree / unpushed HEAD refusals). Minors (deferred to the final fix wave as a dated 'Amendment 1 — correction' note + a YAML doc word, none blocking the launch): 'below it' → 'above it'; the pg19-val corpus load unbilled (minutes, inside 3.2 h of headroom); the YAML doc's '1.8 h point for the nine latency cells' compresses cells + overhead; A1.2's 'met when' omits the errors == 0 conjunct the renderer applies; a test docstring nit; module timing 1.3–2.8 s under a concurrent writer; the '--' dash.
Task 8: complete (commits 48d5b65..17b4e7f, review clean)
LAUNCH SEQUENCE (after Task 9 commits and its reviewer is dispatched — the launch's commits are the controller's, one writer): pick the offer (A100 SXM4 40 GB, reliability ≥ 0.99, avoid host family 5116338x; ≈ $0.73/h now) → `scripts/pod.py launch --pod kernel_smoke --offer <id>` from the clean pushed L4 worktree → watchdog `caffeinate -s -i scripts/pod/watchdog.sh kernel_smoke` (Mac on AC, log in the scratchpad) → a Monitor on `vastai logs` for ===(RUN_SHA|ALL_DONE|RUN_FAILED|RUN_TIMEOUT|SELF_DESTRUCT), `[kernel_check`, `[latency`, `[error]`, Traceback, and the gpu-test summary → commit results/kernel_smoke/manifest.json + the DECISIONS D-011 addendum 12 (launch-entry-draft.md) + a STATE line by explicit path, push.
2026-09-21 05:55 EDT: LAUNCH BLOCKED — A1.2 promises the gpu tests run on the pod before pod.py run, but boot.sh has no such step and installs no pytest (the Task 8 reviewer verified the bars, not the mechanism). Ruling R-L4-27: a generic pre_run hook (pod YAML → boot.sh runs it after the ENV block, before the timeout hand-off; ===PRE_RUN_BEGIN/END_rc markers; [pre_run] rows kept by the watchdog; harvest → pre_run.txt + manifest.pre_run_rc; check refuses rc != 0 or unrecorded; kernel_smoke.yaml declares its gpu tests) + a dated 'Amendment 1 — correction' note; config_hash moves (no manifest exists). Cost if wrong: ≈ 60 lines and one review round. Task 10 (opus) dispatched with task-10-brief.md; Task 9's reviewer runs read-only in parallel.
Task 10 BASE = 084f6277e8570a47cea1046d85b9012a2df2b791
2026-09-21 06:11 EDT: Task 9 review (opus): APPROVED with follow-ups — the knob proven (pre path byte-identical incl. comments; 0 rotations under post on both ingest paths, measured; V untouched; the refusal unbypassable; the test bites at 0.865 on a silent fallback; batched rows inherit); the prereg's arithmetic re-derives (0.151×/0.282× from bug_footprint; 10.7/11.7 h; $5.3–8.7/$10.8–17.8; 416/53,248 rows) but SEVEN prereg items: §10's reading path cannot run (gate1.load refuses a second Llama pod; ARM_TRACKER lacks the arm) → a pre-registered snippet; §11 omits 'a pass still fails Gate 3's 0.25×'; §4 lacks the n = 24 caveat and the (≥ 7, 0) resolution; §2's ceiling mis-attributed (1.00/1.00/0.92/0.75 on the four Gate-1 tasks; n = 12 codes 2 vs this pod's n = 24 codes 4); §9's 18.6 h needs its rate set named (Gate-1 §9's 0.6/3.1/5.0); no perplexity effect size/CI; REFUSED without a consequence. Ruling R-L4-28: the prereg is UNLAUNCHED → revised in place (the L3 pre-launch precedent, D-011 addendum 9), no amendment; cost if wrong: none (the file governs nothing yet). Fix round 1 (opus — prereg/statistics) queued behind Task 10's writer slot; findings in task-9-fix1-findings.md.
2026-09-21 06:17 EDT: Task 10 implementer DONE_WITH_CONCERNS @ 3fafd14 (opus; 677 passed / 8 skipped / 76.9 s; prereg +53/−0; make check failure set unchanged; the pod's pytest resolves kvdlra from the clone's src via pyproject pythonpath — verified in an export tree). Concerns: (1) vast.ai's --env parser may split the quoted command → Ruling R-L4-29: hand the command over as PRE_RUN_B64 (base64, decoded by boot.sh) — one line each side, no first-launch gamble; (2) config_hash omits pre_run when None so other pods' hashes stand (not in the brief; the reviewer verifies); (3) +0.73 s vs 0.5 (three pod.check calls; no subprocess) — accepted; (4) pre_run.txt is a sorted set of lines (the watchdog's sort -u) — stated in the prereg. Task reviewer (opus) dispatched on 084f627..3fafd14; Task 9 fix round 1 (opus, writer) dispatched with task-9-fix1-findings.md; fix base for its re-review = 3fafd14.
2026-09-21 06:31 EDT: Task 9 fix round 1/5 DONE @ 98ce3e9 (opus; +208/−30 in place; the §10 snippet extracted and run on fabricated dirs — clean pair → non-inferior with CI; perturbed pair fires all three refusals; every number re-derived; 677 passed / 8 skipped). Scoped re-review (opus, scientist) dispatched on 3fafd14..98ce3e9.
2026-09-21 06:35 EDT: Task 10 review (opus): APPROVED on every brief item (hook placement; no set -e; rc captured; harvest/check/watchdog; config_hash pop measured 39/40 stable + live manifests OK; prereg append-only; tests non-vacuous) — Critical: the -e PRE_RUN= form is mangled by vast.ai's smart_split (verified on the installed CLI) → R-L4-29 confirmed as required; base64 with padding survives parse_env. Important: rc 0 on an all-skipped run → Ruling R-L4-30: a third refusal (a '^\d+ passed' row required, no failed/error in the summary). Minors taken: pytest --version row; CRLF-safe row regex. Fix round 1 (opus, writer) dispatched with task-10-fix1-findings.md; fix base for its re-review = 98ce3e9.
2026-09-21 06:39 EDT: Task 9 re-review of round 1 (opus): Importants 2–7 and the four minors ADDRESSED with every number re-derived ((7,0) Holm 0.0469; (6,0) 0.0938; n ≥ 34 for the 0.03 clause; ceiling 1.00/1.00/0.92/0.75 from the pre-flight rows; 18.56 h → 39.1 h / $17.6–29.0; measured 37.7 h; 53,248 rows; 12.0× / 72.1×); Important 1 OPEN: the §10 snippet keys the reference by its config name (the runner writes legacy_name 'bugSseed-r64-h256' — 0 vs 33 result files) → false REFUSED + KeyError on real records; and 2 of §11's 5 refusals are prose only (not-decidable TOST reads 'fail'; a missing Stage-1 record shrinks n_paired silently). Fix round 2 (sonnet, exact edits + a runner-keyed fixture run) queued behind Task 10's fixer; findings in task-9-fix2-findings.md.
Task 9: fix round 1/5 (6 addressed + 4 minors, 1 open — the snippet's arm key and two refusals; commits 3fafd14..98ce3e9)
2026-09-21 06:53 EDT: Task 10 fix round 1/5 DONE @ 438b45f (opus; PRE_RUN_B64 verified byte-intact through vastai.utils.parse_env while the quoted form arrives cut; R-L4-30's refusals with a counts-row anchor; pytest --version row; the gate drops the ruling's -q because addopts already carries one and a second prints no counts row (measured: zero rows) — Ruling R-L4-31: accepted, the test pins it; 678 passed / 8 skipped; prereg additions only). Scoped re-review (opus) dispatched on 98ce3e9..438b45f; Task 9 fix round 2 (sonnet, writer) dispatched with task-9-fix2-findings.md; the launch follows Task 9's commit (clean tree) and the Task 10 re-review.
2026-09-21 07:03 EDT: Task 10 re-review of round 1 (opus): ALL RESOLVED — the base64 value arrives byte-intact (256 == 256) through vastai.utils.parse_env and decodes to the YAML command, the old quoted form arrives truncated (counterfactual reproduced); the decode precedes the BEGIN marker; rc is pytest's under pipefail (measured); the third refusal verified on four fixtures and on a REAL hook run (CUDA-less → 'no passed row'); a second -q measured to print zero counts rows (terminal.py:1229-1231) — R-L4-31 stands; prereg additions only (36+/0−) with the command byte-identical to the YAML; no new breakage. Out of scope: pre_run.txt is sorted by the watchdog (presence, not order, is read); a base64 decode failure surfaces only at check (two refusals).
Task 10: fix round 1/5 (all addressed, 0 open; commits 98ce3e9..438b45f)
Task 10: complete (commits 084f627..438b45f, review clean after one fix round) — THE LAUNCH GATE IS CLEAN
2026-09-21 07:04 EDT: OFFER CHOSEN for kernel_smoke: 36878336 (A100 SXM4 40 GB, $0.734/h, reliability 0.999, 4.7 Gbps down, Slovenia host 224078 — the pre-flight's clean host family; the $0.709 German offer has R 0.992 and 0.7 Gbps). Bar 5.0 h ≈ $3.67, expected 2.25 h ≈ $1.65; credit $80.80; no instances running. Launch sequence starts when Task 9's round-2 commit leaves the tree clean: push → launch → watchdog (caffeinate, scratchpad log) → Monitor → commit manifest + DECISIONS addendum 12 + STATE → push.
2026-09-21 07:20 EDT: LAUNCH — attempt 1 (offer 36878336 → 51903328) created unstartable ('success': False, intended stopped, 'resources unavailable'), destroyed 07:18 EDT; attempt 2 (offer 44397667 → instance 51903816, $0.815/h, Czechia, R 0.995) RUNNING at 07:19; watchdog pid 54645 under caffeinate; Monitor armed; launch manifest + D-011 addendum 12 + STATE committed and pushed.
2026-09-21 07:25 EDT: Task 9 re-review of round 2 (opus): RESOLVED by execution — A = legacy key ('bugSseed-r64-h256'); (B) non-inferior with CI, (D) the three refusals, (E) 'REFUSED — missing Stage-1 record, TOST not decidable' (the pre-fix snippet died with the KeyError and read 'fail' on E/H — the decision-consequential half); the three sentences verified against the ADR/Gate-1 §9; no new breakage. Deferred: the ppl axis has no printed-refusal path (a window-set mismatch or an arm with no pplw rows raises; nothing printed is wrong) — an Amendment item with a two-line try/except; the snippet is not CI-pinned.
Task 9: fix round 2/5 (1 addressed, 0 open; commits 438b45f..a691d44)
Task 9: complete (commits 17b4e7f..a691d44, review clean after two fix rounds)
ALL TASKS COMPLETE (1–10). Final whole-branch review next.
2026-09-21 07:25 EDT: FINAL REVIEW dispatched — whole-branch review (fable) + ponytail-review (opus) on 91d149d..99ea29f (24 commits, 45 files, +8,032/−94; package 596 KB; deferred list final-review-deferred.md, 21 lines). The pod runs meanwhile (Monitor ba92ll528; watchdog pid 54645).
2026-09-21 07:40 EDT: PONYTAIL-REVIEW (opus; ponytail-review.md): net −100 lines possible of +8,032 — duplication, one unreachable guard, two dead seams; provenance/record/gate machinery, the Triton wrapper's refusals, the batch guards in _footprint/persist, _OnlineSoftmax and every requested knob left alone.
Ruling R-L4-32 (the cut list for the final fix wave; each cut keeps every test's assertions and every rendered string byte-identical; the golden is the tripwire): TAKE (1) delete run_ppl's unreachable _rows guard (frontier.py:566-573; _footprint raises first; R-L4-19's documented deviation becomes moot); (2) the four `rope_basis == "post"` identity branches → one early return in _mat_rope/_mat_rope_at (the pre path's condition is false — golden; prereg/postrope_r128.md's 'four-site' wording adjusted in place, unlaunched); (3) tests/test_kernel_reference.py's middle_and_dense → the layer's own _factored_middle() + _decode_peek(dense_only=True); (4) tests/test_kernel_path.py's inner run() → import _teacher_forced from tests.test_batched_cache; (6) kernel_operand_dtype validation → the GIST_DTYPES lookup (string-only; call sites checked); (7) drop the constant-true find_spec on kvdlra.kernel.triton_kernel; (8) tables.py's unused archive= seam → the constant; (9) one role_of(cfg) shared by latency_roles and pod.py's kernel-arm test (no re-load of arms); (10) the two config-hash sweeps subsumed by test_pod_manifest's full sweep; plus the small ones: runner.py's duplicated corpus-load/sha block → a helper shared with _ppl_rows; test_kernel_smoke_pod's P50/PEAK → imported from test_latency_table; test_kernel_triton's _cuda → the imported _mid_to. LEAVE: (5) _ensure_mid_cache re-assembly via _mid_coords (it changes the reconstruct path's matmul assembly the golden and the quant tier protect — 11 lines are not worth a settled path); boot.sh's plain-PRE_RUN hand-launch fallback (documented in the prereg correction); N_LAYERS' unreached rows; the prompts.py re-export layer (R-L4-9's shape); kernel_smoke.yaml's doc: paragraph (its config_hash is the RUNNING pod's). Cost if wrong: a fix-wave re-review round.
2026-09-21 07:35 EDT: POD 51903816 first readings — pre_run gate rc=0: '7 passed, 1 skipped (the 1B dump), 8 deselected in 20.08 s' → the Triton kernel compiled and passed on CUDA: vs reconstruct < 1e-2 (3 params), the tight case max ≤ 2e-3 ∧ rms ≤ 1e-4, the 16-split and batch-independence 4e-3 cases, no-middle + refusals, the CUDA tiny-model end-to-end (R-L4-21/26 bars met). kernel_check prompt 0 (8B, bf16): match=1 (32/32 greedy tokens token-exact), per-layer max|Δ| on the first kernel step 2.5e-4 … 8.205e-3 (worst layer 8), all < 1e-2 — thin margin at the worst layer; the precondition reads the WORST over 16 prompts, so a prompt with a layer over 1e-2 would REFUSE the gate per the pre-registered rule (reported as measured, not repaired). RUN_SHA a691d44 confirmed. ETA ALL_DONE ≈ 09:35 EDT.
2026-09-21 07:47 EDT: FINAL WHOLE-BRANCH REVIEW (fable; final-review-report.md): READY WITH FIXES, no Critical — reconstruct path proven untouched (AST-level function bodies identical; guards unchanged); six rounding points mapped reference ↔ Triton; provenance re-derived (prereg byte-identical through §11; every amendment commit and 71c9be6 ancestors of a691d44; config_hash == manifest; PRE_RUN_B64 decodes to the YAML); make test 678/8/76.1 s verbatim; run_latency's real print parses live; forbidden words clean. Important: (1) FactoredMiddle.cat([m]) copies the middle per layer per step at batch 1 (≈ 2 % of the bar; a conservative handicap on the running pod's kernel arm); (2) no backend attestation in the records; (3) the absolute 1e-2 bar scales with |out| — not a defect, an amendment question before any re-run. Deferred triage folded into ONE fix wave (final-fix-wave-brief.md: A the Importants + the regex test; B the R-L4-32 cuts; C the prereg notes incl. the ppl-axis printed refusal; D the plan). Ruling R-L4-33: (3) is recorded in the outcome entry and a dated correction note, never as a change to the running pod's rule. Fix wave (opus) dispatched.
2026-09-21 08:05 EDT: POD 51903816 kernel_check, 11 of 16 prompts in: match=1 on prompts 0,1,2,4,5,7,9,10 (8); match=0 on 3 (first_mismatch step 13), 6 (15), 8 (4) — three misses already, so the ≥ 14/16 clause cannot be met (max 13/16); worst max|Δ| per prompt 5.6e-3 … 1.464e-2 (prompt 7, layer 26), with prompts 2 (1.233e-2) and 10 (1.003e-2) also over the 1e-2 bar (layer 31 dominates). READING under the pre-registered rule (prereg §4 + A1.2): the correctness precondition is NOT met → the Week-3 gate reads REFUSED; the nine latency rows are measured and reported, never cited (renderer R-L4-25). INTERPRETATION (for the outcome entry, not a change of rule): the per-layer compare is bf16 output vs bf16 output, so its absolute error scales with |out| (the whole-branch review's Important 3, anticipated before these rows: 1e-2 ≈ one bf16 ulp at |out| ≈ 2–3); and greedy token-exactness between two bf16 implementations diverges at near-tie steps (the CPU fp32 evidence was 16/16). Whether the kernel is 'wrong' or the bar is mis-specified for bf16 is exactly what the pre-registered bar cannot tell apart — the sound path is an AMENDMENT before any re-run: a relative per-layer bar against an fp32-over-bf16-store reference, and a mismatch criterion that attributes divergences to near-ties (the top-2 logit gap at the first mismatch step under the reconstruct path) — then a re-run pod (≈ 2.25 h ≈ $2). Ruling R-L4-34: nothing about this pod's rule moves; the outcome entry records REFUSED with the numbers; the amendment is drafted for the owner's decision (it changes a correctness criterion, so the owner sees it before any launch, even though the lane's pod launches are authorized). Cost if wrong: a $2 pod.
2026-09-21 08:06 EDT: POD kernel_check COMPLETE: 12/16 token-exact (misses: prompts 3 @ step 13, 6 @ 15, 8 @ 4, 14 @ 8); worst per-prompt max|Δ| 5.6e-3 … 1.464e-2 (prompt 7 layer 26); over 1e-2 on prompts 2 (1.233e-2), 7 (1.464e-2), 10 (1.003e-2), 12 (1.405e-2) — layers 26/31 dominate → the precondition is NOT met on both clauses (12 < 14; 1.46e-2 > 1e-2) → Week-3 gate REFUSED per §4/A1.2; latency rows measured, not cited. LATENCY so far (A100 40 GB, batch 1): 16K — full 24.74 ms / kv_peak 2.05 GB (archived 25.89 / 2.05); reconstruct 126.03 ms / 3.25 GB, spikes 4 (archived 103.25 — +22 %, outside the 10 % agreement band → reported); KERNEL 39.19 ms p50 / kv_peak 0.83 GB / kv_resident 0.83, mean 88.0, max 1,992 ms, spikes 5 (≤ 8, no refusal) — the 2 s max and 5 spikes read as Triton recompiles when the middle's tile count changes at absorb events (a performance defect to diagnose: a runtime arg vs constexpr in the wrapper), not the absorb-event rebuild; 16K ratio 126.0/39.2 = 3.2×, memory 0.83 < 2.05. 32K — full 30.13 ms / 4.08 GB (archived 27.49 / 4.08; within 10 %). Awaiting reconstruct/kernel 32K and the 64K rows, ALL_DONE, the harvest.
2026-09-21 08:18 EDT: AMENDMENT 2 DRAFT (opus; amendment2-draft.md) for the OWNER: recommends (A) amend + Task 11 + a re-run pod kernel_smoke2 (≈ $1.0–1.7; this pod's records carry neither max|ref| nor logits, so no new bar can re-read them). Relative per-layer bar max_l(max|Δ_l|/max|ref_l|) ≤ 2^-6 = 4 bf16 ulps (the CPU calibration's worst term 1.47 u; the split adds ≤ 1.14 u; an algebraic error is ≳ 250 u); near-tie margin M = 2^-5 × the step's top reconstruct logit (8 u; √32 layers' roundings in quadrature → 5.66 → 8), calibrated first on the tiny model in bf16 at $0. Findings: the two failing clauses are DISJOINT (over-bar prompts 2/7/10/12 all token-exact; mismatching prompts 3/6/8/14 all under the bar) — an arithmetic error would put them on the same rows; the recompile hypothesis is REFUTED by reading (triton_kernel.py:39-41 passes n_dense/n_mid/tiles_per_split/n_splits as runtime scalars) — the spikes are first-touch specializations or something else, to be separated from the 16-step absorb lattice by a log-only companion line (Task 11). Nothing launched; the owner decides.
2026-09-21 08:20 EDT: POD latency 32K — reconstruct 248.30 ms / kv_peak 6.45 GB / spikes 3 (archived 188.27 → +32 %, outside the 10 % band; reported); KERNEL 56.12 ms p50 / kv_peak 1.08 GB / kv_resident 1.08 / mean 68.6 / max 232.6 / spikes 4 (≤ 8); full 30.13 / 4.08. The Week-3 conditions at 32K on the pod's own rows: memory 1.08 < 4.08 (margin 73 %, not marginal) ✓; speed 248.3/56.1 = 4.4× ≥ 3.0 ✓ (3.35× against the archived 188.27, also ≥ 3). The gate nonetheless reads REFUSED on the precondition — the systems bars are met on measurement; the correctness bar's bf16 specification is what withholds the pass (→ option A). 64K — full 42.23 ms / 8.14 GB (archived 36.97 → +14 %); reconstruct/kernel 64K rows pending.
2026-09-21 08:21 EDT: FINAL FIX WAVE DONE @ abc8af8 (opus; 4 commits: fw1 the batch-1 middle handed as-is + select_backend attestation (record field/regexes/§7 column) + the print-vs-regex test; fw2 twelve ponytail cuts, net −33; fw3 kernel_smoke 'Amendment 1 — correction 3' appended (i)–(v) + postrope §10 printed refusal; fw4 plan counts + note; 680 passed / 8 skipped / 76.4 s; prereg deletions 0; results/ untouched). Scoped re-review (opus) dispatched on 99ea29f..abc8af8.
2026-09-21 08:25 EDT: L6 CLEAN-CLONE GATE at abc8af8 (scratchpad l6-L4-abc8af8.txt): make env 2.9 s; make test 680 passed / 8 skipped / 105 s COLD (76 s warm on this Mac; the L3 gate recorded 60.8 s warm / 128 s cold — the 90 s gate is read warm/CI); make tables diff-clean; make figures OK; make check OK on the five live manifests, failing only on results/gate1_preflight (partial, known), results/gate1_preflight_rerun (env.txt uncommitted in the L3 worktree — the L3 session's) and results/kernel_smoke (launch-only until harvest); tree clean; forbidden words: 0 real hits (5 grep hits are the repo path 'kv-dlra' — exclude it in future greps); commit messages clean. GATE PASS conditional on the kernel_smoke harvest (as L2/L3's were).
2026-09-21 08:36 EDT: FINAL FIX WAVE RE-REVIEW (opus): ALL ADDRESSED, no new Critical/Important — A1 identity asserted by data_ptr (numerics unchanged; the kernel wrapper makes its own contiguous copies), A2 select_backend the single resolution point + the record fields/regexes/column, the running pod's field-less records replayed through harvest/check/render (check rc 0, 'backend' n/a), A3 the real print parses, every B cut proven behaviour-preserving (golden + rope_basis green; kernel_smoke.yaml 0-byte diff), C the prereg additions only + the postrope snippet run (REFUSED with no traceback vs the old uncaught ValueError), D the plan; gates re-run 680/8/79.2 s, ruff/format/mypy clean, tables diff-clean.
PARKED (the SDD breaker: no second fix wave) — Ruling R-L4-35: (1) kernel_check binds the kernel cache while the reconstruct twin decodes (two 4096-ctx caches coexist on the pod; no recorded number reads memory on that axis) → a Task-11 line (`del cache` before the twin); (2) under rope_basis: post the identity returns after cos/sin are built, so the unlaunched postrope arm recomputes an un-memoized cos/sin per decode step per layer (output bit-identical; a decode-time cost) → move the early return to _mat_rope/_mat_rope_at in Task 11 or Phase 2; (3) the 'torch.float32' alias string is now accepted (harmless widening); (4) the one-cache-one-backend raise fires only with min_sv_frac > 0 (the intended trade). Out of scope: OSError in the snippet's except tuple (a later amendment); the plan's embedded Task-9 brief still says 'four-site' (annotated as superseded at plan:45); the suite's 79 s vs the 90 s gate — ~12 % headroom, worth trimming before the next lane.
FINAL REVIEW: complete (fix wave 99ea29f..abc8af8, re-review clean, 4 parked). L6 gate PASS at abc8af8 (conditional on the kernel_smoke harvest). Remaining: the harvest → outcome commits → the ledger copy → STATE done → the finishing menu.
