# STATE — append-only, dated. Every lane appends on start / block / done; L6 signs "done" entries with commands + output paths. Never edit an earlier entry.

## 2026-09-11 · orchestrator · session 1 (kickoff)
- Base: week7 @ ee8c0ab == origin/week7. No local `origin/main` ref. Tree was clean apart from the four review docs under docs/plan/ (untracked; committed this session).
- Read in full: ICML2027_PLAN §1–2, CODE_AUDIT (summary + Parts A/B/C), PC_REVIEW (AC meta-review), KICKOFF_WEEKS0-3, results/w20-close-report.md.
- Gates confirmed OPEN after Week 20 (one line each):
  - G1 (does the tracker matter?): the W20 swap arm ran Oja at the untuned defaults eta0=1.0/decay=1e-3 (validated pre-RoPE schedule is 20.0/0.03; bug_cache.py:890-897 passes only n_seen); the FD arm crashed on every trial (linalg.svd non-convergence); frozen-prefill-SVD and no-gist controls never ran → no valid tracker ablation exists.
  - G2 (edge over faithful 2-bit?): single-shot-prefill KIVI-2 on Llama 16K scores mk 1.00 / mv 0.83 vs r64 1.00 / 1.00 (2 discordant of 12) → the chunked-prefill multi-value edge was a protocol artifact; Mistral/Qwen and 32K untested.
  - G3 (kernel win?): the only measured decode path is reconstruct-then-attend: 103/188/508 ms/tok vs full 26/28/37 (4–14× slower), KV peak 3.25/6.45/12.83 GB vs full 2.05/4.08/8.14 (≈1.6×). No kernel exists.
- Scaffolding written verbatim from KICKOFF_WEEKS0-3.md by awk extraction (diff-verified against the pack): CLAUDE.md (Part A, 75 lines), docs/plan/lanes/L0..L7 (Part C), docs/plan/lanes/GATES.md (Part D); the Part E §2 note is prepended to CODE_AUDIT.md; DECISIONS.md created (D-001..D-005 OPEN).
- Tag: `paper-v1-archive` → ee8c0ab (annotated, local until L0's branch is pushed). Nothing that fed a paper-v1 table may be deleted before this tag exists — it now does.
- ponytail-debt: `grep -rnE '(#|//) ?ponytail:'` → 0 markers. Clean ledger.
- Dispatched, read-only on the week7 checkout (reports land in docs/plan/cleanup/): (1) repo-wide ponytail-audit → ponytail-audit.md; (2) static reachability graph from the pod command lines + tests → reachability.md. Both are inputs to the L0 plan and deletions.md.
- Baseline `.venv/bin/python -m pytest -q` on ee8c0ab: running in the background; result appended in the next entry.
- vast.ai credit read 2026-09-11: $23.85. Enough for the filler-realism diagnostic (D-005), not for Gate-1 v2 (≤ 50 GPU-h) — top-up before L3.
- Worktree: .claude/worktrees/L7-paper-correction on branch lane/L7-paper-correction (off week7). L7 dispatch follows; L0 dispatch waits for the two cleanup reports.

## 2026-09-11 · orchestrator · session 1 (cont.) — baseline + Day-1 reports
- Baseline `pytest` on ee8c0ab (Mac, .venv, torch CPU): 70 s wall. 1 FAILED — tests/test_w10_ruler_quant.py::test_quant_arm_runs_through_retrieve_and_footprint: quanto's C++ `unpack` extension JIT-compiles and needs `ninja`, absent from the venv (`RuntimeError: Ninja is required to load C++ extensions`). Environment, not code → L0 adds `ninja` to the dev deps (or the test skips cleanly without it) so `make test` is green on a clean clone. 1 SKIPPED (bf16 QR unsupported on CPU), 1 strict XFAIL (Week-15 `seed_scores` latent bug). Pass/fail counts appended when the single-`-q` rerun finishes.
- ponytail-audit committed: docs/plan/cleanup/ponytail-audit.md — net −26,600 lines / −13 deps possible. Facts that change the lane briefs: (1) the W20 swap runs the torch `oja_step`/`fd_step` inside streaming_torch.py:307+, so the numpy integrators/oja.py and frequent_directions.py are dead — L1's FD numerics fix and Oja plumbing target streaming_torch.py and bug_cache.py:890-897, not the numpy files; (2) morph_cache.py and shadow_cache.py are reachable only via the superseded w11_baselines.sh — shadow_cache stays (the ShadowKV v1 rows must appear in `make tables`, L2 §6), morph_cache is a delete candidate unless a paper-v1 table cites it (reachability report decides); (3) configs/ is three empty dirs — the YAML configs are new construction; (4) hqq and optimum-quanto are load-bearing through backend strings with no direct import — keep; (5) `dlra` is the package name (`kvdlra`), so the CI forbidden-word grep must match the branding words in prose (DLRA as a word, "dynamical low-rank", "BUG integrator", honest, marquee, flagship), not the identifier; a package rename is out of scope for L0.
- ponytail-debt: docs/plan/cleanup/ponytail-debt.md — 0 markers, clean ledger.
- Dispatched in lane worktrees, all $0, nothing launched, nothing merged: L7 paper correction (lane/L7-paper-correction, base ee8c0ab) → diff + report for owner review; L2prep filler-realism prereg + scripts/pod/w21.sh (lane/L2-generator-v2) → makes D-005 a one-word go; L4adr kernel ADR 0001 with FLOP/HBM model (lane/L4-kernel) → D-002. Reachability agent still running read-only on the week7 checkout → docs/plan/cleanup/reachability.md.

## 2026-09-11 · orchestrator · session 1 (cont.) — baseline counts, L7 done
- Baseline counts (single `-q` rerun on ee8c0ab): **388 passed, 1 failed (ninja/quanto, environment), 1 skipped, 1 xfailed, 71 s**.
- L7 DONE_WITH_CONCERNS on lane/L7-paper-correction (base ee8c0ab): 4 commits 3537a77 (a) · 6388822 (b) · 0376dcb (c) · 6d5ae54 (d); paper/main.tex +47/−50, paper/arxiv-abstract.txt 1 line. Orchestrator review: (a) `tab:swap` + the load-bearing claim deleted, replaced by "ablation required; first attempt invalid (untuned Oja schedule; FD SVD non-convergence; controls never run)"; (b) seven sites now say "in-house generator only; 0.00 on the official anchor"; (c) the analytic 1.06× survives only as the superseded estimate, measured 4–14× / 1.6× KV peak stated; (d) the literal "beats fixed-rank iSVD everywhere" was already gone (retracted in 4d37043) — the agent instead added one clause scoping 1.01–1.03× to the final basis (judgment call, inside §4.1; owner may revert). No dangling refs; braces/environments balanced; no TeX toolchain here so no PDF build. Forbidden-word grep on added lines: only pre-existing words carried inside re-wrapped lines (abstract single line, "Honest fp32-at-rest bytes", "Two honesty notes", "BUG's honest bytes") — no new occurrences; the wording pass owns them.
- Ruling (orchestrator): the contributions bullet (main.tex 154–155) still asserts the 2-bit multi-value edge flat; L7 adds the abstract's own qualifier "under chunked prefill" there as commit L7e. Why: it is the paper's claims list and the settled fact is that the edge is a chunked-prefill artifact on the one cell tested. Cost if wrong: three words to revert.
- L7 report lists 12 further sentences that are now unsupported but out of scope (6× the §4.6 chunked-prefill multi-value edge incl. every bolded `tab:fairquant` mv cell; 6× near-oracle/Oja numbers quoted outside §4.1) plus 4 framing sites — punch list for the Phase-3 wording pass, kept in the L7 report (scratchpad L7-report.md; to be copied under docs/plan/cleanup/ when the lane merges).
- arXiv: no ID/URL/receipt anywhere under paper/ (all arXiv hits are citations in refs.bib) → strengthens D-001 option (a): v1 never posted, no v2 needed.

## 2026-09-11 · orchestrator · session 1 (cont.) — Day-1 deliverables landed
- Reachability report committed: docs/plan/cleanup/reachability.md (92 files / 24,183 LOC under src+scripts): pod-sh 10 (2,519) · tests-only 9 (1,744) · pod-sh+tests 35 (10,410) · scripts-only 4 (1,029) · none 34 (8,481). No dynamic imports anywhere. Six "none" files are the paper's own report/figure generators (w19_a1_report, w19_a2_misses, w19_figures, w20_close_report, w16/w17_intervals; w19_fork_report reached only via a function-body import in w20_close_report) — they fold into tables.py/figures.py per the L0 plan, their outputs stay as narrative reports.
- **L0 plan written: docs/plan/plans/2026-09-11-L0-repo-cleanup.md — AWAITING OWNER APPROVAL (Day-1 milestone).** Phase A (Tasks 1–5, Day 2): paper-v1 archive → results/paper-v1/<pod>/{trials,cells}.jsonl + r64 CPU bit-identity golden; kvdlra.eval.stats; `make tables` diff-clean vs a golden transcribed from main.tex@ee8c0ab; YAML arms/tasks/pods + parity test vs legacy build_arms; pod.py (run/launch/harvest/check + manifest) / figures.py / dump_kv.py skeletons, Makefile, Dockerfile → merge so L1/L2/L4/L5 start. Phase B (Tasks 6–11, Day 4): tracker/baselines/util moves + dead-module deletion behind the golden; bug_cache dead paths; eval scripts → kvdlra.eval.* driven by configs (trials that raise are recorded as error, never skipped); deletions with evidence; dep prune (+ninja); README; reachability + forbidden-word tests in CI; ponytail after-report. Scope rulings inside the plan: L5's manifest/--check work is delivered by L0 Task 5 (one owner for pod.py); tier/ and kernel/ dirs are created by L1/L4; splitting bug_cache.py is NOT in L0.
- L2prep done (lane/L2-generator-v2 @ 8bce69a, e08cfef; not merged): prereg/filler_realism.md + scripts/pod/w21.sh (MODE `filler`: 5 arms × 4 tasks, no --depths, same generator defaults, filler=wikitext). Findings: `--filler` accepts cycle|wikitext|wikitext-103|pg19 (w10_ruler.py:632-638) — pg19 exists but streams a sentence bag through the fragile legacy loader, so `wikitext` (WikiText-2 test pool) is the $0 choice; a PG-19 *chapter* source does not exist (generator v2). Cost: 9.1 h base / 18.3 h at 2× → $11–20 (D-005's $3–6 was 3–4× low: tasks-per-cell, the Week-14 error again). Launch blockers, all small: watchdog hardcodes results/w19-*; ROWS regex lacks W21_; quant arms need the -devel image; the SKIP-on-exception path can shrink n (prereg excludes any cell with n≠12).
- L7e done: af28096 qualifies the contributions bullet ("… loses under chunked prefill"). Lane total: 5 commits, +48/−51 in paper/. Patch + report sent to the owner for review (G7). Nothing merged.
- L4adr (kernel ADR) still running.

## 2026-09-11 · orchestrator · session 1 (cont.) — L1 plan written; L7 sent for review
- L7 patch (5 commits, +48/−51) and its report delivered to the owner (SendUserFile). G7 waits on the owner's review; merge via finishing-a-development-branch after.
- **L1 plan written: docs/plan/plans/2026-09-11-L1-harness-hygiene.md — AWAITING OWNER APPROVAL; executes after L0 Phase A merges** (uses the post-L0 paths; same edits at the old paths if Phase B lags). Tasks: (1) orthonormality tripwire + guard — `orth_fix_tol=1e-3` (re-orthonormalize via thin QR, coordinates/core/quant tier rotated by R), `orth_abort_tol=1e-1` (→ OrthonormalityError recorded as a trial error), `qr_every=None` (the experimental factor), per-layer diag rows drained to diag.jsonl; bit-identical on any stream that never trips (the L0 golden proves it); the audit's synthetic ratchet is the regression test in both directions. (2) live-rank billing in `_footprint`. (3) FD fix = shared augmentation + `_svd_core` with an eigh-of-Gram fallback (+jitter on a second failure); `oja_step` requires eta0/decay; the cache plumbs `oja_eta0=20.0/oja_decay=0.03` from config; tracker string `isvd` ("bug" deprecated alias). (4) `eval/recon.py`: every tracker through the `step(u,b,block)->(u,b,rot)` contract, error on `U @ C` with rot-carried coordinates (the cache's own model); methods svd_oracle/isvd/fd/fd2/oja(tuned, boundary-optimum rejected)/frozen_prefill_svd/random_basis; 1B study on CPU now, 8B dump command recorded for a pod. (5) perplexity on WikiText-103 TEST / PG-19 val, window 2048 × 32, per-window rows, paired bootstrap + TOST(±0.05). (6) `configs/pods/hygiene_table4.yaml` + `prereg/hygiene_table4.md` (guard on/off × floor on/off; ≈6 GPU-h) — launch is an owner decision.
- Root causes confirmed in code while planning: streaming_torch.py:307-347 `oja_step(..., eta0=1.0, decay=1e-3)` defaults and bug_cache.py:890-897 passing only `n_seen`; streaming_torch.py:350-390 `fd_step` uses a plain residual QR (no rank-revealing admission) and a dense SVD of the shrunken core — the non-convergence class.

## 2026-09-11 · orchestrator · session 1 (cont.) — L2 plan written
- **L2 plan written: docs/plan/plans/2026-09-11-L2-generator-v2-and-baselines.md — AWAITING OWNER APPROVAL; executes after L0 Phase A merges (rebase the lane).** Tasks: (1) svd_oracle docstring + D-004 evidence, `palu` confined to the oracle module; (2) faithful KIVI arm `kivi2_faithful`/`kivi4_faithful` = G=32, R=128, per-channel K / per-token V, fp16 single-shot prefill with a plain DynamicCache, then post-hoc chunked quantization (exact, groups are 32 consecutive tokens), KIVI's residual rule, scales/zeros + actual residual billed; streaming arm relabelled; KVQuant → DECISIONS D-006 (defer); (3) generator v2 `eval/gen.py`: pg19/arxiv/wikipedia/essays materialized per pod with SHAs, contiguous-text haystacks (the "chapter" the Day-1 diagnostic lacks), balanced haystack×depth×code-family design enumerated by trial index (seed = random draws only), 7-digit numbers + word-pair code families, official task semantics incl. multiquery and 4-hop vt, `prompt_sha256` pairing golden; v1 builder moved unchanged; (4) SnapKV/PyramidKV/EA at k∈{0.10,0.15,0.25}, ThinK+SnapKV composed, `baselines` table with the omitted ShadowKV post-fix and ea_k0.25 v1 rows; (5) `ss2_families` + `l2_smoke` pod configs + preregs (launch = DECISIONS); (6) OjaKV adapter + version-pinned arm.
- Prereg ownership settled in the plans: L2 owns filler_realism / ss2_families / l2_smoke; L5 owns gate1_tracker_swap_v2 / bf16_gist / kernel_smoke / hygiene_table4 (the last written by L1 Task 6, L5 reviews).

## 2026-09-11 · orchestrator · session 1 (cont.) — L3/L5 plan; L4 ADR drafted; session close
- **L3+L5 plan written: docs/plan/plans/2026-09-11-L3-L5-gate1-bf16-prereg.md — AWAITING OWNER APPROVAL; executes after L1 + L2 merge.** L3: `frozen` + `random` trackers, byte-matched no-gist arm (H computed, 5% pin); Gate-1 v2 pods in two stages sized from the harvested per-trial times (Stage 1 Llama+Qwen 16K ≈ 41 GPU-h at 1×, 82 h at 2× — over the 50-h line at 2×, cuts pre-committed: drop `random`, then n=16 for oja/fd; Stage 2 Mistral + 32K primaries ≈ 64 h); `gate1` table (Holm-corrected McNemar on isvd-vs-frozen / isvd-vs-nogist, TOST ±0.02 ppl, `FAILED (k errors)` never `--`) and `gate1_verdict()` implementing the ICML2027_PLAN branch rule. L5: `gist_dtype=bfloat16` storage (fp32 accumulation, billed at 16 bits, default fp32 bit-identical; the per-carry re-rounding risk is named and measured) riding the Gate-1 pods as a 7th arm; preregs gate1_tracker_swap_v2 / bf16_gist / kernel_smoke; prereg-order launch refusal test.
- **L4 ADR drafted on lane/L4-kernel @ e432df3 (not merged): docs/adr/0001-factored-attention-kernel.md (248 lines, OPEN) + docs/adr/0001-cost-model.py (stdlib; self-check pins the footprint to bug_footprint's 0.151×/0.086× and full-KV resident to the measured 4.08 GiB).** Recommendation (iii) Triton tile-wise reconstruct-inside-attention primary; (i) post-RoPE r=128 as a parallel accuracy row; (ii) rejected — under GQA 4:1 the RoPE-pair factorization is per query head while reconstruction is per KV head, so (ii) costs 4× (iii) on keys (532 vs 282 kFLOP/token). 32K model rows (GFLOP / KV-HBM GB / resident KV GB): full 17.2/4.29/4.29 (b1), 137/34.4/34.4 (b8); reconstruct 34.4/16.3/5.29, 275/130/42.4; (iii) 293/0.59/0.60, 2341/4.74/4.78. Week-3 gate reachable in principle (KV peak 0.60 vs 4.29 GB at 32K; 10.7 ms roofline vs the measured 188 ms reconstruct). Concerns recorded in DECISIONS D-002.
- Session-1 close. Merged to week7 this session: docs only (kickoff pack, cleanup reports, STATE/DECISIONS, four lane plans). Nothing under src/ changed on week7. Lane branches awaiting the owner: L7 (5 paper commits, diff sent), L2 (prereg + w21.sh), L4 (ADR). Worktrees: .claude/worktrees/{L7-paper-correction,L2-generator-v2,L4-kernel}.
- Owner decisions queued (DECISIONS.md): D-001 arXiv (recommend: not posted, no v2); D-002 kernel ADR accept; D-003 GPU split + top-up (credit $23.85 vs ≥ $45 for Gate-1 Stage 1); D-004 svd_oracle rename (done in plans) / real Palu later; D-005 filler diagnostic go (recommend the 3-arm cut, $6–12); plan approvals L0 (Day-1 milestone), L1, L2, L3/L5; L7 diff review (G7).
- Process notes for the next session: SendMessage is not available in this harness — follow-ups to a lane go to a fresh agent with the report file; TaskOutput on a local agent dumps its transcript into the controller context — never use it, wait for the notification; agents must not append to STATE.md (three worktrees share it) — the orchestrator writes every STATE entry on week7.

## 2026-09-13 · orchestrator · session 2 — L0 approved, execution started
- Owner approved docs/plan/plans/2026-09-11-L0-repo-cleanup.md. Executing with subagent-driven development in .claude/worktrees/L0-cleanup (lane/L0-repo-cleanup off week7 @ 5c78755); ledger at that worktree's .superpowers/sdd/2026-09-11-L0-repo-cleanup/progress.md (gitignored). Phase A (Tasks 1–5) merges to week7 first so L1/L2/L4/L5 can start.
- Pre-flight rulings (ledger): R1 `pod.py check` skips paper-v1 archive manifests (`converted_by`); R2 Task 7 also prunes the dead retention branches in accounting.bug_footprint; R3 Task 1 makes scripts/_paths.py prepend the repo's own src unconditionally — the shared venv's editable .pth points at the main checkout, so a worktree's scripts would otherwise import the wrong tree.
- 2026-09-13 · L0 progress: Tasks 1–3 complete on lane/L0-repo-cleanup (paper-v1 archive with verbatim raw copies + trials/cells/ppl JSONL; r64 CPU bit-identity golden; kvdlra.eval.stats; `make tables` regenerates Tables 1,2,3,6,7,8 incl. memory columns diff-clean against docs/plan/paper-v1-tables.md). Two plan defects found and corrected by rulings (BH pin; per-arm memory = mean of cell rows). iCloud-sync hazard → D-007. Task 4 (configs + parity) in progress.

## 2026-09-14 · orchestrator · L0 Phase A MERGED to week7 (600c171)
- Merged lane/L0-repo-cleanup Tasks 1–5 (+ fix rounds) with `--no-ff`; merged-tree verification in the main checkout: `make test` 498 passed / 0 failed / 86 s; `make tables` diff-clean vs docs/plan/paper-v1-tables.md; `make check` green. Nothing pushed (owner's call; pods clone from GitHub, so a push precedes any launch).
- What week7 now has: results/paper-v1/<pod>/ (74 pods: verbatim raw/ copies of all 98 source line files, trials/cells/ppl JSONL, self-describing manifests, dumps/llama3.2-1b.sha256); kvdlra.eval.{records,stats,config}; scripts/{tables,pod,figures,dump_kv}.py + scripts/pod/{boot,watchdog}.sh; configs/{arms (28), tasks (10), pods (24)}; Makefile (env/test/tables/figures/check/clean; venv bin on PATH so torch finds ninja — the old "pre-existing ninja failure" was a PATH artifact); Dockerfile; prereg/README.md; tests/test_golden_cache.py (r64 CPU bit-identity, frozen at ee8c0ab-identical sources) + tests/golden/; tests/test_configs_vs_archive.py (13 documented config-vs-archive divergences, allowlist cannot rot); CI checks out full history.
- Gate: every task review clean after ≤1 fix round; whole-branch review "with fixes" → single fix round, re-review clean; ponytail-review −7 lines applied; L6 clean-clone gate PASS (498 passed / 83 s). Rulings R1–R15 in the lane ledger (.superpowers/sdd/…/progress.md in the worktree); two plan defects corrected (BH pin; per-arm memory = mean of the arm's cell rows); R13 amends GATES G0 (uv.lock stays gitignored); R9 withdrawn (no per-arm trial-count override — task-config variants carry a different n).
- Deferred to Phase B / lanes (ledger): persist/latency parsers for the two unconverted row kinds; source sha256 in archive manifests; golden lib versions; depth-aware cell size (Task 8/L2); results/w19_harvest/done.txt + w18_checkpoint/*.raw.log already tracked (Task 9); requires-python ≥3.11 (tomllib/file_digest; Task 9); the generator must be part of the record key for mixed in-house/official pods (Task 8/L2); Task 8 re-points the golden's `_prefill_chunked` import, never regenerates the golden.
- L1/L2/L4/L5 may now start against this layout once their plans are approved. L0 Phase B (Tasks 6–11) continues on the lane.

## 2026-09-14 · orchestrator · L0 Phase B MERGED to week7 (10c19ac) — L0 COMPLETE, G0 ticked
- Merged lane/L0-repo-cleanup @ 211ef1d (Tasks 6–11 + fix rounds; 28 commits since the Phase-A merge 600c171) with `--no-ff`. Merged-tree verification in the main checkout: `make test` 380 passed / 0 failed / 75.9 s; `make tables` diff-clean vs docs/plan/paper-v1-tables.md; `make check` rc 0; reachability + forbidden-word + r64 golden tests green. Nothing pushed.
- Gate before the merge: every task review clean after ≤1 fix round; whole-branch review (opus) "with fixes" → one fix dispatch + four follow-ups (7ae5aac, d291284, 4714d74, d82c9d1, 211ef1d), fix-range re-review clean (12/12 addressed, no new breakage); ponytail-review applied; L6 clean-clone gate PASS on d82c9d1 (make env 1 s, test 380 passed / 74.9 s, tables diff-clean, figures + check green, tree clean after targets, forbidden words 0). 211ef1d is docs-only (two lines).
- What week7 now has: `src/kvdlra/{tracker/isvd.py,cache/bug_cache.py,baselines/,quant/,eval/{records,stats,config,runner,ruler,frontier,official_ruler,longbench,latency,persist,data}.py,util/,accounting.py}`; five entrypoints `scripts/{_paths,pod,tables,figures,dump_kv}.py` + `scripts/pod/{boot,watchdog}.sh`; 64 omegaconf YAMLs under `configs/{arms,tasks,pods}/` (hydra-core removed); `results/paper-v1/` archive; `docs/plan/{reports,reviews,cleanup}/`; README 110 lines; Dockerfile; pyproject exact pins (requires-python ≥ 3.11, 13 deps pruned, ninja in dev); CI runs make test/tables/figures/check.
- Size: tracked files 573 → 534 (ee8c0ab → 10c19ac); Python LOC under src+scripts+tests 33,738 → 16,989; `git diff --shortstat ee8c0ab 10c19ac` = 868 files, +24,983 / −68,424 (the additions are the archive's verbatim raw copies and docs/plan).
- **G0 ticked in docs/plan/lanes/GATES.md.** Evidence per line: tag `paper-v1-archive` → ee8c0ab; deletions.md 169 path rows in evidence buckets + `Kept despite being a candidate`; reachability.md + tests/test_reachability.py (0 unreachable under src/), LOC delta negative; clean-clone make env && make test green in 75 s (< 90 s); make tables regenerates Tables 1,2,3,6,7,8 diff-clean; every pod that fed a v1 table has a YAML (R14; tests/test_configs_vs_archive.py, 13 allowlisted divergences); Dockerfile + pins, uv.lock stays gitignored (R13/D-008), `pod.py check` exercised on synthetic manifests (tests/test_pod_manifest.py, tests/test_pod_run_records_errors.py), launch/watchdog scripts reused; forbidden-word test green (scope src/configs/scripts/docs/paper), README ≤ 120, ponytail-after.md committed.
- Rulings R16–R39 (full text in docs/plan/cleanup/l0-ledger.md, a verbatim copy of the lane ledger): R16 wording pass owns CLAUDE.md citations; R17 results/*.md narrative reports moved to docs/plan/reports; R18 uncited results/*.json deleted (tag keeps them), cited ones byte-copied; R19 paper `% source:` comments resolved by docs/plan/cleanup/paper-source-map.md instead of editing paper/ (L7 owns it); R20 requires-python ≥ 3.11; R21 morph_cache + its pod-side scripts deleted; R22 only `\includegraphics`-referenced figures kept; R23 docs/reviews → docs/plan/reviews, mkdocs gone; R24 scripts outside the five entrypoints deleted with one bucketed deletions.md row each; R25 deps pruned (list in the ledger); R26 README ≤ 120 lines with the three-command reproduction; R27 litter removed by the orchestrator post-merge (see below); R28 config-schema additions free until the first launched pod carries a config_hash, `TaskCfg.data_dir` + `pod.py prepare` go to L2; R29 `check` fails on any errored trial, no tolerance knob; R30 harvest fills generator from the pod's tasks; R31 latency is a runner generator writing latency.jsonl; R32 golden test docstring-only edits allowed; R33 `[latency]` rows parsed on harvest, `[error] axis=latency` with a batch group; R34 turbo_press kept (Phase-2 composition Pareto); R35 hydra-core dropped, omegaconf direct pin, zero-caller helpers deleted; R36 whole-branch findings adjudicated (tests/ outside the forbidden-word scope; Makefile PATH export fixed for PY="uv run python"); R37 diag rows counted → manifest `diag_skipped`, check fails when > 0; R38 persist stays archive-only until L4 wires it; R39 watchdog ROWS filter keeps `[error]` lines.
- Deferred to lanes (ledger): frontier.py private exports (L2); persist.py as a runner generator + figures.py regex → records.py (L4, R38); `TaskCfg.data_dir` + official RULER/LongBench prep step (L2, R28); depth-aware cell size (L2); `check --log` regression test; the tautological manifest-arithmetic test and the relative imports in tests/test_reachability.py (cosmetic); source sha256 in archive manifests; golden lib versions in the golden JSON; diag rows < 400 chars or a `part=` splitter (L1, R37 note).
- Post-merge duties done in the main checkout: `uv pip install -e ".[dev]"` re-pointed the shared venv's editable install at this checkout's src (it pointed at the L0 worktree); 20 `.DS_Store` files deleted; the 12 `.gitignore` lines that only hid the litter dropped (deletions.md's list; `.DS_Store` and every build-output rule kept); **the litter itself was PARKED, not deleted** — `~/Desktop/kv-dlra-litter-2026-09-14/` (2.7 MB: handover.md, explanation_week_{1_2,3_4,5_6}.md, next-session-prompt.md, compass_artifact_*.md, dashboards/, results/gpu_logs/ = 30 raw pod-log tails from Weeks 11–12 + the 1B/8B runs that no archive cites, results/scratch/ = 9 smoke JSONs) — D-010 asks the owner to delete or keep it; the repo tree is clean either way. Ledger copied to docs/plan/cleanup/l0-ledger.md. Worktree .claude/worktrees/L0-cleanup removed and branch lane/L0-repo-cleanup deleted (fully merged; was 211ef1d).
- Next: L1 / L2 / L3+L5 plans still await approval (L2's lane rebases onto week7 @ 10c19ac before it runs; L1 uses the post-L0 paths as written); L7 diff awaits the owner's review (G7); decisions D-001–D-005, D-007 (the clone is still in iCloud — the quiet point is now), D-010; push week7 before any pod launch.
- Post-merge finding (D-007 addendum 2): iCloud's hidden flag on `.venv/**/*.pth` made Python 3.12.13 skip the editable install; `chflags nohidden` on the four `.pth` files restores `import kvdlra` from outside the repo. Nothing in the repo relied on it (`_paths.py` + conftest prepend `src/`). One more reason for D-007 (a).

## 2026-09-17 · orchestrator · L1 + L2 plans APPROVED; L1 started
- Owner approved docs/plan/plans/2026-09-11-L1-harness-hygiene.md and docs/plan/plans/2026-09-11-L2-generator-v2-and-baselines.md (2026-09-17) and said "start L1". L1 executes now with subagent-driven development in .claude/worktrees/L1-hygiene (branch lane/L1-harness-hygiene off week7); ledger at that worktree's .superpowers/sdd/2026-09-11-L1-harness-hygiene/progress.md. L2 starts after L1 merges and rebases its lane first (both edit eval/frontier.py, eval/config.py, eval/records.py, scripts/tables.py, configs/arms, tests/test_config_parity.py, tests/test_pod_manifest.py).
- Pre-flight (execution kickoff approved by the owner): (1) the plan's synthetic ratchet does NOT ratchet on the shipped step — max ‖UᵀU−I‖ = 6.9e-4 over 1400 steps at n=512/cap 256/block 16, bf16-rounded; the pre-Week-7 augmentation (plain residual QR) reaches 45 within 60 steps; CODE_AUDIT:172 (through the production layer, Qwen r256, 36K tokens) is the finding that holds → Task 1 opens with a systematic-debugging write-up and a DECISIONS finding; the test becomes a pre-W7-vs-shipped contrast + injection. (2) The plan's `reorthonormalize` would leave a triangular core and break the 2r-word core billing → it re-diagonalizes inside the fix branch. (3) `oja_step` pins its rank at the seed block width (the W20 Oja arm ran rank 16 billed as 64) and its `n_seen` freezes at tier saturation → both fixed in Task 3, every Oja number changes (the W20 cell was already void). (4) WikiText-103 test supplies ≈15/7 non-overlapping windows at 16K/32K → PG-19 validation carries the 32-window claim. (5) The 1B recon study writes recon.jsonl + provenance.json, no manifest (a CPU study is not a pod). (6) Table-4 control arms run with both tolerances null so the divergence can reproduce. Rulings PR-1–PR-26 verbatim in the lane ledger.
- Kickoff-pack alignment (Week 1, day 4): L1 is on the Week-1 path (merge-ready ~09-19/20). The other Week-1 items wait on the owner: Table-4 re-run (D-011 opens at merge; needs a push + ~6 GPU-h), kernel ADR (D-002), bf16 gist + gate1 prereg (the L3+L5 plan is unapproved), filler diagnostic (D-005, top-up). Gap outside L1: results/w19_harvest/*.raw are gitignored and hold the only FD-crash evidence (ICML plan 0.4(d) asks for them to be committed) — recommend an L6 micro-task archiving them verbatim under results/paper-v1/<pod>/raw/.

## 2026-09-18 · orchestrator · L1 COMPLETE on lane/L1-harness-hygiene — all reviews clean; docs committed on the lane before the push/launch
- Lane: 18 commits af2ca1a..fbe5d82 (Tasks 1–6 + 4b, each task-reviewed with ≤ 1 fix round and a clean scoped re-review; whole-branch review (opus) "with fixes" → one fix round in three commits (e0f2243 review findings, 5d553f4 fixture consolidation, fbe5d82 ponytail cuts, net −246 lines) → re-review clean, "ready to merge: yes"). Code diff +4,226/−542 outside results/. Suite 453 passed / 61 s; the 12-file bit-identity guard set 121 passed with zero tolerance changes; the r64 golden untouched. `make tables` diff-clean; `make figures`/`make check` green. Rulings PR-1–PR-32 and R-L1-1–R-L1-20 with every review verdict: docs/plan/cleanup/l1-ledger.md (verbatim ledger copy).
- Decisions recorded: D-012 (guard defaults + the non-reproducing synthetic ratchet), D-013 (perplexity protocol), D-014 (Oja rank pin + re-tune + parity golden), D-015 (live-rank billing). week7 merged into the lane (D-011) so the prereg's citations resolve at the launch SHA.
- Study: results/recon_1b (12,800 rows, provenance.json; ≈3.1 h CPU). Block 16, pre-RoPE keys, mean over 5 docs, stored-representation error relative to the rank-r oracle: incremental SVD 1.019× / 1.025× / 1.052× at r = 16 / 64 / 256; FD at ℓ = 2r 1.26–1.37× (respects the rank-2r oracle); Oja (re-tuned, keys (5.0, 0.3)) 1.27× → 2.04×; frozen-prefill SVD 1.05× → 1.32×; random basis 3.6× → 15.7×; the r = 256 floor variant 2.29× with the stored width collapsing to 87–172. Figure: `scripts/figures.py rank-sweep --in results/recon_1b/recon.jsonl --out docs/paper/figures/rank_sweep_1b.pdf`.
- G1 (ticked in GATES.md): lines 1–3, 5, 7 met; line 4 met as amended (crash class, D-012); line 6 half (1B; 8B needs a dump pod whose data cannot return through the log — design pending); line 8 open until the Table-4 pods are harvested.
- Next (this session): L6 clean-clone gate on the lane head → push lane/L1-harness-hygiene → `pod.py launch` hygiene_table4_llama first, hygiene_table4_qwen after its first sample rows harvest cleanly (D-011) → watchdogs → the merge into week7 STOPS at the finishing menu (owner's call). L2 rebases after the merge.

## 2026-09-18 · orchestrator · Table-4 pods HARVESTED — G1 line 8 closed (outcome 3(a)); L1 ready for the merge decision
- Three pods under prereg Amendments 1–2 (16 windows, PG-19 validation, 16K): the guard alone takes Qwen r=256 from +10.94 to +0.17 bits/token over `full`; periodic and tolerance forms indistinguishable (+0.003); the floor closes the remaining 0.15 bits (floor arms TOST-equivalent to `full`). Qwen r=128 without the guard also degrades (+0.69); Llama r=256 is unaffected either way (+0.02) although its basis loses orthonormality too. Full numbers, Holm, cost (.8; ≈ $12 of it idle billing while this Mac slept 12:27–22:42) and the harness gaps found: DECISIONS D-011 addendum 8. Records committed at 287dc45 (`make check` green); `results/hygiene_table4_*_v2_aborted_*/` keep the voided runs' evidence.
- Post-gate lane work since the 09-18 L6 pass at ce48b29: Task 7 (repair-then-abort guard; `_svd_core` left-factor check with a 1e-2 tolerance; NaN-safe tripwire — one review with a Critical fixed + two scoped re-reviews clean), Task 8 (env.txt from the log's ENV block; scripts + tests; review in progress), prereg Amendments 1–2, D-011 addenda 1–8. L6 clean-clone gate re-run at 287dc45: GATE PASS (make test rc=0 81s :: 469 passed, 15 warnings in 69.85s (0:01:09)). Evidence: scratchpad l6-L1-final.txt → copied into the ledger.
- The `svd_nonorthonormal` core repair fired once per pod on the first sample's rank-deficient seeding block (Llama 0.921, Qwen 0.506) and applies to all arms including the controls (Amendment 2 A2.5) — stated with the numbers.
- Open after L1: retrieval neutrality of the guard at r128 (a follow-up pod, in-house task); the 8B rank sweep (dump pod design); the owner's decisions D-001–D-005, D-007 (two more iCloud hazards logged), D-010; L2 rebases onto week7 after the merge. **Merge of lane/L1-harness-hygiene into week7: stopped at the finishing menu — the owner's call.**

## 2026-09-19 · orchestrator · L1 delivered as a pull request (owner chose option 2)
- `week7` pushed to origin (the L0 merges + docs, already public through the lane's merge commit) so the PR base is current; PR: https://github.com/hkrishna42/kvdlra/pull/4 (lane/L1-harness-hygiene @ 2bb1ed0 → week7). The worktree stays for PR feedback. The merge happens on GitHub or by the owner locally.

## 2026-09-19 · orchestrator · L1 MERGED into week7 (6160315); main fast-forwarded to week7
- Owner: "Merge the Branches to the main branch" (after choosing option 2). Merged lane/L1-harness-hygiene into week7 with `--no-ff` (6160315; PR #4 closed as merged); merged-tree verification in the main checkout: `make test` green, `make tables` diff-clean, `make check` rc 0. `main` fast-forwarded to week7 and pushed (main was a strict ancestor: ee8c0ab → 6160315, 62 commits: L0 + L1 + plan docs).
- Post-merge duties: shared venv re-pointed at the main checkout (+ `chflags nohidden` on the .pth files); the L1 worktree and branch removed (local + origin); final ledger copy refreshed.
- Next: L2 (approved) — create `.claude/worktrees/L2-generator-v2` from lane/L2-generator-v2 rebased onto week7 @ 6160315; L2 Task 1 is the filler-realism diagnostic pod (D-005). Owner decisions still open: D-001–D-005, D-007 (three iCloud/sleep hazards logged), D-010. Harness follow-ups for L6: harvest non-shrink guard for every record file; watchdog under `caffeinate -s` + a pod budget self-destruct; the retrieval-neutrality and 8B-sweep pods.

## 2026-09-19 · orchestrator · everything merged to main; week7 retired — ONE branch
- Per the owner: L7 (paper corrections), L4 (kernel ADR) and L2prep (filler-realism prereg) merged into week7 (`--no-ff`), the dead `w21.sh` removed and the L7 vocabulary fixed (D-016), the merged tree verified (469 passed / tables diff-clean / check rc 0 / ruff clean), then `main` fast-forwarded and pushed; week7 and the historical week1-scaffold/week2/week3 deleted on origin; lane branches deleted locally. Working branch: **main**.
- What main holds beyond L1: the corrected paper (paper/main.tex, paper/arxiv-abstract.txt), docs/adr/0001-* (OPEN), prereg/filler_realism.md (awaiting D-005; L2 Task 1 amends it to the pod-YAML path).
- Next: L2 — branch `lane/L2-generator-v2` afresh from main (the old lane is merged), worktree `.claude/worktrees/L2-generator-v2`, SDD as for L1; Task 1 = the filler-realism diagnostic pod under D-005/D-011. Owner decisions open: D-001, D-002, D-003, D-004, D-005, D-007, D-010.
- Follow-up after D-016: the abstract's method sentence now names the tracker as a rank-adaptive block incremental SVD with no bound claimed (CLAUDE.md settled facts); `paper/main.tex` still carries the v1 vocabulary in 109 places (pre-existing text, outside L7's sentence-level scope and outside the CI grep's scope) — the Phase-3 paper rewrite scrubs it. A stray harness branch `claude/amazing-tharp-f865db` (one unique commit) was preserved as tag `archive/claude-amazing-tharp-f865db` and deleted, so `main` is the only branch, local and remote.

## 2026-09-19 · orchestrator · L2 started on lane/L2-generator-v2; Task 1 done; filler-realism pods LAUNCHED
- Worktree .claude/worktrees/L2-generator-v2 off main @ 1b57656; SDD as for L1 (ledger at that worktree's .superpowers/sdd/2026-09-11-L2-generator-v2-and-baselines/progress.md, copied to docs/plan/cleanup/l2-ledger.md at merge). Pre-flight audit against main: 24 drift rows, rulings PR-L2-1–21 — the load-bearing ones: any new `TaskCfg` field breaks `make check` on the three live Table-4 manifests (v2 fields go on a `TaskV2Cfg` subclass); the `[trial]` log line carries none of haystack_id/depth/code_family/prompt_sha256 (a harvested pod cannot verify the pairing invariant — the line is extended in Task 2); the plan's 24-cell design test is impossible as written (12 distinct triples); post-hoc KIVI quantization needs no chunk merge; the archived rows the baselines table cites carry `n: null` (no Wilson invented); `palu_footprint`/`press_type "palu"` are the code identifiers left to rename, the archive keys stay.
- Task 1 (commits 82b3a36..56e89ee; review approved, two short fix rounds, re-reviews clean; 474 passed / 78 s; `make check` green): pods `filler_realism` + `filler_realism_cycle`, prereg Amendment 1 (+A1.7), `launch --max-hours` → boot.sh `timeout` + 2 h grace self-destruct, watchdog expiry destroys. Launched 03:45 (D-005 addendum): 51553610 ($0.449/h, bar 18.3 h) and 51553637 ($0.471/h, bar 6 h); watchdogs under caffeinate -s -i.
- Next: Task 2 generator v2 → Task 3 faithful KIVI → Task 4 svd_oracle rename → Task 5 presses + baselines table → Task 6 ss2 pods + prereg → Task 7 OjaKV → Task 8 smoke pod + prereg; whole-branch review + ponytail-review + L6 clean-clone gate; the merge into main stops at the finishing menu unless the owner says "merge L2 when green". Owner decisions open: D-001, D-002, D-003 (credit $97.67 before this launch), D-004, D-005 (now running), D-007, D-010.

## 2026-09-19 · orchestrator · L2 code COMPLETE on lane/L2-generator-v2 (423ecd0) — whole-branch review + ponytail-review + final fix wave clean; Task 7 BLOCKED (D-017); merge waits for the real-text pod's harvest
- Lane: 32 commits 82b3a36..423ecd0 (Tasks 1–6 and 8 each task-reviewed with ≤ 2 fix rounds and clean scoped re-reviews; Task 7 closed BLOCKED with the research recorded in D-017; whole-branch review (opus) "with fixes" → one fix wave in three commits (c437e75 harness: `[stage] dataset_sha256` lines parsed by harvest, watchdog venv python + per-poll dedupe + BUDGET_ITERS from the pod's bar; dd79c38 KIVI empty residual + fold test + kivi2_streaming YAML-comment label; 423ecd0 ponytail cuts −190 lines) → re-review clean. Diff vs main: 69 files, +23.4 K / −154 incl. the harvested cycle pod. Suite 538 passed / 75.6 s in-tree, 77.8 s warm in a clean clone (112 s cold); `make tables` diff-clean (new `table_baselines`); `make check` OK on the four harvested manifests, fails only on results/filler_realism until its harvest.
- BLOCK (Task 7, OjaKV): the authors' code cannot run through this harness (transformers 4.44 + flash_attn replacement model, unpackaged, calibration-checkpoint-dependent, ranks floored at ≈ 0.47× stored state) — D-017 OPEN, recommendation (A) their harness on its own pod at their ratios in Phase 2; G2 line 7 open.
- G2 map (whole-branch review): lines 2 (pairing + balanced-depth tests), 3 (kivi2_faithful), 8 (ShadowKV + ea_k0.25 rows in `make tables`) MET; 5 met as amended (PR-L2-14: palu hits = disclaimer + archive keys + DECISIONS); 1 open until the real-text pod harvests (cycle control harvested: harness replicates the archive); 4 and 6 open pending the ss2/smoke launches (pre-registered: prereg/ss2_families.md 60.6/121.2 GPU-h, prereg/l2_smoke.md 84/168 GPU-h — both need the D-003 top-up); 7 open (D-017).
- Harness facts surfaced (for L6 / CODE_AUDIT): `doc:` is inside `config_hash`, so an arm named by a live manifest has a frozen docstring; `run_ppl` bills press arms after the scored window (≈ 3 % over-bill, flatters ours — fix before any Phase-2 ppl table); the orthonormality repair fires in essentially every 64-absorb window at bf16 on the r64 arm (bug_cache.py's "never fires at the defaults" comment is false on GPU); transformers 5.8 builds one causal mask from layer 0 (PyramidKV decodes token-by-token; its ppl axis is refused as a recorded error); harvested manifests carried no dataset digests until L2.9a; the watchdog called bare `python` (the cycle pod's harvest step failed; the destroy worked); iCloud re-hides the venv's `.pth` files and resurrected a pathspec'd stash mid-fix (D-007 — move the clone).
- L6 clean-clone gate at 423ecd0 (scratchpad l6-L2-423ecd0.txt): env 3 s, test 538 passed, tables diff-clean, figures OK, tree clean, forbidden 0; `make check` rc 2 solely on results/filler_realism's launch-only manifest → GATE PASS expected on the re-run after that harvest.
- Next: harvest 51553610 by hand at ALL_DONE (its watchdog runs the pre-fix script: destroys, cannot harvest), commit records + close D-005 in DECISIONS, re-run the L6 gate, copy the ledger to docs/plan/cleanup/l2-ledger.md, present the finishing menu. Owner decisions open: D-001, D-002, D-003 (top-up before ss2/smoke), D-004 (real Palu), D-006, D-007, D-010, D-017.

## 2026-09-19 · orchestrator · L2 DONE on lane/L2-generator-v2 @ 9ac5eef — D-005 CLOSED (cycled filler retired); L6 gate PASS; stopped at the finishing menu
- Real-text pod harvested (97fd76e; 240 trials, 0 errors): the pre-registered rule fires on all six cells — r64 0.25 / 0.08 / 0.00 (vt 0.00), q4 0.00 ×4, against 1.00 cycled, with `full` at 1.00 / 1.00 / 0.92 and the cycle control replicating the archive; KIVI-2 chunked 1.00 / 0.50 / 0.50, single-shot 1.00 / 0.83 / 0.42. D-005 CLOSED (9ac5eef): the cycled generator is retired from every headline claim; v1 in-house retrieval tables are "diagnostic only"; Gate 1 v2 and the ss2 pods run generator v2 (ss2 prereg amended before launch per its own branch 3). Spend ≈ $5.3; credit $92.5.
- L6 clean-clone gate at 9ac5eef: GATE PASS (make env 2 s; make test 538 passed, 112.9 s cold / 77.8 s warm; make tables diff-clean; make figures OK; make check OK on all five live manifests; tree clean; forbidden-word grep 0). Evidence: docs/plan/cleanup/l2-ledger.md (verbatim ledger copy). GATES §G2 ticked (lines 1, 2, 3, 8 met; 5 as amended; 4, 6 pre-registered; 7 open, D-017).
- Rulings the owner should read (all in l2-ledger.md): PR-L2-5 (TaskV2Cfg subclass keeps live hashes), PR-L2-7/23 (design cell; source rotation decoupled from family), PR-L2-10 (no chunked post-hoc quantization), PR-L2-14 (rename scope), PR-L2-18/19/20/21 (D-005 design: five arms + a $2 cycled-replication control; budget at measured rates; self-destruct), R-L2-2 (materialize skips > 2 M-char docs), R-L2-3 (cycle host destroyed at 47 min "loading"; cycled-`full` bought), R-L2-4 (vt's value takes the code family), R-L2-5 (PyramidKV token-by-token; ppl refusal), R-L2-6 (Llama 16K in the ss2 pods), R-L2-8 (OjaKV blocked → D-017), R-L2-9 (ponytail cuts taken/kept), R-L2-10 (final fix wave; merge after the harvest).
- **Merge of lane/L2-generator-v2 into main: stopped at the finishing menu — the owner's call** (the kickoff's closing line "Merge L2 to main once done" is read as intent; the hard rule asks for "merge L2 when green").

## 2026-09-19 · orchestrator · L2 MERGED into main (e116212) — "merge L2 when green"
- Owner said "merge L2 when green". Merged lane/L2-generator-v2 (30 commits, 82b3a36..6053478) into main with `--no-ff` (e116212); merged-tree verification in the main checkout: `make test` 538 passed / 86.9 s, `make tables` diff-clean, `make check` OK on all five live manifests (filler_realism, filler_realism_cycle, hygiene_table4_llama/qwen_r128/qwen_r256), `ruff` clean, tree clean; pushed. Post-merge: the shared venv's editable install re-pointed at the main checkout (+ `chflags nohidden` on the .pth files; two iCloud twins deleted); the L2 worktree and lane branch removed (local + origin). One branch: main.
- Next lane: L3 (Gate 1 v2) on generator v2 — its prereg must be written against `ruler_v2_16k_g1.yaml` (n=24) with the D-005 outcome as the measured baseline (the r64 configuration retrieves 0.25/0.08/0.00 on real text at 16K); the ss2 prereg's branch 3 amendment (`ruler_v2_*`) precedes any ss2 launch; both launches and the smoke pod wait on D-003's top-up (bars 121 + 168 GPU-h vs credit $92.5). Owner decisions open: D-001, D-002, D-003, D-004, D-006, D-007, D-010, D-017.

## 2026-09-19 · orchestrator · D-007 closed — the clone moved to `~/src/kv-dlra` (out of iCloud); venv rebuilt; all gates green at the new path
- Owner: "Move the clone out of iCloud now (D-007)". Moved, venv rebuilt, `make test` 538 / tables diff-clean / check OK ×5 / ruff clean; SESSION_PROMPT_L3.md's cwd updated. Every later session starts at `~/src/kv-dlra`. The `.pth`-hidden and `* 2.py`-twin hazards end here; the "cwd resets between turns" harness note in the L3 prompt still applies.

## 2026-09-20 · orchestrator · L3 + L5 lane in progress on lane/L3-gate1-tracker-swap-v2; Gate-1 pre-flight pod LAUNCHED
- Worktree .claude/worktrees/L3-gate1 off main @ 278bd01 (the repo moved to ~/src/kv-dlra mid-session, D-007). SDD as for L1/L2; ledger at that worktree's .superpowers/sdd/2026-09-11-L3-L5-gate1-bf16-prereg/progress.md (pre-flight drift table PR-L3-1–30, rulings R-L3-1–15). Merged so far on the lane (each task-reviewed, every fix round re-reviewed clean): L3.1 frozen/random trackers + byte-matched no-gist arms `nogist_h2423`/`nogist_h4460` (7f3e546); the pre-flight pod + prereg (8db2db2, 09dbe2f, 27b5767, 18102a9) and the `[stage] cell … elapsed_s=` timing line harvested into `manifest.cell_elapsed_s` (03fba42); L5.1 `gist_dtype` bf16 storage, billed at 16 bits, default fp32 bit-identical, drift generator `recon.bf16_gist_drift` (63dfa80, a27058d); the Gate-1 prereg `prereg/gate1_tracker_swap_v2.md` (4c8b5d7, ce994e5, 27b5767) — 32 perplexity windows, an exclusive A/B-vs-C rule (the perplexity route to A/B is TOST-gated; TOST α = 0.05 as an intersection-union test), per-stage Holm families, refusals for error rows / frozen dispatch / the byte match; L3.2 Stage-1 pods `gate1_v2_stage1_{llama,qwen}` (bar 82 h each), the `gate1` table and `gate1_verdict()` (4bcfdb5, d78cea6, 9e77314; a per-family refusal round in flight); the bf16 and kernel-smoke preregs (71c9be6; a fix round in flight).
- Pre-flight LAUNCHED (D-011 addendum 9): instance 51722149, offer 31632919, launch SHA 9e77314, bar 17 h, ≈ $5.6 expected. Its harvest feeds the Gate-1 prereg's Amendment 1 and decides whether Stage 1 is re-sized.
- Owner decisions open: D-003 (Stage-1 top-up: bar 164 GPU-h, $74–121; credit $92.5 − the pre-flight), D-001, D-002, D-004, D-006, D-010, D-017; GATES §G3 line 1's "≤ 50 GPU-h" cannot hold at the measured rates (per-pod bars 82 h). Next: L3.3 harness follow-ups → whole-branch review + ponytail-review + L6 gate → finishing menu (the merge waits for the pre-flight harvest so CI is not left red).
