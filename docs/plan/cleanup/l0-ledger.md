# L0 lane ledger — copied verbatim from the lane SDD workspace at merge time

Source: `.claude/worktrees/L0-cleanup/.superpowers/sdd/2026-09-11-L0-repo-cleanup/progress.md`, copied 2026-09-14 after the Phase-B merge (10c19ac). Rulings R1–R39 live here; docs/plan/STATE.md summarizes them. Task supplements (task-8/9/10/11-notes.md) and per-task reports were not copied.

---

# SDD ledger — plan: docs/plan/plans/2026-09-11-L0-repo-cleanup.md
Worktree: /Users/hari/Desktop/kv-dlra/.claude/worktrees/L0-cleanup (branch lane/L0-repo-cleanup, base 5c78755 = week7). Venv: symlink to the main checkout's .venv.
Spec: docs/plan/lanes/L0_repo_cleanup_reproducibility.md + CLAUDE.md + docs/plan/lanes/GATES.md §G0. Owner approved the plan 2026-09-13.

## Pre-flight conflict scan (2026-09-13)
| pair / task | shared file or interface | produces vs consumes | finding | ruling |
|---|---|---|---|---|
| T1 ↔ T3 | scripts/tables.py; records.read_jsonl | T1 defines convert-v1 + records; T3 adds build | consistent | — |
| T2 ↔ T3 | stats.mcnemar_exact(dict, dict) | T3 calls with {(seed,trial): hit} | consistent | — |
| T4 ↔ T8 | config.arm_kwargs; legacy build_arms | T4 parity imports w10_frontier; T8 deletes it and re-pins vs tests/golden/legacy_arm_kwargs.json | consistent (order) | — |
| T5 ↔ T1 | Makefile `check` loops results/*/manifest.json | T1 archive manifests lack config_hash | CONFLICT: `make check` would fail on archives | Ruling R1: `pod.py check` treats a manifest carrying `converted_by` as a paper-v1 archive → prints "archive, skipped", exit 0 — archives are static data. Cost if wrong: an archive escapes checking. |
| T5 ↔ T8 | pod.py run / runner.run_pod | T5 dry-run writes manifest+env only; T8 adds the loop | consistent | — |
| T6 ↔ T8 | press/compat → baselines/compat; kind "morph" in build_arms | T6 moves + updates importers before T8 moves w10_ruler | consistent (order); if morph_cache is deleted T8 drops kind "morph" | note for T8 |
| T7 ↔ accounting | retention modes attn/energy/blend | plan lists bug_cache only; accounting.bug_footprint has _TRACK_SCORE/_TRACK_SURPRISE branches | GAP | Ruling R2: T7 also prunes the dead retention branches in accounting.bug_footprint and keeps tests/test_accounting.py's anti-drift pin green. Cost if wrong: a few lines to restore. |
| all scripts | scripts/_paths.py prepends src only when kvdlra is NOT importable; the shared venv's editable .pth points at the MAIN checkout's src → a script run in a worktree imports the wrong tree | environment | CONFLICT | Ruling R3: T1 makes `_paths.py` prepend the repo's own src unconditionally (one line). Cost if wrong: none — the repo's own src must win for its own scripts. |
| T1 golden | test_golden_cache placeholders (make_tiny_model_and_cache, ingest, reconstruct_middle) | real fixture/methods identified by the implementer | self-consistent | note for T1: exercise the real chunked-ingest path (attach + chunked prefill, seed_hh_warmup, surprise tier) on a tiny CPU model |
| T10 ↔ T5 | forbidden-word scope includes scripts/pod/*.sh copied from w18/w19 (words in comments) | T10 rewords | consistent (order) | — |
| T9 ↔ T1 | T9 deletes results/*-lines.txt + pertrial after T1 archived them | consistent (order) | — |
Per-task self-consistency: T1 expects 192 / 756 records (matches `grep -c '^\[trial\]'`) ✓ · T2 Holm [0.03,0.06,0.06], BH [0.03,0.04,0.045] ✓ · T3 golden transcribed from main.tex@ee8c0ab ✓ · T5 dry-run must work without a GPU ✓ · T8 test signature run_pod(pod, out, model, dry_model) ✓ · T10 regex excludes kvdlra / kv-dlra ✓.

## Progress
Task 1: dispatched 2026-09-13 (BASE 5c78755; implementer model opus; brief task-1-brief.md; report task-1-report.md)
Baseline in worktree @ 5c78755: 388 passed, 1 failed (ninja/quanto, pre-existing), 1 skipped, 1 xfailed, 68 s
Task 1: implementer DONE_WITH_CONCERNS (commits 1da5b3d, 20bfa05); review package .superpowers/sdd/2026-09-11-L0-repo-cleanup/review-5c78755..20bfa05.diff
Task 1: review (opus) → Needs fixes. Important #1: 15 pods partially converted (196 evidence rows dropped: 175 ppl=, 9 ms/tok=, 6 stored_ratio=, 6 persist save=), manifests silent; the report's proposed Task-9 gate would pass them. Minors: cwd-relative globs in convert_v1; golden imports w10_frontier._prefill_chunked (Task 8 must re-point, not regenerate); per-source count audit not captured in a test; "ten reader scripts" comment is six; duplicate (task,ctx,arm) cells in w19-fork-llama / w17-llama8b cells.jsonl (Task 3 must not pool blindly); hh_budget=32 in the golden (deliberate, stronger); _paths.py bundled in the archive commit (disclosed).
Ruling R4 (fix round 1): every source file (71 *-lines.txt + 27 *-trials.txt) is byte-copied verbatim into results/paper-v1/<pod>/raw/<basename>; ppl rows get parse_ppl_lines → ppl.jsonl; manifests record per-source total / parsed-per-kind / unconverted line counts and a test asserts they add up for every pod — repeatable after Task 9 deletes the sources. Cost if wrong: ~1–2 MB of duplicated text under results/paper-v1.
Note for Task 3: do not pool duplicate (task, ctx, arm) cells; take the trials.jsonl records where they exist (cells are aggregates of the same runs).
Note for Task 8: re-point tests/test_golden_cache.py's `_prefill_chunked` import to kvdlra.eval.ruler; never regenerate the golden.
Task 1: fix round 1/5 (implementer sonnet, DONE; commit 97f1507); re-review package .superpowers/sdd/2026-09-11-L0-repo-cleanup/review-20bfa05..97f1507.diff
Task 1: fix round 1/5 (4 addressed, 0 open; commits 20bfa05..97f1507); re-review (sonnet): all findings addressed, no new Critical/Important.
Task 1: minor (deferred): tests/test_records.py:215 `sum(parsed)+unconverted==lines` is tautological (unconverted is derived by subtraction) — an independent per-line three-regex check would make it meaningful.
Task 1: minor (deferred): scripts/tables.py:106 clobber guard removes re-run idempotency — `convert-v1` now requires `rm -rf results/paper-v1` first; document in the subcommand help.
Task 1: complete (commits 5c78755..97f1507, review clean)
Task 2: dispatched 2026-09-13 (BASE 97f1507; implementer model sonnet; brief task-2-brief.md; report task-2-report.md)
Task 2: implementer NEEDS_CONTEXT — plan defect: the BH pin [0.03,0.04,0.045] is the non-monotone per-rank transform; the adjusted (cummin-enforced) values for [0.01,0.04,0.03] are [0.03,0.04,0.04], which scipy returns. Ruling R5: correct the pinned value to [0.03, 0.04, 0.04] (scipy is right; the plan was wrong). Cost if wrong: none — the arithmetic was re-derived by hand. Ruling R6: the "verbatim" test/code may be edited for mypy/ruff mechanics (ArrayLike param types, float casts, no semicolons, dict.fromkeys) with no asserted value changed except R5.
Task 2: re-dispatched (fresh implementer, sonnet) with R5/R6.
Task 2: implementer DONE (commit 3ab3569, resumed with R5/R6). Aside to ledger (deferred, for L6): deselecting the ninja test surfaces an order-dependent failure in tests/test_w19_official_ruler.py — test isolation issue, pre-existing.
Plan corrected on the lane (R5): docs/plan/plans/2026-09-11-L0-repo-cleanup.md BH pin → [0.03, 0.04, 0.04].
Task 2: review package review-97f1507..3ab3569.diff; reviewer sonnet.
Task 2: review (sonnet) → Approved. Minor (deferred): McNemar.n_paired never asserted (inherited from the brief); module docstring says "ported from" w15/w18 scripts while wilson is a scipy re-implementation (value-level port).
Task 2: complete (commits 97f1507..3ab3569, review clean)
Task 3: dispatched 2026-09-13 (BASE 6585b83 [= 3ab3569 + the R5 plan fix]; implementer model opus; brief task-3-brief.md; report task-3-report.md)
Task 3: implementer DONE (commit c5427ce): all six tables diff-clean on first build; selection pod-scoped (20 cross-pod key collisions); duplicate (seed,trial) raises; T8 mean = mean of rounded cells (paper's convention; pooled gives 0.80). Concern: memory columns not regenerated — CellRecord drops `sbits`. Review package review-6585b83..c5427ce.diff; reviewer opus.
Task 3: review (opus) → Approved with 1 Important (Table 6 OMIT_MEM footnote is false) + lane-level Important (memory columns not regenerable: CellRecord drops sbits; per-trial pods have no cells.jsonl — aggregate rows live in differently named line-file pods). Ruling R7: fix round 1 makes the memory columns regenerable — CellRecord gains `sbits` (re-convert; trials byte-identical), each table's memory column comes from the mapped aggregate pod (explicit MEMORY_SOURCE map with a comment; convention per table as the paper's caption states: fp16-equivalent `ratio` vs stored-bits `sbits`), golden gains the paper's memory values; plus: drop the false T6 footnote, compute T8's pooled mean, cwd-independent test paths, a .tex round-trip test, FileNotFoundError → SystemExit, Table 1's `feat. n` column from an architecture-constant map (not a result). Skipped: paper's \mathbf emphasis in T1/T2/T3 (no rule behind it). Cost if wrong: one re-convert of cells.jsonl (reproducible).
Task 3: fix round 1/5 (implementer opus, DONE; commit fbaba5d); deviation: per-arm memory = mean of the arm's cell rows (≤1e-3 spread asserted); re-review package .superpowers/sdd/2026-09-11-L0-repo-cleanup/review-c5427ce..fbaba5d.diff (sonnet)
Task 3: fix round 1/5 (7 addressed, 0 open; commits c5427ce..fbaba5d); re-review (sonnet): all addressed, no new breakage. Deviation accepted: per-arm memory = mean over the arm's cell rows (ratio_stored_bits is a run-level quantity; 24/24 v1 matches vs 22/24 first-task), ≤1e-3 spread asserted, footnoted in every table (MEM_RULE).
Task 3: environmental: stray "<name> 2.py" duplicates appeared under scripts/ and tests/ during a full test run (macOS conflicted-copy naming; repo lives under ~/Desktop) — removed by the reviewer. Implementers must commit by explicit path and delete any "* 2.*" files.
Task 3: complete (commits 6585b83..fbaba5d, review clean)
Task 4: dispatched 2026-09-13 (BASE fbaba5d; implementer model opus; brief task-4-brief.md; report task-4-report.md)
Task 4: implementer DONE (commits 35f5b77, bcb3f8c): 28 arms / 9 tasks / 24 pods; parity 34/34; arm_kwargs deviates from the brief's code (brief's version had the wrong key set for bug/bugevict and wrong coord_budget for the eviction control); coord_budget/quant_budget/hh_retain/w_key carried in cache:; pods mode-named; gpu_budget_h 0.0 (v1 budgeted dollars); no configs for storage/persist/latency benches or W10/11 baseline pods. iCloud: 128 empty duplicate dirs under results/paper-v1 deleted. Review package review-fbaba5d..bcb3f8c.diff; reviewer opus.
Task 4: review (opus) → Needs fixes. Important: configs/pods/w18_g1{,_qwen,_mistral}.yaml list isvd_r64_h256_seed_q4, an arm those pods never ran (they ran the unseeded bugS-r64-h256-q4, invalid); contradicts their own doc. Minors: parity T fixed at 16384 (parametrize 16K+32K); ppl_32k_w8 task for w18_g4's 8 windows; `chunkable` in the test's IGNORED set; shared kwargs dict between arm["kwargs"] and the lambda default (dict(kwargs)); pod files mix "what v1 ran" vs "what a re-run should do" (one doc sentence); ⚠️ fd_r64_h256_seed's legacy_name has no archive rows (v1 crashed) — Task 8 join must exempt it. Fix round 1 → sonnet.
Task 4: fix round 1/5 (implementer sonnet, DONE; commit 9da2f7c); re-review package review-bcb3f8c..9da2f7c.diff (sonnet)
Task 4: fix round 1/5 (6 addressed, 0 open; commits bcb3f8c..9da2f7c); re-review (sonnet): all addressed, no new breakage.
Task 4: minor (deferred): configs/tasks/ppl_16k.yaml and ppl_64k.yaml carry a stale "(w18_g4) ran ... 8 windows" doc sentence (w18_g4 never used them).
Task 4: complete (commits fbaba5d..9da2f7c, review clean)
Task 5: dispatched 2026-09-13 (BASE 9da2f7c; implementer model opus; brief task-5-brief.md; report task-5-report.md)
Task 5: implementer DONE (commits fcefeac, 6fe212d, ac5dd90): pod.py run/launch/harvest/check + manifest (R1 archive skip); boot.sh/watchdog.sh; figures.py from the archive (3 paper figures kept, verified vs v1); dump_kv.py sha256/verify + dumps/llama3.2-1b.sha256 (175 files); Makefile env/figures/check/clean; Dockerfile; prereg/README. Concerns: make check glob widened; `sha256 --dir` subcommand; docs/paper/figures/ neither committed nor ignored (paper still points at ../figures/week19/); check fails a faithful w19_a1 harvest (8-bit control n=4 vs config 12 — no per-arm n in the schema). Review package review-9da2f7c..ac5dd90.diff; reviewer opus.
Ruling R8 (pending review): docs/paper/figures/ is generated output → gitignored until Phase 3 re-points paper/main.tex (after L7 merges); figures/week19/'s three included files stay (Task 9 keeps them). Ruling R9 (pending review): per-arm trial-count override (`PodCfg.n_trials_override: {arm: n}`) consumed by `check` and by the Task-8 runner — added in Task 8 unless the reviewer wants it now.
2026-09-14: owner: 'keep going, merge Phase A when green' — merge to week7 authorized once the Phase-A gate (task reviews, whole-branch review, ponytail-review, L6 clean-clone make env/test/tables/figures/check) is green. Clean-clone make env started in scratchpad/l0-clone.
L6 preview on ac5dd90 (clean clone, scratchpad/l0-clone): make env 6 s (uv cache), make tables/figures/check green (5/5/8 s); make test 84.7 s with 8 FAILED — all "Ninja is required to load C++ extensions": the ninja PACKAGE is installed (1.13.2) but torch looks for the `ninja` EXECUTABLE on PATH, and `.venv/bin/python -m pytest` never puts .venv/bin on PATH. With PATH=.venv/bin:$PATH the 16 quant tests pass (verified). The shared venv's "1 pre-existing failure" was the same artifact.
Ruling R10: Makefile prepends the venv bin dir to PATH for every recipe (`export PATH := $(dir $(PY)):$(PATH)`); Task 9 still adds ninja to dev deps explicitly. Cost if wrong: none. Ruling R8 confirmed: docs/paper/figures/ is generated → .gitignore (the clone showed it untracked). Both go into Task 5's fix round.
Task 5: review (opus) → Needs fixes. Important: #1 `check` only validates cells present (empty/partial harvest passes) — expected cells must come from the config (arms' legacy_name × non-ppl tasks); #2 `harvest` drops split `[pplw] ... part=i/N` lines (PPLW_RE lacks the part group) and partially-whole logs skip the aggregate fallback silently; #3 ppl.jsonl carries two schemas (per-window vs PplRecord) → per-window rows go to pplw.jsonl. Minors: #4 scripts/w12_calibrate_qkey.py imports capture_kv (renamed) → unimportable until Task 9; #5 boot.sh echoes ALL_DONE unconditionally after `pod.py run` (watchdog destroys + harvests nothing); #6 generated docs/paper/figures/* and results/<pod>/{pods.txt,*.raw,watchdog.pid,status.txt} unignored → launch refuses on a dirty tree; #7 PPLW/DIAG regexes live outside records.py; #8 no tests for the git_sha/prereg-ancestor/equality/env rules; #9 polish (load_pod before writes; one manifest reader). Reviewer agrees with R1, the glob widening, `sha256 --dir`, R8-now; disagrees with R9.
Ruling R9 WITHDRAWN: no per-arm trial-count override — a task-config variant carries a different n (precedent ppl_32k_w8); the archive stays exempt under R1. Ruling R11: fix round 1 = Important #1–#3 + Minors #4 (one-word import fix so week7 is never unimportable), #5, #6 (+R8), #7, #8, #9 + R10 (Makefile PATH). Implementer opus.
Task 5: fix round 1/5 (implementer opus, DONE; commits 9b04d0c, 243343e): make test 490 passed / 0 failed / 88.5 s. Concerns: manifest `status` written but not read by check; the generator!="ppl" cell filter unexercised (ppl tasks have empty tasks lists); a truncated [pplw] set aborts the whole harvest; results/w19_harvest/pods.txt + w18_checkpoint/*.raw.log already tracked (Task 9). Re-review package review-ac5dd90..243343e.diff (sonnet).
Phase-A gate: whole-branch review (opus) + ponytail-review (sonnet) dispatched on 243343e in parallel with the T5 re-review; L6 gate running in the clean clone
L6 gate on 243343e (clean clone): make env rc=2 — NOT idempotent (uv: "A virtual environment already exists… Use --clear"); the fresh-clone run earlier passed in 6 s. make test rc=0 but 105 s at load average 17 (four agents running) and NO count line: the Makefile passes `-q` on top of pyproject's addopts `-q` → `-qq` suppresses pytest's summary. make tables/figures/check green; tree clean after targets; forbidden-word count 130 (pre-Task-10, expected).
Ruling R12 (pre-merge fix round): `make env` uses `uv venv --python 3.12 --allow-existing`; the `test` target drops its own `-q` (addopts carries it) so the count line prints; the 90 s budget is re-measured on an idle machine before merge and recorded.
Phase-A ponytail-review (sonnet): config.py:L75 delete redundant exists-check (OmegaConf.load raises); dump_kv.py:L220 stdlib hashlib.file_digest; figures.py:L341 yagni one-verb subparser → parse --out directly. net −7 lines. → pre-merge fix round.
Task 5: fix round 1/5 (10 addressed, 0 open; commits ac5dd90..243343e); re-review (sonnet): all addressed, no new breakage; four implementer concerns judged addressed-by-design / out of scope.
Task 5: complete (commits 9da2f7c..243343e, review clean)
Note for Task 8 (from the re-review): `_expected_cells` keys (arm, sub-task, ctx) without the task-config name; `ruler_inhouse_16k` and `ruler_official_16k` share the sub-task names `niah_multivalue`/`vt` at ctx 16384 (w19_fork pods list both) — the runner must make the generator part of the record key (e.g. TrialRecord gains `generator`, or official sub-tasks are namespaced) before a mixed pod is re-run; this is the CODE_AUDIT's task-name collision (ICML plan §1 0.4c).
Note for Task 9: scripts/w12_calibrate_qkey.py docstring still says capture_kv (file deleted in Task 9 anyway).
Phase-A whole-branch review (opus) → With fixes. No Critical. Important: #1 CI shallow checkout breaks the history-walking prereg test (fetch-depth 0); #2 check passes a ppl-only pod that produced nothing; #3 harvest overwrites trials.jsonl before parsing (a short fallback log shrinks evidence; pplw SystemExit leaves a half-updated dir); #4 watchdog label = pod name → a relaunch is "done", never destroyed; #5 launch does not refuse an unpushed SHA; boot failure markers lack the _${POD} suffix the watchdog greps; #6 results/*/done.txt unignored → dirty tree blocks launches; #7 several pod YAMLs diverge from their archived runs without saying so (w18_g4 n=16, w19_a1 hqq n=4, w19_a4 n=8, w19_fork 21–24 records, w18_g2_qwen/w19_swap arms with zero rows, w18_g1's fourth arm) — need doc sentences + a config-vs-archive drift test with an allowlist; #8 Tables 3/7 pair McNemar across pods (prompt_sha256 null) without a footnote; #9 G0 says uv.lock but the repo gitignores it; #10 suite 85.8 s idle / 121.6 s loaded — dump_kv.py's module-scope torch/transformers imports cost 12.7 s. Minors: PERSIST_RE outside records.py; stale ppl_16k/64k doc; convert-v1 help; suppress(Exception) too broad; Makefile -q; golden lacks lib versions; T7 prose claim computable; manifest lacks source sha256; .md not pinned in tex test; figures.py protocol provenance; parse_diag_lines model param; cell size ignores depths.
Ruling R13: G0's "Dockerfile + uv.lock" is amended: uv.lock stays gitignored (platform-specific, per .gitignore); reproducibility = pyproject exact pins + Dockerfile + `pod.py check` comparing env.txt against the pins (torch, transformers, kvpress, hqq; add optimum-quanto and omegaconf when pinned). GATES.md G0 line updated on the lane. Cost if wrong: a lock could be added later without breaking anything.
Ruling R14: "every v1 arm/task/pod is a YAML" = every pod that fed a paper-v1 table (the plan's reading; satisfied). The 74 archived pod names are not all configs.
Ruling R15 (pre-merge fix round, one dispatch): Important #1–#8, #10; R12 (make env --allow-existing; test target without -q); the three ponytail cuts; minors: stale ppl_16k/64k doc, convert-v1 help, suppress(ImportError), computed T7 claim, figures.py protocol provenance, parse_diag_lines model param. Deferred to Phase B / lanes: persist/latency parsers (when a systems table is regenerated), golden lib versions, source sha256 in archive manifests, .md pin, depth-aware cell size (Task 8 / L2).
Phase-A fix round (opus, DONE; commits 505e2e1, 78a305a, abd9ea8): make test 498 passed / 0 failed / 70.9 s (count line prints); tables diff-clean (+2 footnotes); 13 pods allowlisted in tests/test_configs_vs_archive.py (two divergences beyond the review's list: w19_a1q_qwen no 32K half; w19_ss2 rows keyed quant-2bit-kivi); w19_ss2 → w19-sysfix-llama archive mapping is an inference (documented). GATES.md G0 amended per R13 (commit on the lane). Re-review package review-243343e..abd9ea8.diff (sonnet); L6 gate rerun in the clean clone.
L6 gate on abd9ea8+96e1398 (clean clone): GATE PASS — make env idempotent; make test 498 passed / 0 failed / 83.4 s (reviewer running concurrently; 70.9 s idle in the worktree); tables diff-clean; figures + check green; tree clean after targets. Forbidden-word count 130 = pre-Task-10 baseline (src docstrings + old pod scripts), not a Phase-A regression.
2026-09-14: PHASE A MERGED to week7 @ 600c171 (no-ff); merged-tree make test 498 passed / 86 s; tables diff-clean; check green. Lane synced with week7. Phase B continues on lane/L0-repo-cleanup.
Note for Task 9: pyproject requires-python → ">=3.11" (tomllib, hashlib.file_digest); results/w19_harvest/done.txt and results/w18_checkpoint/*.raw.log are tracked → git rm with the harvest dirs.
Task 6: dispatched 2026-09-14 (BASE = lane head after sync; implementer model opus; brief task-6-brief.md; report task-6-report.md)
Task 6: implementer DONE (commits 2461018 moves, 0e32bf6 deletions + deletions.md): LOC 36,907 → 31,896 (−5,011); make test 423 passed / 0 failed; golden untouched + green; tables diff-clean. Kept: product_quant.py + morph_cache.py (+tests) — module-level imports in bug_cache.py:158/160 → Task 7; turbo_press.py moved to baselines/ (its pod-sh edge is w5 scripts cloning week3) → Task 9. Six `none`-bucket scripts deleted early (imported deleted modules). CLAUDE.md still cites streaming_torch.py:290-293 → wording pass (Task 10). Review package review-0815997..0e32bf6.diff (opus).
Task 6: review (opus) → Approved. Minors (deferred): deletions.md lacks a bucketed row for src/kvdlra/press/__init__.py; two cited line numbers are pre-move (bug_cache.py:159/160 swapped) → Task 7 fixes when it edits deletions.md; CLAUDE.md:6 cites streaming_torch.py:290-293 (now tracker/isvd.py:288-290) → Ruling R16: Task 10's wording pass updates CLAUDE.md citations; README broken links + experiments/ imports of deleted modules → Task 9; tests/test_isvd.py band test is now self-consistency (external anchor remains in test_blockT_equals_oracle) — note; baselines/__init__ docstring "Prefill presses" → one word, Task 7.
Task 6: complete (commits 0815997..0e32bf6, review clean)
Task 7: dispatched 2026-09-14 (BASE 0e32bf6; implementer model opus; brief task-7-brief.md; report task-7-report.md)
Task 7: implementer DONE (commits 9bed011 7a, f64ced4 7b, e9fa697 7c, 5c7afad ledger/fixes, 59e60e4 self-review): LOC 31,896 → 27,224 (−4,672); make test 369 passed / 0 failed (xfail retired with retention="attn"); golden green after every step; accounting pin green; product_quant.py deleted; morph_cache.py KEPT — scripts/w5_streamppl.py (pod-sh via w5/w7 launchers cloning older branches) references MorphKV → Task 9 deletes launchers + script + module. Concerns: superseded launchers w7_streamppl_8b.sh (bugA) / w11_r128.sh (--qwhiten-file) now fail loud (Task 9 deletes them); w12_calibrate_qkey.py orphan (Task 9). Review package review-0e32bf6..59e60e4.diff (opus).
Task 7: review (opus) → Approved. Minors: (1) deleted test_slash_heavy_hitters_are_exact_and_highest_scored carried the only tensor-level pin that the exact tier stores verbatim post-RoPE K (surviving behaviour) → re-add on the surprise-driven path (Task 8 extras); (2) scripts/w7_decode_needle.py default methods still name bugA/morph (Task 9 deletes); (3) stale comments about no-op attach/fences in w5_streamppl.py, tests/test_w15_harness.py, w10_frontier.py (Task 10); (4) vestigial `self.hh_select` (write-only; kept as a config pin) and `self._second_tier_budget` alias (Task 8 extras).
Task 7: complete (commits 0e32bf6..59e60e4, review clean)
Task 8: dispatched 2026-09-14 (BASE 59e60e4; implementer model opus; brief task-8-brief.md + task-8-notes.md; report task-8-report.md)
Rulings R17–R27 for Task 9 written to task-9-notes.md (narrative reports → docs/plan/reports; JSON dumps deleted unless cited; paper source-map instead of editing paper/; requires-python ≥3.11; morph deletion with its pod-side scripts; figures kept only if included; docs relocation; scripts/pod deletions; deps prune + ninja; README; litter removed post-merge by the orchestrator).
Task 8: implementer DONE (commits a177200 8a, 4de82c5 8b, 9615e5d 8c, 0795ab1 8d, 26860c2 8e): make test 375 passed / 0 failed; golden byte-identical (import re-pointed only); LOC −859 net (src +2207 from moves, scripts −3115); moved modules shrank 2715 → 1953 lines. Left for Task 9: w16_tier2_probe, w4_fair, w4_head_to_head, w5_fp16_longctx, w5_hybrid, w5_longctx, w5_needle, w5_ruler (import deleted builders/perplexity_sweep); `_footprint`'s morph branch unreachable. Concerns: official_ruler locates RULER prompts via $RULER_DATA (no TaskCfg field, to avoid moving every config_hash) → note for L2 (R28 below); longbench has no pod config (no e2e coverage); generator shape constants are module constants (L2 owns). Review package review-59e60e4..26860c2.diff (opus).
Ruling R28: no launched pod carries a config_hash yet (archives are exempt), so schema additions are free until the first launch; L2's plan adds `TaskCfg.data_dir` (official RULER / LongBench prompt dirs) and a `pod.py prepare` step that generates the RULER prompts on the pod (replacing w19.sh's a2_prep); until then the env var stays, documented in official_ruler's docstring.
Task 8: review (opus) → Needs fixes. Important #1: harvest leaves `generator` null (parse_trial_lines hard-codes None) while `_expected_cells` keys "inhouse" → every harvested live pod fails check; the synthetic-harvest tests hand-wrote the field and masked it. Important #2 (policy): a pod whose every trial raised passes check (errors only counted). Minors: golden test touched 4 lines (import + 3 stale names — accepted); all-error cell rows print ratio=nan (unparseable by CELL_RE); ppl-axis failures filtered, not counted; runner truncates trials.jsonl at start (undocumented); official-axis `trial` is now the 0-based position, v1 used RULER's record index; morph dead code in frontier/ruler (Task 9, R21); frontier.py carries three responsibilities (not now); latency/storage have no tests (never had).
Ruling R29: `check` fails with `CHECK FAIL errors: N trials raised` whenever errors > 0 — an errored pod is not citable until L6 rules on it per case; no tolerance knob (YAGNI).
Ruling R30 (fix round 1 scope): #1 (harvest fills generator from the pod's tasks; sub-task→generator map when a pod mixes generators; a test that harvests a synthetic log through the REAL parser and then passes check); #2 (R29); all-error cells print the aggregate row without ratio/sbits (`ratio=` optional in CELL_RE → None); ppl-axis failures counted in `errors` and printed as an `[error] axis=ppl …` line that harvest counts; official-axis `trial` = RULER record index (v1 convention, joinable); runner docstring states the fresh-start truncation.
Task 8: fix round 1/5 (implementer opus, DONE; commit 8cc10d8; 386 passed): also fixed the unparsed error= field on the harvest path; collision refusal fires per row needing a fill (w19_fork harvestable with generator= rows); manifest errors = both axes. Re-review package review-26860c2..8cc10d8.diff (sonnet).
Task 8: fix round 1/5 (7 addressed incl. the sibling error= fix, 0 open; commits 26860c2..8cc10d8); re-review (sonnet): all addressed, no new breakage. Out-of-scope: two pre-existing hand-written generator rows remain in check-only tests (fine); a ppl-axis error prints redundantly (frontier + runner) — cosmetic.
Task 8: complete (commits 59e60e4..8cc10d8, review clean)
Task 9: dispatched 2026-09-14 (BASE 8cc10d8; implementer model opus; brief task-9-brief.md + task-9-notes.md; report task-9-report.md)
Task 9: implementer DONE (commits afa4117 9a, 6176ede 9b, 6f190a6 9c): LOC 26,681 → 16,465 (−10,216); tracked files 931 → 529; make test 367 passed / 61 s; README 104 lines; 11 cited files copied into raw/ (cmp-verified); reports → docs/plan/reports; docs/reviews → docs/plan/reviews; 13 deps pruned, ninja, py>=3.11; pre-commit now excludes ^results/paper-v1/ (EOF fixer was rewriting raw copies). Concerns: eval/{latency,storage}.py have no importer; main.tex body text cites 5 deleted paths (mapped); 25 dangling docstring refs → Task 10; litter + venv re-sync → orchestrator post-merge.
Ruling R31 (for Task 10): `kvdlra.eval.latency` becomes a runner generator (`TaskCfg.generator: latency`; rows → results/<pod>/latency.jsonl {model, arm, ctx, batch, ms_per_token_p50, ms_mean, ms_max, spikes, resident_gb, peak_gb, kv_peak_gb}; a pod config `w19_sysfix_latency` documents the W20 measurement's re-run contract; `pod.py check` requires one latency row per (arm, ctx) for latency tasks); `kvdlra.eval.storage` is DELETED (its 0.98× "workspace" number is the identity the audit flagged; L4's measured kernel numbers supersede it; the tag keeps it). Cost if wrong: latency wiring is ~40 lines; storage restore from the tag.
Review package review-8cc10d8..6f190a6.diff (opus).
Task 9: review (opus) → Approved. Minors: deletions.md header says one line per path but ~322 paths are covered by scoped group rows (Task 11 rewords + appends the name-status listing); accounting.py:221,255 cite deleted scripts unqualified (Task 10 wording); tests/test_golden_cache.py docstring re-pointed a dangling w18.sh reference (2 lines, values untouched) — BLESSED (Ruling R32: docstring-only edits to the golden test are allowed; vectors/assertions/tests/golden never); README lacks the local-GPU `pod.py run` line (Task 10); .gitignore litter entries stay until the orchestrator removes the files post-merge; results/paper-v1/_cited-extras/ has no digest (Task 11 adds sha256 lines to its README); "13 deps" = 11 packages + 2 extras (label).
Task 9: complete (commits 8cc10d8..6f190a6, review clean)
Task 10: dispatched 2026-09-14 (BASE 6f190a6; implementer model opus; brief task-10-brief.md + task-10-notes.md; report task-10-report.md)
Phase-B gate plan (after Task 11): (1) whole-branch review of 0815997..HEAD (opus, code-reviewer template) + ponytail-review (sonnet) in parallel; (2) one fix round + scoped re-review; (3) L6 gate `scratchpad/l6-gate.sh lane/L0-repo-cleanup` (fresh-clone make env/test/tables/figures/check + forbidden-word count must be 0); (4) merge --no-ff into week7; merged-tree make test; (5) post-merge orchestrator duties in the MAIN checkout: `uv pip install -e ".[dev]"` (adds ninja, drops the 11 pruned packages from the venv is not automatic — run `uv pip sync`-equivalent or leave extras installed and note), delete on-disk litter (handover.md, explanation_week_*.md, next-session-prompt.md, compass_artifact_*.md, dashboards/, .DS_Store, results/gpu_logs?, results/scratch) then drop their .gitignore lines in a follow-up commit, `git worktree` cleanup of L0-cleanup only after the owner confirms Phase B is done (finishing skill), STATE.md entry, DECISIONS.md (G0 checklist ticked with evidence paths), memory update.
Observation 2026-09-14: the forbidden-word grep over src/configs/scripts/docs/paper already returns 0 hits at 6f190a6 (the 130 baseline lived in the deleted pod launchers and the moved/deleted modules' docstrings); Task 10 confirms with before/after counts.
Task 10: implementer DONE (commits 74637a2 10a R31, 97b7ff3 10b tests+wording+CLAUDE.md+README, e498ba2 10c CI): make test 372 passed / 56.6 s; forbidden words 22 → 0 (token-level regex; the "130" was the crude grep); reachability 34 files / 0 unreachable (deviation: the test follows parent packages so tracker/baselines/util __init__.py count as reached — accepted, they execute on import); TaskCfg +4 fields (batch_sizes, ctxs, n_steps, warmup) → config_hash shift (harmless, no live manifest); `[latency …]` log line gained ` batch=` with NO parser → harvest cannot recover latency rows (reviewer to weigh; likely fix round). Review package review-6f190a6..e498ba2.diff (opus).
Task 10: review (opus) → Needs fixes. Important: no LATENCY_RE/parse_latency_lines; harvest writes no latency.jsonl while runner.py:14 claims `[latency ctx<T>]` is harvestable → a harvested latency pod fails check (9 CHECK FAILs). Minors: TaskCfg +2 fields beyond the supplement (accepted; hash shift inert); `[error] axis=latency` line omits batch=; isvd.py:102 "with exactly the validated integrator math" survives; commit 97b7ff3's message mentions the forbidden tokens (mention-not-use; accepted — rewriting three commits is not worth it; the merge commit will be clean); reachability test ignores relative imports (safe direction; noted).
Ruling R33 (fix round 1): add `LATENCY_RE` + `parse_latency_lines` (records.py) and `harvest` writing latency.jsonl (mirroring pplw); `[error] axis=latency … batch=<b> error=…` with an optional batch group in ERROR_RE; reword isvd.py:102; a harvest→check round-trip test for the latency pod. Implementer sonnet.
Task 10: fix round 1/5 (implementer sonnet, DONE; commit b5009db; 376 passed). Note: implementer used git stash to confirm RED — stash stack checked afterwards. Re-review package review-e498ba2..b5009db.diff (sonnet).
Task 10: fix round 1/5 (3 addressed, 0 open; commits e498ba2..b5009db); re-review (sonnet): all addressed, no new breakage; stash stack empty; 376 passed.
Task 10: complete (commits 6f190a6..b5009db, review clean)
Task 11: dispatched 2026-09-14 (BASE b5009db; implementer model opus; brief task-11-brief.md + task-11-notes.md; report task-11-report.md)
Phase-B L6 preview on b5009db: GATE PASS (make env idempotent; 376 passed / 82 s; tables diff-clean; figures + check green; clean tree); the crude grep's 6 hits were generated figure binaries — gate script now skips binaries and docs/paper/figures.
Task 11: implementer DONE (commit c70c9af): G0 7/8 green in-worktree (make env fresh-clone = orchestrator's gate: PASSED on b5009db with the pruned pyproject); deletions.md 352 D + 165 R, 0 uncovered; net LOC −16,897; after-report net −209 lines / −1 dep (hydra-core zero importers); make test 376 / 77 s (66 s idle).
Ruling R34: keep src/kvdlra/baselines/turbo_press.py (+ test) — Phase 2's composition Pareto (ICML plan §3.4) needs TurboQuant as a pure quantizer; L2 adds its arm YAML; record as kept-despite-candidate.
Ruling R35 (Phase-B fix round scope, with the whole-branch findings): drop hydra-core (omegaconf stays a direct pin), delete the two zero-caller accounting helpers if tests confirm zero callers, drop seed_everything's never-passed `deterministic` flag; skip the RoPE-LRU and readers-fold refactors (churn/numerics-adjacent; noted).
Phase-B gate: Task 11 review (sonnet) + whole-branch review 0815997..c70c9af (opus) + ponytail-review (sonnet) dispatched in parallel.
Phase-B ponytail-review (sonnet): production code proportionate; two test-boilerplate shrinks (test_latency_runner.py dropped-row block ×2 → helper; test_pod_run_records_errors.py cfg setup ×6 → helper). net −16 lines → fix round.
Phase-B whole-branch review (opus) → With fixes. No Critical. Important: #1 [diag] lines that fail json.loads are silently dropped (vastai truncates ~500 chars; L1's per-layer rows will hit it); #2 frontier.py exports its shared core privately (defer to L2 — de-privatize when it opens the file); #3 persist.py production-dead (no generator/YAML/parser; figures.py inline regex) — defer to L4 (owns systems measurements) with a ruling; #4 README:102 claims manifests carry "dataset and haystack digests" but nothing writes dataset_sha256; #5 only trials.jsonl streams (ppl/pplw/latency buffered) — docstring must say harvest recovers them; #6 test budget 81 s/90 with no `slow` marker registered. Minors: paper/main.tex:629 `% source:` comment re-pointed in Task 6 (Ruling R36: accepted — comment-only pointer fix, no rendered text); errors message conflates axes; no pplw.jsonl rule in check; Makefile PATH export breaks under PY="uv run python" ($(lastword)); tests/ outside the forbidden-word scope (defer); tautological manifest arithmetic test (open since T1; defer); no TaskCfg validation; relative imports not followed (defer); make check not in CI; R35 confirmed (hydra-core 0 importers; shadow_bandwidth_per_token + audit_matched 0 callers; deterministic never passed).
Ruling R37 (Phase-B fix round, one dispatch): Important #1 (count skipped [diag] lines, manifest `diag_skipped`, harvest prints it, check fails when > 0; note for L1: diag rows < 400 chars or reuse the part= splitter), #4 (README clause → trials.jsonl per-trial digests; corpus digests arrive with generator v2), #5 (docstring), #6 (register `slow` marker); minors: axis-aware errors message, pplw.jsonl check rule, Makefile $(lastword $(PY)), TaskCfg validation (n_trials ≥ 1, seeds non-empty, ctx > 0), `make check` in CI; R35 (hydra-core drop, two zero-caller helpers, deterministic flag); ponytail's two test-boilerplate shrinks. Deferred with rulings: #2 (L2), #3 (L4 — Ruling R38: persist stays archive-only until L4 wires it as a generator; figures.py's inline regex moves to records.py then), tests/ scope, tautological test, relative imports.
Task 11: review (sonnet) → Needs fixes: deletions.md "Coverage" per-bucket counts (126+35+82+73+36) do not reconcile with the document's own prose (figures 86, docs 57, results 91); total 352 and "0 not covered" independently verified. Everything else reproduced exactly (LOC table, listing, sha256, G0 evidence). → follow-up item K after the Phase-B fix round (recompute the buckets from the listing or replace the breakdown with the verified total).
Phase-B fix round (opus, DONE; commits 7ae5aac fix1a, d291284 fix1b, 4714d74 fix1c): 380 passed / 66.9 s; tables diff-clean; README 110 lines. Deviations: DIAG_RE loosened (json.loads is the arbiter); E's [error]-line cross-check not implemented because the watchdog ROWS filter drops `[error]` lines; F is a behaviour change (short corpus now fails check); ASCII `--` in the harvest warning.
Ruling R39: the watchdog ROWS filter must keep `[error]` lines (`|^\[error\]`) — a watchdog harvest must count the same errors a full-log harvest does; one token in scripts/pod/watchdog.sh + a bash -n. Follow-up dispatch (sonnet): K (deletions.md coverage buckets recomputed from the listing) + L (R39).
Phase-B follow-up (sonnet, DONE; commit d82c9d1): buckets 112/6/86/91/57 = 352; watchdog keeps [error]; check --log cross-check (7 lines). Re-review package review-c70c9af..d82c9d1.diff (sonnet); L6 gate rerun on d82c9d1.
Final L6 gate on d82c9d1 (clean clone): GATE PASS — env idempotent; 380 passed / 74.9 s; tables diff-clean; figures + check green; tree clean; forbidden words 0. Awaiting the fix-range re-review, then merge to week7.
Phase-B fix-range re-review (sonnet): all 12 findings ADDRESSED; no new breakage; minor docs residue: deletions.md:623 still says hydra-core kept (contradicted by fix1c); CLAUDE.md "hydra configs" → omegaconf; `check --log` has no regression test (manually verified; deferred minor); the venv still carries hydra-core until the post-merge re-sync. → micro-fix (sonnet), then merge.
