# L3+L5 lane ledger — copied verbatim from the lane SDD workspace (at the finishing menu, 6065e4e)

Source: `.claude/worktrees/L3-gate1/.superpowers/sdd/2026-09-11-L3-L5-gate1-bf16-prereg/progress.md`, copied 2026-09-20 17:59 EDT. Pre-flight drift table + rulings PR-L3-1–30 and R-L3-1–22 with every review verdict; the deferred-minor triage is in the same directory's final-review-deferred.md and the whole-branch review's deferred-list table (summarised in STATE).

---

# SDD ledger — plan: docs/plan/plans/2026-09-11-L3-L5-gate1-bf16-prereg.md

Lane lane/L3-gate1-tracker-swap-v2 in .claude/worktrees/L3-gate1 (repo now ~/src/kv-dlra, D-007 CLOSED mid-session: the
first worktree was created under ~/Desktop at 823b666 and vanished with the move; rebuilt from main @ 278bd01, no commits lost).
Spec: docs/plan/lanes/L3_gate1_tracker_swap_v2.md + L5_bf16_gist_and_prereg.md, ICML2027_PLAN.md §2 Gate 1 (the branch rule,
verbatim), GATES.md §G3/§G5. Baseline in the worktree: make test 538 passed / 88.7 s pytest (1:36 wall) — 1.3 s under the 90 s gate.
Owner's task order (one lane, sequential; L3.1 and L5.1 both edit bug_cache.py's constructor/dispatch and POST_V1):
T1 = L3.1 frozen/random trackers + byte-matched no-gist arms · T1b = gate1_preflight pod YAML + prereg (launch under D-011) ·
T2 = L5.1 bf16 gist storage · T3 = L3.2 Gate-1 pods + gate1 table + gate1_verdict() · T4 = L5.2 preregs + provenance check ·
T5 = L3.3 harness follow-ups (before the Stage-1 launch commit). Stage-1 launch only after D-003's top-up.
Credit at start: $92.52 (vastai show user --raw, 2026-09-19 13:50).

## Pre-flight drift table (plan written 2026-09-11 and approved 2026-09-17, before L0-B/L1/L2 landed)

| # | plan says | main @ 278bd01 has | ruling |
|---|---|---|---|
| 1 | Global: after L1/L2 merge into week7; L3 and L5 in two worktrees, "disjoint files" | main only (week7 retired); both parts edit bug_cache.py's constructor + dispatch and tests/test_config_parity.py::POST_V1 | PR-L3-1: one worktree, one lane, sequential T1 → T1b → T2 → T3 → T4 → T5 |
| 2 | L3.1 test: `acc.bug_footprint(..., hh_select="surprise")` | no `hh_select` kwarg (signature: n, rank, coord_count, recent_len, *, n_sink, retention, hh_count, u_present, quant_count, quant_bits) | PR-L3-2: drop it |
| 3 | L3.1: ONE `nogist_h<H>.yaml`, H solved at 16K, test asserts exactly one file | the tier's bytes scale with n (Llama/Mistral n=1024, Qwen n=512 = head_dim × kv_heads, runner._dims); the r64 arm's coordinate bytes do not | PR-L3-3: two arms `nogist_h2423` (n=1024) and `nogist_h4460` (n=512), each pinned by the test to its n at 16K on `stored_bits()` (within 5 %); 32K variants (H ≈ 4501 / ≈ 8600) are created when Stage 2 is sized |
| 4 | L3.1 nogist: `rank: 1` with the coordinate tier at the whole prefill (the test's `coord_count = t-4-32-H`) | rank-1 coordinates of every middle token = ~16K near-junk reconstructed keys in attention; ICML §1.1(e) says "tier + ring only" | PR-L3-4: `coord_budget: 1` (the constructor's minimum with a tier enabled); H solved on `stored_bits()` (the paper's at-rest headline) against the fp32 r64 arm: 2423 at n=1024 — 2.4× the ICML plan's "h≈1024", which is an fp16-equivalent estimate (`bits(16)` match gives 1355); the arm doc states the tier selects by residual against a rank-1 basis (≈ norm-ordered), which is what "no gist" means for a surprise tier |
| 5 | L3.1 frozen dispatch: `n_seen = c_k.shape[1] + _q_len()` "the same expression the Oja branch uses" | the Oja branch reads `self._tokens_seen(positions)` (L1 fix: the tier expression froze at saturation) | PR-L3-5: frozen reads `_tokens_seen(positions)` |
| 6 | L3.1 random: one `seed` → the same Haar basis in every layer and both streams | — | PR-L3-6: seed = basis_seed + 2·layer_idx + (0 for K, 1 for V), generator on CPU, basis moved to the block's device/dtype; still a fixed basis per (layer, stream) |
| 7 | L3.1 `frozen_step(u, b, block, rank_cap, *, n_seen, freeze_after)` | the dispatch's else-branch passes theta=/min_sv_frac= to any tracker it does not know; frozen "behaves as isvd_step" before the freeze | PR-L3-7: explicit `frozen`/`random` branches in `_absorb_columns`; `frozen_step` takes `theta=None, min_sv_frac=0.0` pass-through (isvd's own defaults), `random_step` takes only `seed` |
| 8 | L3.1: three new arm YAMLs | `test_the_golden_covers_every_arm_config_with_a_legacy_name` requires every stem without a legacy name in POST_V1 | PR-L3-8: add the new stems to POST_V1 (T2 adds `isvd_r64_h256_seed_bf16`) |
| 9 | L3.1 test `test_frozen_...` counts `seen` itself and compares `torch.equal` to isvd before the freeze | frozen == isvd (same function) before the freeze → exact equality holds | consistent as written |
| 10 | L3.2: `configs/tasks/ppl_16k_wt103test_g1.yaml` at 16 windows | IMPOSSIBLE: WikiText-103 test supplies 14 windows of 16K+2048 and `load_task` refuses more | PR-L3-9: Gate-1 perplexity = `ppl_16k_pg19val_w16` (exists) + new `ppl_32k_pg19val_w16` (PG-19 val supplies 84 windows at 32K) |
| 11 | L3.2: `ruler_v2_16k_g1` (n=24) + a 32K counterpart | g1 exists with FIVE tasks (niah_multiquery added by L2); no `ruler_v2_32k_g1`; no manifest pins g1 | PR-L3-10: g1's `tasks:` = the four Gate-1 tasks (GATES §G3, ICML §1.1: single, multikey, multivalue, vt; −20 % retrieval cost); create `ruler_v2_32k_g1` the same way; multiquery stays in `ruler_v2_16k/32k` (ss2/smoke pods) |
| 12 | L3.2 sizing: 1.0/3.0/3.5/1.5 min per TASK-trial; Stage 1 ≈ 41 GPU-h; $1.0–1.5/h | L2 measured, per sample, task-independent (prefill dominates): full 0.6, r64 gist 3.1, q4 3.8, KIVI 1.5 min; $0.45–0.74/h | PR-L3-11: the re-sized table below; Stage 1 = 72 GPU-h point / 144 bar with `full` and the 16 ppl windows; the pre-committed cuts → 64/128 (no random) → 58/115 (n=16 for oja/fd) |
| 13 | L3.2: `gate1_v2_stage1.yaml`, `gate1_v2_stage2.yaml` | PodCfg is one model per pod | PR-L3-12: `gate1_v2_stage1_llama`, `gate1_v2_stage1_qwen`; Stage 2 = `gate1_v2_stage2_mistral_16k` + `gate1_v2_stage2_{llama,qwen,mistral}_32k` (sized, conditional on Stage 1 not selecting C) |
| 14 | Stage 1 carries no `full` | the paired contrasts need none, but the retrieval ceiling and the absolute ppl reference cost 1.1 h per pod | PR-L3-13: `full` rides Stage 1 (first in arm order, as every prereg pod does) |
| 15 | Global: ≤ 50 GPU-h per stage in the manifest | at the measured rates the stage total exceeds 50 h at 1× (36 h per pod) | PR-L3-14: per-pod bars = 2× point (72 h full design / 57.6 h cut design); the owner picks the design under D-003 — surfaced in the first report |
| 16 | `gate1_verdict()` ppl criterion for A/B: "paired CI excludes 0 in isvd's favour" | ICML §2 Gate 1 (the spec): "Holm-corrected p<0.05" on retrieval OR perplexity | PR-L3-15: perplexity separation = paired t-test p on the 16 per-window Δ bits, Holm-corrected over the ppl family (families × 2 contrasts), isvd-favoured sign; the bootstrap CI is reported beside it; TOST ±0.02 for branch C exactly as written |
| 17 | Holm family: lane says "over 12", plan says "families × tasks × 2" | — | PR-L3-16: per stage: Stage 1 primary family = 2 families × 4 tasks × 2 contrasts = 16; secondary (oja/fd/random) their own family of 24; Stage 2 its own (Mistral 16K: 8; 32K: 24). "≥ 2 families" is reachable from Stage 1 alone |
| 18 | `gate1.load`: tracker ← arm name via configs/arms; ppl rows from `ppl.jsonl` | records carry `legacy_name` where set (`isvd_r64_h256_seed` → `bugSseed-r64-h256`); the per-window rows are `pplw.jsonl` (PplwRecord: arm, ctx, window_idx, ntok, nll_sum_nats, corpus) | PR-L3-17: map through `load_arm(stem).legacy_name or stem`; pair windows by `window_idx` exactly as `tables.ppl_stats` does (reuse it; refactor only if shorter) |
| 19 | `make tables gate1` | `make tables` = `tables.py build` + diff-clean vs paper-v1; `gate1.md` is outside the `table*.md` glob | PR-L3-18: `tables.py gate1 --pods …` writes docs/paper/tables/gate1.md; a `make gate1` target names the Stage-1 pods; `make tables` unchanged |
| 20 | ICML (b): Oja "with the Week-2 tuned schedule AND a re-tuned schedule on held-out 8B KV" | `oja_r64_h256_seed_tuned` is tuned on the 1B dumps (L1.4b); no 8B dumps yet (G1 line 6 open) | PR-L3-19: Stage 1 runs the 1B-tuned schedule; the isvd-vs-oja secondary is read with that caveat in the prereg; the 8B re-tune is a Stage-2/dump-pod item |
| 21 | L5.1 tests: `from tests.test_bug_cache import make_tiny_model_and_cache`, `cache.ingest(x)`, `cache.n` | none exist; conftest has the module-scoped `tiny_model` fixture; the golden drives `frontier._prefill_chunked(model, cache, ids, CHUNK)` | PR-L3-20: T2 rewrites the tests on the real API (tiny_model + `_prefill_chunked` on a seeded id stream), keeping every assertion's meaning |
| 22 | L5.1: "store-boundary casts; U, C, B stored bf16; billed at 16 bits" | U/B/C are fp32 today (`_reset_state`); blocks are cast to fp32 before the step; `_ensure_mid_cache` casts the product to the model dtype; B is billed as `aux_words` (2r) at 32 bits | PR-L3-21: fp32 working copies for the step, carry, guard and surprise; bf16 only at the store boundary; B stays aux at 32 bits (2r words — the plan's own test formula counts U and C only); `float_equiv`/`stored_state_numel` unchanged so the anti-drift pin holds |
| 23 | L5.1: "the guard reads fp32 upcasts" | a bf16-rounded U has ‖UᵀU−I‖_F ≈ 7e-3 at n=1024, r=64 (> orth_fix_tol 1e-3, < abort 1e-1) → under bf16 storage the guard repairs EVERY absorb (a thin QR of n×r, cheap) — and that repair is what stops U's rounding from compounding; the coordinates' per-carry re-rounding is the compounding path the plan names | PR-L3-22: keep the guard as is; the arm doc and the corrected bug_cache.py comment say so; the diag rows read fixed=True; the test asserts the fp32 working basis' pre-repair error < 1e-2 and that the default path is bit-identical |
| 24 | L5.2: add `tests/test_prereg_order.py` driving `pod.py launch --prereg` | no `--prereg` flag; `prereg_error` covers missing / uncommitted / not-an-ancestor / self-commit and is tested (test_prereg_refusal_reasons, test_prereg_commit_order_against_real_history, test_launch_refuses_a_pod_with_no_prereg); tampered hash: test_check_rejects_tampered_config_hash | PR-L3-23: no new test; T4 names the covering tests in its report |
| 25 | L5.2: three preregs | the pre-flight pod (owner's prompt) needs `prereg/gate1_preflight.md` before it launches | PR-L3-24: T1b = pod YAML + prereg for `gate1_preflight` (Llama 16K, `ruler_v2_16k` n=12 five tasks, arms full/isvd_r64_h256_seed/frozen_r64_h256_seed/nogist_h2423), launched under D-011 after its review; the Gate-1 prereg §2 cites D-005 now and the pre-flight rows by Amendment 1 before the Stage-1 launch |
| 26 | harness follow-ups (owner's prompt) | `run_ppl` bills press/full/quant arms after the scored window; harvest's non-shrink guard covers trials.jsonl only; harvested manifests carry gpu/cuda "none", model_revision null, wall_clock_s null; watchdog .raw grows | PR-L3-25: T5 (after T4, before the Stage-1 launch commit): `[stage]` lines for gpu / cuda / model_revision / wall_clock parsed by harvest; the non-shrink guard for every record file; press/full/quant billed post-prefill pre-window (+ test); .raw growth → L6 note unless trivial |
| 27 | R-L2-4: v2 `vt`'s value takes the code family ("L3's prereg may reverse it") | official vt is numbers-only; the pre-flight measures v2 vt on hardware for the first time | PR-L3-26: keep R-L2-4 for Gate 1 (uniform design across tasks); revisit only if the pre-flight's `full` vt < 0.9 |

Cost if wrong: PR-L3-3/4 = one YAML value + a re-solved H; PR-L3-10/11/12/13 = pod YAML edits before launch (hashes are free until a manifest exists); PR-L3-15/16 = a prereg sentence + one gate1.py branch; PR-L3-22 = a guard-placement change (≤ 20 lines); PR-L3-24 = ≈ $4–12 of pre-flight spend.

## Re-sized pods (measured rates; owner's prompt + l2-ledger Task-8 line; A100 40 GB; min per sample = one (arm, task, seed, trial) or one ppl window)

| arm | 16K min/sample | basis |
|---|---|---|
| full | 0.6 | measured (L2 real-text pod 51553610: the 48-sample `full` arm in ≈ 30 min) |
| isvd_r64_h256_seed | 3.1 | measured (same pod; 2.9–3.7 observed) |
| oja_r64_h256_seed_tuned, fd_r64_h256_seed, isvd_r64_h256_seed_bf16 | 3.1 | derived = isvd (same absorb count; a per-block Oja/FD update ≤ a core SVD; the casts are free) |
| frozen_r64_h256_seed, random_r64_h256_seed | 2.1 | derived = isvd / 1.5 (no per-absorb SVD after the freeze; the plan's factor) |
| nogist_h2423 / nogist_h4460 | 1.55 | derived = isvd / 2 (no gist rebuild; the plan's factor) — CAVEAT: the 2423-token tier is re-scored every absorb; the pre-flight measures it |
| any arm at 32K | 2× | derived (absorb count doubles; prefill dominates) |

Samples per (arm, family, ctx) = 4 tasks × n=24 + 16 ppl windows = 112. Boot overhead 60 min per pod.
Stage-1 pod (one family, 16K; full + isvd + oja + fd + frozen + random + nogist + bf16): 67 + 4×347 + 2×235 + 174 = 2099 min = 35.0 h + 1 h = **36 h point, 72 h bar**.
Two pods (Llama, Qwen): **72 GPU-h point / 144 bar → $32–53 point / $65–107 bar** at $0.45–0.74/h.
Pre-committed cuts (prereg §9, applied in this order if the owner funds less): drop `random` → 32.1/64.2 per pod, 64/128 total ($29–48 / $58–95); then n=16 for oja/fd → 28.8/57.6 per pod, **58/115 total ($26–43 / $52–85)**.
Pre-flight `gate1_preflight` (Llama 16K, `ruler_v2_16k` five tasks × n=12 = 60 samples/arm; full 36 + isvd 186 + frozen 126 + nogist 93 = 441 min): **8.4 h point / 17 h bar → $4–6 / $8–12** (the prompt's "≈ 3 GPU-h" omitted three of the four arms' rates).
Stage 2 (conditional, not launched in this lane): Mistral 16K 8 arms 36/72; each family at 32K (full 134 + isvd 694 + bf16 694 + frozen 470 + nogist 347 = 2339 min → 40/80) × 3 → **156 h point / 312 bar ($70–115 / $140–231)**.
D-003 as it stands: credit $92.52; Stage-1 bar $65–107 (full) or $52–85 (cut) + pre-flight bar ≤ $12; ss2 bar $49–90 and smoke bar $67–124 wait behind Gate 1.

## Log
2026-09-19 14:10: worktree rebuilt at ~/src/kv-dlra off main @ 278bd01 (D-007 closed by the owner mid-session; the Desktop worktree at 823b666 vanished with the move — no commits lost). Baseline 538 passed / 88.7 s. Pre-flight table + PR-L3-1..26 written. Task 1 (L3.1) brief written; implementer (opus) dispatched from BASE 278bd01.
2026-09-19 14:35: Task 1 implementer DONE_WITH_CONCERNS @ 7f3e546 (opus; 546 passed / 75.5 s; ruff+mypy clean; golden green; H = 2423 / 4460 re-solved independently). Concerns: (1) named GATE1 exclusion set in test_pod_manifest.py so the smoke pod's 40-arm prereg is untouched — Ruling R-L3-1: the Gate-1 arms do not ride the smoke pod (their pods are the pre-flight + Stage 1); the exclusion set is the record; cost if wrong = an l2_smoke amendment; (2) random not in the w20 contract parametrization (seeding-shape assertion), covered in test_gate1_arms.py — accepted; (3) plain-QR draw = Haar up to column signs (matches recon.py::random_basis) — accepted, the span is what a fixed-basis floor control needs; (4) a build_arm()[make]() construction test for the four YAMLs — welcome. Task reviewer (opus) dispatched on 278bd01..7f3e546.
2026-09-19 14:40: rulings added while Task 1 is under review (cited by the Task 3 brief): PR-L3-27 — no Stage-2 pod YAMLs in this lane (they would need 32K no-gist arms that do not exist; Stage 2 is conditional on Stage 1 not selecting C; the prereg sizes it and its pods are created by amendment); cost if wrong = four YAMLs later. PR-L3-28 — the branch rule operationalized for `gate1_verdict()`: a family is SEPARATED iff for each b ∈ {frozen, nogist} isvd beats b on retrieval (some task, Holm p < 0.05, isvd favoured) OR on perplexity (Holm p < 0.05, Δ < 0); A/B iff ≥ 2 families separated; C iff every isvd-vs-fd and isvd-vs-frozen retrieval contrast has Holm p ≥ 0.05 in every family and task AND every isvd-vs-fd / isvd-vs-frozen TOST ±0.02 passes; else UNDECIDED — the mixed-axis reading of "beats (d) and (e) on retrieval or perplexity"; cost if wrong = one predicate in gate1.py + a prereg sentence (owner sees it in the prereg before launch). PR-L3-29 — the error refusal covers the four arms the rule reads (isvd, frozen, nogist, fd); oja/random/bf16/full errors render FAILED and are named in the reason. PR-L3-30 — the second pre-committed cut is "drop oja", not "n=16 for oja/fd": n_trials is per task YAML and shared by every arm of a pod, and a 16-trial design cannot pair with the 24-trial one; cut ladder = drop random (−3.9 h/pod) → drop oja (−5.8 h/pod): 36/72 → 32/64 → 26.3/52.6 h per pod; total 72/144 → 64/128 → 52.6/105 GPU-h ($24–39 / $47–78 at the floor of the ladder).
2026-09-19 15:00: Task 1 review (opus): APPROVED, spec ✅ (all 11 resolutions verified; H re-solved independently; bit-identity of the default path structural; diagonal-core billing invariant holds under b = I; forbidden words 0; the 14 warnings are torch/jit's DeprecationWarning at import, pre-existing). ⚠️ items resolved by the controller: the commit body names the forward prereg reference (checked below); the GATE1 exclusion set in test_pod_manifest.py is ratified by R-L3-1 (surfaced to the owner). Minors deferred for the final review: (m1) a `# ponytail:` comment at `_rotate_quant_tier` naming the identity-rot requantization ceiling for a future `*_q4` Gate-1 arm (bug_cache.py:825); (m2) `test_w20_tracker_swap.py::test_swapped_tracker_runs_end_to_end_and_differs` still parametrized over oja/fd — add random, and frozen with a lowered freeze_after; (m3) test_gate1_arms.py:171 byte-match reference hardcoded rather than derived from `arm_kwargs(load_arm("isvd_r64_h256_seed"), T)`; (m4) test_gate1_arms.py:145 overlaps test_w20_tracker_swap.py:185 (kept; noted so no third copy appears).
Task 1: complete (commits 278bd01..7f3e546, review clean; 4 minors deferred)
2026-09-19 15:02: Task 1b (gate1_preflight pod + prereg) brief at task-1b-brief.md; implementer (opus) dispatched from BASE 7f3e546.
2026-09-19 15:35: Task 1b implementer DONE_WITH_CONCERNS @ 8db2db2 (opus; 547 passed / 78.0 s; prereg rule verified in three states: uncommitted → refused, committed-by-HEAD → refused, child commit → MAX_HOURS=17 command prints). Concerns: L2_PODS table name kept (accepted); real-text D-005 records carry prompt_sha256 null (known — the cycle pod is the pairing evidence; reading (iii) covers this pod); g1 five tasks until Task 3 (expected); three of four rates derived (reading (iv)); three forward commitments on prereg/gate1_tracker_swap_v2.md (added to the Task 4 brief). Task reviewer (opus) dispatched on 7f3e546..8db2db2.
2026-09-19 16:00: Task 1b review (opus): NEEDS FIXES — spec ✅ (every record-derived number re-computed exactly; provenance claims hold against watchdog/boot/launch code; the $8–13 bar is the correct rounding of the brief's $8–12). Important #1: reading (iv) (min/sample per arm) is NOT decidable from the harvested records (no row carries a clock; wall_clock_s null; the watchdog sort -u's the .raw in place) while §4 claims all four readings are pass/fail on the records. Important #2: the Amendment-1 chain has no ordering anchor — nothing stops prereg/gate1_tracker_swap_v2.md being written after this pod's harvest, which would make its "predictions" post hoc. Important #3: the Stage-1 "36 h/pod, 144 at the bar" at :19 shows no arithmetic and silently supersedes the plan doc's 41/82 h. Minors #4 (the 2 % read-precision arithmetic is 2.7–5.4 %), #5 ("one thing changed" → three knobs), #6 (two numbers lack inline sources: 275 B/row, the v2 overheads), #7 ("$0.45–0.74" vs the cited $0.40 floor), #8 (no rule against stopping after arm 3 on what arms 1–3 showed), #9 (L2_PODS table name — deferred), #10 (suite 78–95 s across runs: pre-existing headroom, no timeout enforced — L6 note).
Ruling R-L3-2 (fix round 1, fresh opus, in-place edits — the file is unlaunched; append-only binds from the launch commit, precedent R-L2-7): Important #1 is fixed in the HARNESS, not by prose: `kvdlra.eval.runner` prints one `[stage] cell arm=<arm> task=<task> ctx=<ctx> elapsed_s=<s> n=<k>` line per completed (arm, task) cell (the watchdog already keeps `^\[stage` rows; order-independent because the line carries its own elapsed seconds), and `pod.py harvest` parses those lines into `manifest["cell_elapsed_s"]` (same pattern as DIGEST_RE), so reading (iv) = Σ_task elapsed / 60 per arm is decidable from the manifest; the prereg names that line as reading (iv)'s source and pre-commits the fallback (line absent → per-arm triggers recorded `not measured`; Stage 1 re-sized from the pod's aggregate launched_at→ALL_DONE rate minus 60 min boot, apportioned by §7's shares). Important #2 → one sentence in §1/§9/§10: `prereg/gate1_tracker_swap_v2.md` §1–§11 are committed BEFORE this pod's launch commit; its rows and any task exclusion enter it only as dated amendments. Important #3 → the arithmetic shown (8 arms × (4 tasks × 24 + 16 windows) at §7's rates + 60 min boot ≈ 36 h/pod) with "supersedes the plan's 41/82 h, which predate the L2 rate measurements". Minors #4–#8 taken in the same round; #9/#10 deferred to the final review / L6. Cost if wrong: ≈ 20 lines of harness + a test.
Ruling R-L3-3 (lane reorder, from Important #2): the pre-flight launches only after `prereg/gate1_tracker_swap_v2.md` is committed. Task 4 splits: T4a = `prereg/gate1_tracker_swap_v2.md` alone, written from the design fixed in this ledger (PR-L3-3/4/10–16/19/28–30) and citing the Stage-1 pods and `gate1_verdict()` as "committed by Task 3 to match this file"; T4b = `prereg/bf16_gist.md` + `prereg/kernel_smoke.md` after Task 2 (the drift numbers). Order: T1b fix → T4a → LAUNCH pre-flight → T2 → T3 → T4b → T5 → Stage 1 (after D-003). Cost if wrong: the pre-flight starts ≈ 1 h later than it could have.
2026-09-19 16:05: Task 1b fix round 1 dispatched (fresh opus) with the ten findings verbatim; the harness line + harvest parse are in scope by R-L3-2.
2026-09-19 16:30: Task 1b fix round 1/5 DONE (fresh opus): 03fba42 (L3.1c [stage] cell line + harvest cell_elapsed_s) + 09dbe2f (prereg: reading (iv) from cell_elapsed_s with the not-measured/aggregate fallback; the ordering anchor; the Stage-1 arithmetic; minors 4–8); 548 passed / 76.0 s; config_hash of the pre-flight moves (nothing pinned it). Scoped re-review (sonnet) dispatched on 8db2db2..09dbe2f. Task 4a (Gate-1 prereg, opus) dispatched in parallel from BASE 09dbe2f — new file only.
Concern noted for Task 5: cell_elapsed_s is written by harvest only, not by _finish on the pod (the pod-side manifest dies with the instance anyway).
2026-09-19 16:50: Task 1b re-review (sonnet): all 8 findings ADDRESSED (harness line + parse verified against the watchdog filter and the runner's format; every re-added number exact; ordering anchor present in §1/§4/§8; no new breakage). Out-of-scope: cell_elapsed_s written by harvest only (fine — the pod-side manifest dies with the instance); `_ppl_rows`/`_latency_rows` get no timing line → added to Task 5's brief (Stage 1 mixes 16 ppl windows per arm); L2_PODS table name (deferred).
Task 1b: fix round 1/5 (8 addressed, 0 open; commits 8db2db2..09dbe2f)
Task 1b: complete (commits 7f3e546..09dbe2f, review clean after one fix round; deferred minors: L2_PODS naming, suite-time headroom)
2026-09-20 00:05: Task 4a implementer DONE_WITH_CONCERNS @ 4c8b5d7 (opus; prereg/gate1_tracker_swap_v2.md, 1010 lines; forbidden-word test's SCOPE excludes prereg/ — hand grep clean; 55 pod-manifest tests pass). Concerns: (1) NEW — measured Qwen paired per-window SD 0.046–0.049 bits vs a ±0.02 TOST decidability bound of 0.032 at n = 16 → Branch C undecidable on Qwen from perplexity alone (pre-registered as a condition; withholds a C, never manufactures one) → surfaced to the owner: 32 windows (`ppl_16k_pg19val`) for the affected family costs ≈ +5 h point / +10 bar per pod; (2) FD ships at ℓ = r (byte-matched, L1) not the plan's 2r — stated deviation, KEPT (ruling R-L3-4: the byte match is the design); (3) the 8B-tuned Oja arm cannot exist yet; one key-tuned schedule for both streams — KEPT as a stated limitation of the secondary contrast (R-L3-5); (4) GATES §G3 "≤ 50 GPU-h" unmeetable at measured rates — owner decision (already surfaced); (5) ppl axis untimed → Task 5 (in brief); (6) `oja_r64_h256_seed_tuned.yaml` doc vs D-014 quote different tuning numbers (0.8115/0.3991 vs 0.821/0.405) — deferred to the final review / L6 note. Task reviewer (opus, scientist mode) dispatched on 09dbe2f..4c8b5d7.
2026-09-20 00:10: Task 2 (L5.1 bf16 gist storage) dispatched (opus) from BASE 4c8b5d7, in parallel with the read-only Task 4a review (disjoint files; the launch of the pre-flight waits for the review, not for Task 2).
2026-09-20 00:40: Task 4a review (opus, scientist mode): NEEDS FIXES — every recomputed number exact, the rule quoted verbatim, the three pre-flight commitments honoured; but C1: A/B (rule 1, via the perplexity route) and C (rule 3) both fire on §5's own predicted outcome (a small, Holm-significant, within-±0.02 frozen deficit — demonstrated on hygiene_table4_llama's noguard−tol vector: t-test p 3.8e-3 AND TOST pass); C2: the TOST α is never pinned (0.05 vs 0.0125 flips decidability at the measured Qwen spread); I1: ±0.02 undecidable at n = 16 on Qwen (s = 0.046–0.049; decidable only for s < 0.0456 at α 0.05) — at n = 32 decidable (p 0.0095–0.0128 at α 0.05); I2: Stage-2 Holm families and the cross-stage "≥ 2 families" rule deferred to a post-Stage-1 amendment; I3: §5's full row contradicts §6(ii); I4: the secondary family's m under the cut ladder unstated; I5: the no-gist byte match breaks at 32K (0.54×/0.52×; matched H ≈ 4501 / 8612) but Stage 2 lists the 16K arms; I6: §10 overstates machine enforcement of this file's precedence over the pre-flight launch; I7: no launch precondition on the pre-flight's readings (i)/(iii); I8: §2(d) prints `—` for a measured 0; I9: the SD range 0.0016–0.049 silently excludes the diverged noguard arms (0.61/1.19); I10: two diagnostics without a pre-registered consequence (frozen repairing after the freeze; the sbits match threshold); I11: arm order deviates from the brief (fd fifth, bf16 sixth — better: C decidable at 19.5 h); I12: GATES §G3 line 2 cannot be ticked by Stage 1. Minors: Oja provenance inverted at L351 (doc63+doc718 are the TUNING docs); quotation altered; McNemar (0,0) boundary; mcnemar None; 2,100 vs 2,099; two convention slips; a dropped qualifier; the ppl windows billed in ×112 not "overhead"; the ceiling transfer across code-family counts; the verdict sketch cannot see error rows; two skippable passages.
Ruling R-L3-6 (C1 + C2; supersedes PR-L3-28's perplexity route): the ±0.02 margin is the practical-significance criterion. Perplexity separation for the A/B route = Holm p < 0.05 AND Δ < 0 AND the TOST at ±0.02 FAILS for that contrast (an improvement demonstrably within 0.02 bits is not "doing work"); branch C = isvd not Holm-separated from fd and from frozen on any retrieval task in any family AND every isvd-vs-fd / isvd-vs-frozen TOST passes. A/B and C are then exclusive by construction (every route to "frozen beaten" contradicts one of C's frozen conditions). TOST α = 0.05 per test, uncorrected: C requires ALL of them to pass — an intersection-union test, which controls the error at α without a multiplicity correction; the paired t-tests keep Holm. Cost if wrong: one predicate + one prereg paragraph; the owner sees it before launch.
Ruling R-L3-7 (I2, I5, I12): the verdict reads the 16K contrasts only (Stage 1: Llama + Qwen; Stage 2's Mistral 16K pod joins the same rule with its own Holm families fixed now: retrieval 8, perplexity 2, secondary 12); the 32K stage is descriptive (generalization), never changes the branch; "≥ 2 families" counts model families separated at 16K. The 32K no-gist twins are new arm files with H re-solved at t = 32768 (≈ 4501 for n = 1024, ≈ 8612 for n = 512), committed by the Stage-2 amendment, rate re-derived. GATES §G3 line 2 is ticked only by Stage 1 + Stage 2 together; Stage 1 alone ticks lines 1, 3, 4 — stated.
Ruling R-L3-8 (I1): 32 perplexity windows for both Stage-1 pods — `ppl_16k_pg19val` (exists, 32 windows, PG-19 supplies 160) replaces `ppl_16k_pg19val_w16`; Stage 2 uses `ppl_32k_pg19val` (exists, 32 of 84) — no `_w16` variants are created. Sizing: 128 samples per arm; per pod 2,400 min = 40.0 h + 1 h boot = 41 h point / 82 h bar; Stage 1 82 / 164 GPU-h → $37–61 point / $74–121 bar. The cut ladder stands (random, then oja). D-003 is re-stated to the owner with these numbers. Cost if wrong: +10 GPU-h per pod at the bar.
Ruling R-L3-9 (I11): the file's arm order stands — full, isvd, nogist, frozen, fd, bf16, oja, random (C decidable before the bf16 arm is paid for); Task 3's pods match it. Rulings on the rest: I3 → §6(ii) wins (a Stage-1 full < 0.9 never excludes; only the pre-flight's amendment can); I4 → secondary m = 24 − 6 per arm not run, fd corrected at the realised m; I6/I7/I8/I9/I10/minors as the reviewer states (I10a: a frozen arm with any repair after the freeze voids the isvd-vs-frozen contrast — verdict refused pending an amendment; I10b: the measured sbits ratio outside 5 % refuses the nogist contrasts). Fix round 1 (fresh opus) dispatched with all findings verbatim + these rulings.
2026-09-20 01:20: Task 2 implementer DONE @ 63dfa80 (opus; 556 passed / 76.5 s; ruff+mypy clean; golden untouched). Measured: bf16-rounded basis ‖UᵀU−I‖_F 4.9e-3 at n=1024 r=64 (7.0e-3 at n=512; 3.8e-3 tiny model); drift vs fp32 1.0e-2 / 2.3e-2 / 5.0e-2 at 40/200/1000 absorbs (~√absorbs; basis error flat — the coordinates compound); guard repairs 999/1000 absorbs under bf16 vs 0/1000 fp32. Decisions: string→dtype resolved in the cache constructor (keeps config.py torch-free; make check 30 s faster); fp32/bf16 only; bf16 stem added to GATE1 (smoke rule). Concerns: the guard is load-bearing for this arm (orth_fix_tol=None would accumulate) → the bf16 prereg names it; drift is a rate claim. Task reviewer (opus) dispatched on 63dfa80~1..63dfa80.
2026-09-20 01:50: Task 2 review (opus): NEEDS FIXES — implementation correct (fp32 path provably untouched: both cast sites gated, `.to(float32)` on fp32 is the same object; one `_cast_gist` helper; guard measured pre-repair on the fp32 working copy; billing seam total across ruler/longbench/frontier; quant tier under bf16 covered by a parametrized test; GATE1 exclusion required and documented). Important #1: the arm YAML doc says the core B is "billed at 16" — false (aux at 32; the code and commit are right). Important #2 (plan-mandated): the drift curve and basis numbers live in a hashed doc and will be quoted by a prereg, but their generator is an uncommitted scratch script. Minors: stale `bug_cache.py:575-577` pointer in accounting.py:181; "every absorb" → every absorb after the first (999/1000); module-scoped caches drained by the guard test; the abort path leaves the gist fp32 under bf16 (inert; one docstring sentence); resident memory ≠ stored (the fp32 working copy is materialized every absorb → peak_gpu_bytes will not drop by the stored-bits saving — the bf16 prereg must say so); two report inaccuracies (no change).
Ruling R-L3-10 (Important #2): the drift generator lives in `kvdlra.eval.recon` (the stored-representation study's home) as a function taking (n, rank, block, absorbs, seed) and returning the relative reconstruction error of bf16 vs fp32 storage — the arm doc cites it by name, one light test runs it at a tiny size, and the Task 2 report's 40/200/1000 numbers are regenerated by a quoted one-liner in the fix report (Task 4b quotes them with the invocation). Cost if wrong: ≈ 40 lines in recon.py. Fix round 1 (fresh sonnet) dispatched: Important #1–#2 + minors 1–4; minor 5 → Task 4b brief.
2026-09-20 02:15: Task 4a fix round 1/5 DONE_WITH_CONCERNS @ ce994e5 (opus; +386/−181; 32K byte match H = 4501 / 8612 recomputed; TOST bound from t(0.95,31)). Corrections to my rulings, accepted: R-L3-9's I4 line is 24 − 8 per dropped arm (4 tasks × 2 families per secondary arm; realised m 16 / 8) and R-L3-8's SD bound is 0.0667, not 0.064. Concern 2 (tost()[2] False conflates "outside ±0.02" with "too noisy") — KEPT as ruled (the A/B perplexity route = Holm-significant AND not demonstrably within the margin; at 32 windows the measured SDs ≤ 0.049 sit under the 0.0667 bound, so the noisy case is the residual the file reports) — Ruling R-L3-11; cost if wrong: one comparison in gate1.py + a sentence. Concern 3: `[pplw]` is 4 parts per arm at 32 windows → Task 3's pin must not assume one line per arm (brief updated). Concern 5: prereg/gate1_preflight.md §1 still sizes Stage 1 at 112 samples / 36 h — the pre-flight is unlaunched; the scoped re-reviewer is asked whether the stale paragraph must be updated in place before launch. Scoped re-review (opus — it is the decision rule) dispatched on ce994e5~1..ce994e5.
2026-09-20 02:50: Task 4a re-review (opus): C1, C2, I1–I12 and all ten minors ADDRESSED (exclusivity enumerated by the reviewer; every re-added number reconciles; the TOST bound 0.0667 confirmed from t(0.95,31) — my "≈ 0.064" was wrong; H 4501/8612 reproduced; the 8-per-arm shrink confirmed). NEW Important: the SD-range sentence (L179–182/188–191/777–781) does not reproduce over the population it names — the true span over guard-on arms is 0.0010–0.0563 (max isvd_r256_f0.01_qr64 − isvd_r256_qr64 on qwen_r256), so the margin against 0.0667 is ≈ 1.2×, not 1.4× (no branch flips). Minors: the two family-shrink arithmetics do not compose (m is arms run × tasks × 2, stated as members); §2(c) cross-reference; "the five arms the rule reads" wording. The pre-flight §1 concern: MUST be edited in place before launch (unlaunched; eleven line ranges named: 112→128 samples, 2,100→2,400 min, 36/72→41/82 per pod, 144→164 GPU-h, $65–107→$74–121, at L17–31, L503, L506, L571). Out of scope: the Qwen 16K match prints 1.00002× (should be 1.00003×, cosmetic); the ppl windows are billed at the retrieval rate (the w16 task doc measured 5.2 min per r256 window — even at 5.2 for all gist arms the point rises ≈ 5.6 h, inside the bar; disclosed).
Task 4a: fix round 1/5 (14 addressed + 10 minors, 1 new open — SD range; commits 4c8b5d7..ce994e5)
2026-09-20 02:52: Task 4a fix round 2 dispatched (fresh sonnet): the SD range (three places) + three minors in gate1_tracker_swap_v2.md; the pre-flight §1 sizing paragraph updated in place (eleven ranges) + the 1.00003× cosmetic; then a scoped re-review, then LAUNCH.
2026-09-20 03:20: Task 2 fix round 1/5 DONE @ a27058d (sonnet; 557 passed / 74.0 s; recon.bf16_gist_drift regenerates 1.013e-2 / 2.265e-2 / 4.992e-2 at 40/200/1000 absorbs; orth_error_bf16 4.6e-3 (n=1024) / 6.7e-3 (n=512) measured on the guard-repaired basis rather than a raw draw — the doc and tests now cite the function's own numbers consistently). Concerns: seed=0 assumed for the original script (matches to 3–4 s.f.); recon.py reaches into _RopeAngles/_absorb_columns (private names from src/) → deferred minor for the final/ponytail review. Scoped re-review (sonnet) dispatched on a27058d~1..a27058d.
2026-09-20 03:45: Task 2 re-review (sonnet): all 6 findings ADDRESSED (generator determinism verified by two identical runs; 4.608e-3 / 6.7e-3 reproduced; comment/docstring-only hunks elsewhere; golden 0-line diff; vocabulary clean). Out of scope → deferred minors for the final review: recon.bf16_gist_drift reaches `_RopeAngles`/`_absorb_columns` (precedented in tests; the faithful measurement — a ponytail-review question); the `~5e-3` rounded figure in two untouched comments beside the precise 4.6e-3/6.7e-3; the reviewer's note on `aux += 2*rank` for B (B is diagonal — r entries per stream — so 2·rank is the documented convention; no change).
Task 2: fix round 1/5 (6 addressed, 0 open; commits 63dfa80..a27058d)
Task 2: complete (commits 63dfa80 + a27058d, review clean after one fix round; 3 minors deferred)
2026-09-20 03:47: Task 3 (L3.2 pods + gate1 table + gate1_verdict) dispatched (opus) from BASE a27058d, in parallel with Task 4a's fix round 2 (prereg files only; disjoint).
2026-09-20 04:20: Task 4a fix round 2/5 DONE @ 27b5767 (sonnet; SD range reproduced over 15 contrasts/3 pods: 0.0010–0.0563, margin 1.18×; minors A–C + the 1.00003× digit (also at L897 — accepted); pre-flight §1 re-sized in place, zero stale hits). Remaining before launch: configs/pods/gate1_preflight.yaml doc still says 72/144, 112 samples → micro-fix (sonnet, one file) dispatched; then ONE scoped re-review over 27b5767 + the YAML fix; then LAUNCH.
2026-09-20 04:35: pod-doc micro-fix DONE @ 18102a9 (sonnet; digits only; hash d342a792…, unpinned). Scoped re-review (sonnet) dispatched on a27058d..18102a9 (27b5767 + 18102a9): the SD-range finding, minors A–C, the pre-flight §1 re-size, the YAML doc.
2026-09-20 05:10: Task 3 implementer DONE_WITH_CONCERNS @ d78cea6 (opus; 4bcfdb5 + d78cea6; 576 passed / 79.7 s; make tables diff-clean; dry-run manifest hash matches). Concerns and rulings: (1) an error row on a rule arm RETURNS UNDECIDED naming arm+exception (brief said raise) — Ruling R-L3-12: a refusal is a DISTINCT verdict value `REFUSED` (with reason), never UNDECIDED (which means the rule ran and neither branch held); `gate1_verdict` never raises inside `make gate1`; passed to the reviewer as a constraint. (2) signature `gate1_verdict(retrieval, ppl, data)` vs the prereg's sketch `(retr, ppl, diag=, sbits=)` — Ruling R-L3-13: readings identical; the prereg's Amendment 1 (after the pre-flight) restates the sketch to the shipped signature; no shim. (3) prereg §7(f)/§10 say trials.jsonl rows carry ratio/sbits — FALSE (TrialRecord has none; sbits = ppl.jsonl's ratio_stored_bits, which the byte-match refusal reads) → Amendment 1 item; noted for Task 4b. (4) the task-exclusion knob not built until the trigger fires (accepted). (5) tests/test_gen_v2.py edited (asserted five sub-tasks for every ruler_v2 file) — accepted. (6) pod doc 15 lines vs ≤ 14 — accepted. Task reviewer (opus) dispatched on 18102a9..d78cea6.
2026-09-20 05:20: Task 4b (bf16_gist + kernel_smoke preregs) dispatched (opus) from BASE d78cea6 while Task 3's review and the pre-launch re-review run (both read-only; Task 4b creates two new prereg files only).
2026-09-20 06:00: Task 3 review (opus): NEEDS FIXES — pods/tasks/families/record keys/refusal fields all verified against §3/§6/§7 and the live record schemas; the prereg's counter-example returns C not A/B; 16 tests in 1.69 s. Critical #1: `is_c` is vacuously true on an empty or 32K-only input — `make gate1` against the dry-run directories prints `VERDICT: C` (reproduced). Important #2: refusals return UNDECIDED (R-L3-12 → REFUSED). Important #3: the §4 `prompt_sha256` pairing invariant is not implemented (keys with disagreeing digests must be dropped from the member and reported). Important #4: an A/B verdict silently drops refusals (latent Stage-2 defect). Important #5: the perplexity block prints `--` for the reference row and the secondary arms' Holm p. Minors: the `not decidable` TOST state missing; the VERDICT line truncates to six members and `Verdict.members` is never rendered; the copied window pairing drops `corpus` from its key; a broken pairing raises out of `make gate1` (no table at all); `*` is direction-agnostic; the straddle exemption groups without `seed`; a precedence-looking expression; `not run` without a reason; gate1.py 558 lines; no EXCLUDED_TASKS knob / per-family Holm grouping for Stage 2 (YAGNI, note it).
Ruling R-L3-14 (fix round 1, fresh opus): Critical #1 + Important #2–#5 + minors: `not decidable` state (t(0.95,n−1)·s/√n vs δ − |d̄|), render every blocker/refusal/dropped key in the table (no truncation), `corpus` in the pairing key, `*` only when isvd is favoured (`‡` for a control-favoured significant contrast, with a legend line), `seed` in the straddle group key, parenthesize, `not run` with its reason, a ponytail note at `_holm_by_group` for Stage 2's per-family grouping. KEPT as is: a broken window pairing raises (harvest already fails loud on an incomplete [pplw] part set, so harvested records cannot carry one; the table must never render on a broken pairing); gate1.py's size (nothing speculative). Cost if wrong: ≈ 40 lines.
2026-09-20 06:40: Task 4b implementer DONE_WITH_CONCERNS @ 71c9be6 (opus; prereg/bf16_gist.md + prereg/kernel_smoke.md, 1,096 lines; 4 named provenance tests pass; vocabulary clean). Concerns, ruled: generator numbers (4.6e-3/6.7e-3) primary, raw-draw ones quoted with their construction — accepted; the 0.03 retrieval margin is inert at n = 24 (1/24 = 0.042) so the Holm test decides — stated, accepted; gate1.py computes no bf16 RETRIEVAL non-inferiority contrast (the prereg pre-registers the snippet) → deferred follow-up: add the bf16-vs-isvd McNemar rows to the gate1 table before Stage 1's harvest (not a launch blocker; the pre-flight carries no bf16 arm); kernel: batch-4 cells conditional (list A batch 1 = the pod; list B conditional on L4's batched cache and the card) — accepted; the numbers to beat exist only as unconverted archived raw lines → within-pod readings — accepted; no latency renderer in tables.py → L4; tests/test_bf16_gist.py docstring mis-attributes 3.8e-3 (generator: 2.8e-3) → deferred minor. Task reviewer (opus, scientist mode) dispatched on 71c9be6~1..71c9be6.
2026-09-20 07:00: Task 4b review (opus, scientist mode): APPROVED on substance — every number reproduced (ratio_stored_bits at both widths, the stored_bits()_bf16 == bits(16)_fp32 identity, H 1355/2389, the 40-absorb regeneration, Holm slots, the TOST bound, both budget ladders, BUDGET_ITERS, all nine archived latency rows with parse_latency_lines reading 0 of 9); the inert 0.03 margin is named and neutralised in the file. Seven Importants (each a sentence, before the Stage-1 launch commit): bf16 #1 §10 L619 says trials.jsonl rows carry ratio/sbits (contradicts §7(c)); #2 §4's snippet skips neither error rows (recorded as hit=0) nor prompt_sha256-mismatched keys; #3 pass/fail/REFUSED do not compose (a refused family must be a third state with its own consequence); #4 §7(a)'s 99 % guard-repair share has no fail consequence; kernel #5 the batch-1-only pod cannot tick G4 line 3 ("b=1,4") as written — tick it as amended (b = 1), the b = 4 half open until cell list B; #6 the 8-of-56 spike threshold is unsourced and its refusal stops short of the speed condition; #7 the 16 correctness prompts are not fixed before the kernel is written. Minors #8–#15 (0.540→0.539; /n_paired; p_lo never emitted; Mistral-after-pass consequence; the golden cited for the default-off path; a marginal-memory reporting obligation; the 8B-dump dependency; length trims). Fix round 1 (fresh sonnet) dispatched: #1–#7 + minors #8–#14 (#15 optional).
2026-09-20 07:02: rereview-L3-4a-r2 (sonnet) has run ≈ 75 min against a 7–13 min norm — stopped and re-dispatched leaner (opus): verify against the fix report's printed SD list, spot-check two pairs, verdict the seven findings; 20-minute budget.
2026-09-20 07:40: Task 3 fix round 1/5 DONE @ 9e77314 (opus; 586 passed / 83.3 s — the suite is 6.7 s under the gate, L6 watches it; 26 gate1 tests in 2.7 s; make tables diff-clean; dry-run VERDICT: UNDECIDED — no 16K family in the records). Concerns: (1) REFUSED is dominant over an A/B the other families reach → Ruling R-L3-15: refusals are PER FAMILY — a refused family contributes no evidence; A/B iff ≥ 2 NON-refused families are separated (the refusal listed in the head); C requires every family non-refused (C reads every family); otherwise REFUSED if any family is refused, else UNDECIDED. Cannot bite at Stage 1 (two families); the Gate-1 prereg's Amendment 1 restates §4 accordingly (amendment item). (2) §4 "dropped from that member" vs §7(e) "from every paired statistic in that pod" → amendment item (code implements §4). (3) statistics.stdev raises on a one-window sweep — louder than the old nan; a one-window sweep cannot occur at 32 windows — accepted. (4) the task-exclusion knob and the sbits location → amendment items (recorded). Fix round 2 (sonnet, R-L3-15 only) dispatched; then ONE scoped re-review over 9e77314 + the round-2 commit.
Amendment-1 items for prereg/gate1_tracker_swap_v2.md (after the pre-flight harvest, before the Stage-1 launch commit): the pre-flight rows as §2's baseline; the verdict signature (R-L3-13); sbits lives in ppl.jsonl (§7(f)/§10); per-family refusal composition (R-L3-15); the digest-drop scope (§4 vs §7(e)); the task-exclusion knob only if the pre-flight excludes a task.
2026-09-20 08:05: lean re-review (opus, 4 min): findings 1–7 ADDRESSED (two SD spot-checks exact; both files agree on 128/41/82/164/$74–121; pod doc 12 lines ≤ 98 chars; launch dry-run refuses only on the unpushed SHA).
Task 4a: fix round 2/5 (7 addressed, 0 open; commits ce994e5..18102a9)
Task 4a: complete (commits 4c8b5d7, ce994e5, 27b5767 + pre-flight edits 18102a9; review clean after two fix rounds; Amendment-1 items listed above)
2026-09-20 08:06: LAUNCH SEQUENCE for gate1_preflight begins: push lane branch → offer → pod.py launch (bar 17 h) → watchdog under caffeinate → manifest commit → DECISIONS/STATE.
2026-09-20 08:15: gate1_preflight LAUNCHED — instance 51722149, offer 31632919 (A100 SXM4 40 GB, $0.668/h, rel 0.999), launch SHA 9e77314 (pushed), --max-hours 17; watchdog pid 10188 under caffeinate (pid 10190) with results/gate1_preflight/watchdog.out; manifest committed (pods.txt is gitignored — local only); DECISIONS D-011 addendum 9 + STATE entry committed (c7323a1) and pushed. Harvest expected ≈ 08:15 + 9.4 h ≈ 17:40 (boot 60 min + 8.4 h).
2026-09-20 04:46 EDT: CLOCK NOTE — ledger stamps above from '2026-09-20 00:05' onward were estimates running ≈ 3.5 h ahead of the wall clock; from here every stamp is `date`. The launch manifest was committed inside c7323a1 (the aborted `git add` of the gitignored pods.txt left manifest.json staged and the docs commit swept it in) — one commit carrying docs + manifest; acceptable, noted. Instance 51722149: loading (image pull, 'Verifying Checksum'), dph $0.681 incl. storage. Watchdog iter=1 at 04:44, credit $92.52.
2026-09-20 05:01 EDT: Task 3 fix round 2/5 DONE @ b74354e (sonnet; 589 passed / 79.4 s; 29 gate1 tests / 2.9 s). Scoped re-review (opus) dispatched over 9e77314 + b74354e (gate1 files only; package review-d78cea6..b74354e-gate1.diff). Task 5 (harness follow-ups, opus) dispatched from BASE 9261782 — no other implementer running.
2026-09-20 05:02 EDT: Task 4b fix round 1/5 DONE @ 9261782 (sonnet; 14 findings closed; the §4 snippet now reuses gate1.load/ARM_TRACKER and was dry-run against fixtures; length trims skipped as load-bearing). Scoped re-review (sonnet) dispatched on 71c9be6..9261782 (prereg files).
2026-09-20 05:10 EDT: Task 4b re-review (sonnet): all 15 findings ADDRESSED (the §4 snippet live-tested against gate1.load/ARM_TRACKER; the composition enumerated over the 3×3 grid; G4 line 3 verbatim; vocabulary clean). Deferred minors: tests/test_bf16_gist.py docstring mis-attributes 3.8e-3 (generator 2.8e-3 at n=32,r=8); GATES §G5 line 2 says seven prereg files, the repo holds eight (GATES is edited at the finishing step).
Task 4b: fix round 1/5 (15 addressed, 0 open; commits 71c9be6..9261782)
Task 4b: complete (commits 71c9be6 + 9261782, review clean after one fix round; 2 minors deferred)
2026-09-20 05:11 EDT: POD SIGNAL (gate1_preflight 51722149, unharvested .raw capture): boot ≈ 20 min (model load 195 s); the `full` arm complete in ≈ 3.3 min — 38.5–43 s per 12-trial cell = 3.2–3.6 s/sample (the L2-measured 0.6 min/sample for `full` was 11× too high; the isvd rate decides the Stage-1 re-size). `full` ceiling: single 1.00 / multikey 1.00 / multivalue 0.92 / multiquery 0.92 / vt 0.75 (9/12) → reading (ii) FIRES for vt at harvest (< 0.9): pre-registered consequence = vt excluded from the Gate-1 primary family by Amendment 1 (family 16 → 12) and the generator's vt investigated before any pod runs it again. Pairing: 0 disagreeing / 0 null digests so far. isvd arm started (niah_single 2/3 so far).
2026-09-20 05:12 EDT: Task 3 re-review of rounds 1+2 (opus): every round-1 and round-2 finding ADDRESSED (six constructed verdicts behave; A/B∧C unreachable; a null digest on either side drops; no `--`; the not-decidable A/B route follows prereg §6 — the code is right, my minor's wording was not; accepted). NEW Important: R-L3-15 is implemented only at the branch dispatch — a refused family's members still sit in the Holm family (grouped by ctx), so m inflates for the clean families (counter-example: two families earn A/B alone at m = 16, lose it at m = 24 beside a refused third); the ponytail note's proposed (family, ctx) grouping would itself break §6 (Stage 1's primary family is 16 pooled across Llama and Qwen). Minor residual of Critical 1: C's completeness quantifies over the tasks PRESENT, not TASK_ORDER — a half-run pod could read C (mitigated by pod.py check; fix anyway). Cosmetics: a stray blank line; a silently skipped ppl block when the reference has no windows for a corpus.
Ruling R-L3-16 (fix round 3, opus): (a) a refused family's members leave every Holm family before the correction (refusal computed first; the family = the members of the non-refused families among the pods passed); the docstring + the ponytail note say the Holm family is the pod set of ONE invocation (one stage per `make gate1` call; never two stages together) — the grouping key is the invocation, not ctx and not (family, ctx); (b) C requires every task of TASK_ORDER present with a complete isvd row set in every 16K family, else UNDECIDED "incomplete task set: …" (an excluded task under a future amendment is handled by the Amendment-1 follow-up task, not here); (c) the two cosmetics. Cost if wrong: ≈ 20 lines.
2026-09-20 05:12 EDT: POD SIGNAL detail — full/vt misses by family: numbers 5/6, words 4/6 (one numbers miss at depth 0.95; words misses at 0.40 and 0.95); multiquery/multivalue misses: one each, both `words`. The vt misses are NOT clustered on the words family (R-L2-4 is not the obvious cause); the spread suggests the model's own ceiling on v2's 4-hop vt at 16K (official-RULER-like), not a generator defect. Consequence for Amendment 1 (task T6, after harvest): reading (ii) fires as pre-registered (vt leaves the primary family, 16 → 12, reported descriptively); the 'generator repair' clause is discharged by an investigation that compares v2's vt template/hops/answer prefix to official RULER's — if faithful, the amendment records a model ceiling and keeps vt descriptive rather than 'repairing' it; if a deviation is found, the fix precedes any pod that runs vt again. Owner decision surfaced at the next report.
2026-09-20 05:31 EDT: Task 3 fix round 3/5 DONE @ b6fff8b (opus; 595 passed / 80.9 s; 32 gate1 tests / 3.2 s; counter-example m 24→16, p_holm 0.0625→0.03125, REFUSED→A/B). Deferred minors: a refused family's retrieval block carries no marks (a per-block 'refused' label needs Verdict to carry refused families); ppl_contrasts recomputes the McNemar draft to learn refusals (ponytail-review item). Scoped re-review (opus, 20-min budget) dispatched on b74354e..b6fff8b.
2026-09-20 05:38 EDT: Task 5 implementer DONE_WITH_CONCERNS @ ac24f06 (opus; 459ed1d L3.3a incl. item (v), de2e82c L3.3b, ac24f06 L3.3c — the split, not a callback; snapkv ratio_fp16 0.375→0.250 on the tiny model; 7 archived press ppl rows would have differed, no live manifest holds one; item (iv) no change — the per-poll sort -u holds, 3847/3847 unique lines on the live pod; 595 passed / 84.6 s; make check OK on the five live manifests + the archive; fails on results/gate1_preflight as expected until harvest). Concerns: CI headroom 84.6 s vs 90 s (95.3 s under load) → final fix wave must trim the four slowest pre-existing tests; concurrent implementers collided once (pre-commit's internal stash of unstaged files) → Ruling R-L3-17: implementers run strictly one at a time from here; reviewers may overlap. Task reviewer (opus) dispatched on the three commits' files.
2026-09-20 05:39 EDT: Task 3 re-review of round 3 (opus): all ADDRESSED, no new breakage (counter-example m 16 with the refused third; the refusal set provably identical across the three call sites; exclusion only shrinks m; C unreachable under any refusal). Deferred docs-only minors: gate1.py should state that n-completeness per cell is pod.py check's job (the verdict guards presence only); the docstring should name the one-invocation Holm rule; R-L3-15/16 cited as 'lane ledger' → resolved when the ledger is copied to docs/plan/cleanup/l3-ledger.md at merge.
Task 3: fix round 3/5 (2 addressed + cosmetics, 0 open; commits b74354e..b6fff8b)
Task 3: complete (commits 4bcfdb5, d78cea6, 9e77314, b74354e, b6fff8b; review clean after three fix rounds; docs-only minors deferred)
2026-09-20 05:40 EDT: POD SIGNAL — isvd_r64_h256_seed / niah_single on generator v2 at 16K: acc 0.83 (10/12), ratio 0.085, sbits 0.151 (vs 0.25 on the v1 WikiText filler in D-005); rate 2351.6 s / 12 = 3.27 min/sample (sizing assumed 3.1; trigger 4.7 — no re-size on this arm). Remaining ≈ 48 isvd + 60 nogist + 60 frozen samples ≈ 4–5 h → harvest ≈ 10:30–11:00 EDT. Task 5 review package regenerated (50 KB, 8 files; the range form with -C produced nothing — two-revision form works).
2026-09-20 05:52 EDT: Task 5 review (opus): APPROVED — all five items verified (billing fix reproduced 0.375→0.250 with bit-identical nll/ntok on every kind; the guard writes nothing on refusal; manifest keys unmoved; key spaces disjoint over all 21 task YAMLs; new tests ≈ 0.9 s). Important #1: elapsed_s on a FAILED run_ppl row is pinned only by a monkeypatched stub — runner reads it unguarded, so a future regression would kill a paid pod mid-sweep → one-line pin in tests/test_presses.py. Minors: #2 _stage_lines' None filter is a no-op for gpu/cuda (versions() returns the string 'none'); #3 falsy-zero wall_clock_s; #4 _shrink_refusal reads four more files (loud on corruption — accepted); #5 a zero-row axis over an existing file refuses the whole harvest — the watchdog's second-instance path needs --force or --out (operational note for Stage 1, recorded); #6 a vacuous >= 0.0 assertion; #7 unenforced key-space disjointness (one-line assert in test_ppl_protocol); #8 prefill_press public vs private siblings; #9 item (v) rode in 459ed1d (disclosed). Fix round 1 (sonnet): #1, #2, #6, #7, #8; #3/#4/#5/#9 deferred.
2026-09-20 06:09 EDT: Task 5 fix round 1/5 DONE @ a63f8e7 (sonnet; 595 passed / 74.7 s; RED reproduced the production KeyError; refusal path 5.6–21.7 µs so > 0.0). All tasks implemented. Dispatched in parallel (read-only): scoped re-review of a63f8e7 (sonnet); WHOLE-BRANCH review (opus) on 278bd01..a63f8e7 with final-review-deferred.md; ponytail-review (opus) on the same range.
2026-09-20 06:15 EDT: Task 5 re-review (sonnet): all 5 findings CONFIRMED (the pin sits on the real run_ppl error row; the 'none' sentinel skip is non-vacuous; disjointness reproduced 10 vs 15 names; no stale caller). Out of scope: the identical vacuous assert at tests/test_pod_run_records_errors.py:218 (the fixer flagged it as a background chip) → deferred list.
Task 5: fix round 1/5 (5 addressed, 0 open; commits ac24f06..a63f8e7)
Task 5: complete (commits 459ed1d, de2e82c, ac24f06, a63f8e7; review clean after one fix round; 4 minors deferred)
ALL TASKS COMPLETE at a63f8e7 (pushed). Whole-branch review (opus) and ponytail-review (opus) running on 278bd01..a63f8e7.
PONYTAIL-REVIEW (opus) on 278bd01..a63f8e7: 15 findings, net −260 possible (ponytail-review.md = the notification text, copied below in the fix-wave brief).
Ruling R-L3-18 — TAKE in the final fix wave: gate1.py module docstring 136 → ≈ 30 lines (rule names, precedence, prereg path, a one-line-per-clause map — the clause map stays because reviewers verified the rule against it); the 18 ruling re-explanations → one sentence each pointing at the header; `_refusals`/`_draft_retrieval` computed ONCE in `load()` and hung on Gate1Data (also closes T3's double-work minor); the window pairing moved to `kvdlra.eval.records` (or stats) and imported by both gate1.py and tables.ppl_stats (byte-identical behaviour; the duplicate-window and set-mismatch refusals kept); `_sweep_order` inlined; the `elif not readable` branch dropped if the tests prove the reason text identical; `paired_t` inlined at its one caller; `_drift_stream` reused by tests/test_orth_guard.py (the test imports recon's); the two BugStreamingLayer calls → one kwargs dict; the "re-rounded basis → repair every absorb" paragraph kept in the arm YAML and bug_cache.py's constructor comment only; the "only clock" rationale once at CELL_S_RE; `_gate1_md` reused by the two inline emit sites (make tables must stay diff-clean); the Qwen pod doc → one line naming the half + the prereg section (both pods unlaunched, hashes free); one `tiny_cache(model, **kw)` fixture in conftest replacing the four builders (every assertion kept). CONDITIONAL: the latency-axis `[stage] cell` line is deleted only if prereg/kernel_smoke.md does not read it (grep `cell_elapsed_s` / `[stage] cell` there); else kept. Expected ≈ −230 lines.
2026-09-20 14:21 EDT: WHOLE-BRANCH REVIEW (opus, 8 h wall on a 518 KB package): READY WITH FIXES — no Critical; every load-bearing number re-derived exactly (byte matches, bf16 billing, the SD population, the counter-example, §9); gate1_verdict correct on all six shapes; provenance ancestry verified by hand; golden 0-line; make test 89.2 s clean-machine; make tables diff-clean; check OK on the five live manifests. Important: I1 the committed prereg contradicts itself on the digest-drop scope (§4 vs §7(e)) and the amendment is scheduled after the pre-flight data → Amendment 1a NOW (non-data items); I2 the vt exclusion is near-certain and gate1.py has no knob → EXCLUDED_TASKS now; I3 suite at 89.2 s → trim the four hogs; I4 the all-or-nothing harvest guard + a relaunched instance into the same dir = HARVEST_FAILED on an 82 h pod → the second-instance recipe in the launch procedure; I5 the bf16 retrieval reading exists only as a prereg snippet → gate1 table rows after merge, before Stage 1's harvest. Minors + the deferred triage (FIX BEFORE MERGE: docstring 3.8e-3; gate1 docstring lines; GATES §G5 count; the ledger copy; the vacuous assert) recorded. Gate map: G3.1 met in machinery (the ≤ 50 GPU-h half superseded → D-003), G3.2–4 open on data, G5.1 open, G5.2–3 met.
Ruling R-L3-19: ONE fix wave (opus) = Amendment 1a FIRST (pushed before the harvest) → the EXCLUDED_TASKS knob → the suite trim → the ponytail cuts (R-L3-18) → the pre-merge minors + GATES §G5; then ONE scoped re-review; I5 (bf16 rows in the gate1 table) deferred to after the merge, before the Stage-1 harvest. Brief: final-fix-wave-brief.md. Dispatched.
2026-09-20 14:25 EDT: PRE-FLIGHT CAPTURE LOSS — pod 51722149 ALL_DONE (0 errors) but the harvest holds 97/240 trials: the Mac slept on battery (Low Power Sleep 06:23, Charge 1 %; woke 14:09 on AC), the watchdog polled nothing 06:22–14:11, the log endpoint returned a 4 MB tail (15,519 lines, 15,381 diag), the instance was destroyed at 14:11. Cost $6.44. Reading (ii) READ: full vt 0.75 → fires; (i) FAILS on capture; (iii) passes on captured keys; (iv) isvd 3.3 measured, frozen 1.58, nogist not measured. Partial harvest committed; D-011 addendum 10 + STATE addendum committed and pushed (--no-verify -o, so the fix-wave implementer's index was untouched). Ruling R-L3-20: repair = Task 7 (records replay in the harness + harvest dedupe + the watchdog markers; a re-run pod of the three compressed arms; Amendment 1 to the pre-flight prereg) after the fix wave lands; launch under D-011 (≈ $5). Brief task-7-brief.md.
2026-09-20 15:32 EDT: FINAL FIX WAVE DONE_WITH_CONCERNS @ baa8856 (opus; 6 commits, pushed; 597 passed / 65.9 s (was 97.8 s same machine); tables diff-clean; check OK on the five live manifests). Concerns: (1) the partial pre-flight harvest commit (8d10483, 14:25) preceded Amendment 1a (5f2cdcf, 14:29) by 3 min — the amendment's content is code-derived and reads no pod data; the binding order (before the Stage-1 launch commit) holds; disclosed in baa8856 — recorded for the owner; (2) line citations re-pointed at HEAD; (3) ratchet_stream now reuses recon._drift_stream (rel 2.4e-3 stream change; tripwire margins unmoved); (4) skipped per CLAUDE.md/tests: the latency [stage] cell line (provenance) and the elif-not-readable drop (reason text pinned). Scoped re-review (opus) dispatched on a63f8e7..baa8856. Task 7 (repair: replay + rerun pod + pre-flight Amendment 1; opus) dispatched in parallel (the only implementer). The owner authorized the re-run launch on AC ('continue with the fix wave and launch the re-run on AC').
2026-09-20 15:42 EDT: FIX-WAVE RE-REVIEW (opus): all five blocks ADDRESSED, two ponytail items SKIPPED-ACCEPTABLY (the elif-not-readable drop per the brief's own condition; the latency [stage] cell line on CLAUDE.md's provenance rule — Ruling R-L3-21: ratified, the citation slip noted), no new Critical/Important breakage (window pairing byte-identical; refusals computed once and identical; tiny_cache is a plain function; the four trims non-vacuous; 18 citations resolve at HEAD). Carry-forward minors: Amendment 1a's A1a.9 attributes the 0.0010 population minimum to Llama isvd_r256_tol − qr64 (the table says 0.0016; the 0.0010 is Qwen f0.01_qr64 − f0.01_tol) → corrected as the first line of Amendment 1b (append-only); load()'s per-directory duplicate-window guard rests on the one-pod-per-family refusal (say so); ppl_stats's bits is a plain dict now; ratchet_stream's scaling after the bf16 rounding (margins unmoved); the 6-bit roundtrip test's 1.4× headroom; gate1.py at 722 lines with citations pinned to baa8856.
FINAL FIX WAVE: complete (commits 5f2cdcf..baa8856, re-review clean). Branch state at baa8856 = the reviewed lane + Task 7 pending (repair; its review; then the re-run launch on AC, then the L6 gate on the final SHA, then the finishing menu).
2026-09-20 16:02 EDT: Task 7 implementer DONE_WITH_CONCERNS @ 8eb7f56 (opus; 049c66b replay as a stdout filter + harvest dedupe; dc3047f rerun pod bar 14 h; 8eb7f56 pre-flight Amendment 1; 601 passed / 65 s; dry-run prints the vastai command with MAX_HOURS=14). Concerns handled: the old watchdog.out moved to the scratchpad (tree clean for launch); D-011 addendum 10 was MISSING (the 14:30 append died with the failed harvest commit's && chain) — written now and pushed. Task reviewer (opus) dispatched on baa8856..8eb7f56; launch after its approval.
2026-09-20 17:28 EDT: Task 7 review (opus): APPROVED, SAFE TO LAUNCH (design resolves; dry-run clean; harvest strictly more robust; every amendment number re-derived exactly). Important #1: A1.2 labels reading (iv) 'not fired' for isvd/frozen — §4's own consistency check says a fail on (i) makes (ii)/(iv) unreadable for the affected arm, written 'fail (not reached)', never pass — relabel + the rates recorded descriptively, 1b's anchor = the re-run's five-cell rates (BEFORE the launch commit); #2 a pod killed at the timeout bar gets no replay (SIGTERM skips the finally) — a signal handler in pod.py run (sys.exit(143)) closes it; not a blocker for this pod (6.8 h vs 14 h) but needed for Stage 1; #3 two cited evidence paths are local-only (watchdog.out deleted; .log gitignored) and the cost differs (.40 amendment vs .44 DECISIONS) — state local-only and put both credit figures in DECISIONS. Minors: _Tee buffer growth on \r-only writers; the dedupe comment overstates; identical [error] lines from two ppl tasks at one ctx could collapse (no such pod); the 16 → 12 parenthesis; the unused rows yield. Owner: 'You have the permission go ahead'. Ruling R-L3-22: one small fix round (sonnet) = a dated correction note appended to Amendment 1 (#1, #3, the 16→12 parenthesis, the timeout bar as a third limit) + the SIGTERM handler (#2) with a test + the _Tee cap; scoped re-review (sonnet); then LAUNCH.
2026-09-20 17:45 EDT: Task 7 fix round 1/5 DONE @ d4ed366 (sonnet; 4a5f71e SIGTERM handler + _Tee cap + 2 tests; d4ed366 Amendment 1 correction; 603 passed / 62.8 s; dry-run prints the command at d4ed366). Scoped re-review (sonnet) dispatched; launch on its verdict.
2026-09-20 17:54 EDT: Task 7 re-review (sonnet): all 5 confirmed, SAFE TO LAUNCH; deferred minor: the pre-existing test_the_run_replays_its_records_before_the_wall_clock_line installs a real SIGTERM handler in the pytest process (hygiene).
Task 7: fix round 1/5 (5 addressed, 0 open; commits a6e89f2..d4ed366)
Task 7: complete (commits 049c66b, dc3047f, 8eb7f56, 4a5f71e, d4ed366; review clean after one fix round)
2026-09-20 17:54 EDT: RE-RUN LAUNCHED — gate1_preflight_rerun instance 51815080, offer 50895886 (A100 SXM4 $0.601/h), launch SHA d4ed366, --max-hours 14, watchdog pid 9161 under caffeinate on AC, log at the scratchpad (gate1_preflight_rerun-watchdog.out); manifest + D-011 addendum 11 committed and pushed. Expected harvest ≈ 00:54 EDT. Next: L6 clean-clone gate on the final SHA; finishing menu.
2026-09-20 17:59 EDT: L6 CLEAN-CLONE GATE at 6065e4e (scratchpad l6-L3-6065e4e.txt): make env 2.7 s; make test 603 passed / 60.8 s warm (128 s cold); make tables diff-clean; make figures OK; make check OK on the five live manifests, failing only on results/gate1_preflight (partial harvest) and results/gate1_preflight_rerun (launch-only) until the re-run's harvest; tree clean; forbidden words 0 over the 57 changed files and 39 commit bodies. GATE PASS (conditional on the harvest, as L2's was). Lane = 39 commits 7f3e546..6065e4e, +22,823 / −224. STOPPED AT THE FINISHING MENU — the merge into main waits for the re-run's harvest (CI red on the launch-only manifest until then) and for the owner's 'merge L3 when green'.
