name: think_c0.5
legacy_name: "think-c0.5"
kind: press
chunkable: false
doc: >-
  ThinK: channel-wise low-rank pruning of the KEY cache at compression ratio 0.5. Not a
  ScorerPress, so it runs single-shot. Ablation only -- ThinK's paper composes it with
  SnapKV/H2O (`think_c0.5_snapkv_k0.15`).
press:
  ratio: 0.5
