# L2 lane ledger — copied verbatim from the lane SDD workspace (final, at the finishing menu)

Source: `.claude/worktrees/L2-generator-v2/.superpowers/sdd/2026-09-11-L2-generator-v2-and-baselines/progress.md`, copied 2026-09-19 at 9ac5eef. Pre-flight drift table + rulings PR-L2-1–23 and R-L2-1–10 with every review verdict; the whole-branch review's deferred-list triage is in the same directory's final-review-deferred.md (summarised in STATE).

---

# SDD ledger — plan: docs/plan/plans/2026-09-11-L2-generator-v2-and-baselines.md

Lane lane/L2-generator-v2 in .claude/worktrees/L2-generator-v2, off main @ 1b57656 (2026-09-19).
Spec: docs/plan/lanes/L2_generator_v2_and_baselines.md (+ ICML2027_PLAN §2, CODE_AUDIT Part B, GATES §G2).
Baseline in the worktree: make test 469 passed / 87 s (the suite sits 3 s under the 90 s gate — watch it).
Task numbering (owner's kickoff, not the plan's): T1 filler-realism pod · T2 generator v2 (plan T3) ·
T3 faithful KIVI (plan T2) · T4 svd_oracle docs (plan T1) · T5 presses + baselines table (plan T4) ·
T6 ss2 pods + prereg (plan T5a) · T7 OjaKV (plan T6) · T8 smoke pod + prereg (plan T5b).

## Pre-flight drift table (plan written 2026-09-11, before L0 Phase B / L1 landed)

| # | plan says | main @ 1b57656 has | ruling |
|---|---|---|---|
| 1 | Global: "rebase onto week7"; lane "already exists with two Day-1 commits"; `scripts/pod/w21.sh` | week7 retired; lane merged + deleted; w21.sh deleted (D-016) | PR-L2-1: fresh branch from main; Task 1 re-expresses the diagnostic as configs/pods YAML + `pod.py launch` |
| 2 | T3: `TrialRecord += code_family` | `TrialRecord` already carries generator/haystack_id/depth/code_family/prompt_sha256 (records.py:93); runner fills them from `meta` (runner.py:287) | no-op; BUT the `[trial]` log line prints none of them (TRIAL_RE records.py:38) so a HARVESTED pod has them None → PR-L2-2 |
| 3 | T3: `prompt_sha256 = sha256(prefill.tobytes()+b"\|"+query.tobytes())` | `ruler.prompt_sha256(*ids)` exists (int64 bytes, no separator), used by ruler + official_ruler | PR-L2-3: reuse `ruler.prompt_sha256`; the plan's formula is drift |
| 4 | T3: `code_family` = "numbers"/"words" | `ruler.code_family(sub, trial)` already returns the NEEDLE LABEL for v1 rows | PR-L2-4: v2 writes the family name into the same record field; the `generator` column separates the two meanings; v1 untouched |
| 5 | T3: `TaskCfg += haystacks, code_families, design` | `config_hash` hashes the RESOLVED structured schema; three LIVE manifests (results/hygiene_table4_*) pin their hashes and `make check` compares → any new TaskCfg field fails `make check` | PR-L2-5: v2 fields live on `TaskV2Cfg(TaskCfg)`, chosen by `load_task` from the raw YAML's `generator: v2`; existing hashes byte-identical; a test pins one Table-4 hash |
| 6 | T3 tests: "the suite's tiny tokenizer fixture `tok`" | none exists: tests use `_WordTok` (test_w10_ruler_filler.py, `__call__` only) and `_FakeTok` (test_ruler_template_tail.py:62, chat template + decode, no `__call__`, grow-on-demand ids) | PR-L2-6: one conftest `tok` fixture = whitespace tokenizer with `__call__`/`apply_chat_template`/`decode`, ids = crc32(word) (stable across processes so the golden hashes reproduce); the two private stubs stay |
| 7 | T3 test `design_cell` → `(haystack_idx, depth, code_family)` with 24 DISTINCT cells from design {2,3,4} | 2×3×2 families = 12 distinct triples; `len(set(cells)) == 24` is impossible as written | PR-L2-7: `design_cell` returns `(haystack_idx, depth, code_family, code_idx)`; `codes: 4` = code draws per (haystack, depth), family = `code_families[code_idx % len]`; product 2×3×4 = 24 |
| 8 | T3: 3 depths "chosen evenly from the 6-point grid" = {0.05, 0.4, 0.95} | grid {0.05,0.2,0.4,0.6,0.8,0.95}; even pick = round(i·5/(d−1)) → idx 0,2,5 | matches; pin the triple in the test |
| 9 | T3: `scripts/pod.py prepare` materializes haystacks; boot.sh never calls it | boot.sh runs `pod.py run` only | PR-L2-8: `run` materializes any missing `data/haystacks/<src>.jsonl` a v2 task names (same function `prepare` exposes) and records the SHAs in `manifest.dataset_sha256`; boot.sh unchanged |
| 10 | T3: `essays` = "RULER Paul Graham essays JSON at a pinned URL" | no such JSON in NVIDIA/RULER (their script scrapes paulgraham.com); official_ruler.py expects RULER's prepare.py output (not run by boot.sh — pre-existing gap, L6 note) | PR-L2-9: `essays` = a Hugging Face Paul-Graham-essays dataset pinned by revision (implementer looks it up; recorded, never guessed); sources needing `trust_remote_code` are stated in `haystacks.py` and the smoke pod is the GPU validation |
| 11 | T2: `quantize_after_prefill(..., chunk=4096)` quantizes in chunk slices; test asserts chunk 64 == chunk 256 | one `_quantized_keys` object per layer; merging chunk stores needs a container the mixin does not have; memory argument is void (the fp16 slab already sits in the DynamicCache; quantization is per layer) | PR-L2-10: no `chunk` parameter — one quantize call per layer over `[0, T − T mod R)`; the chunk-equality test becomes "post-hoc == upstream `update` on the same slab" (identical per-channel scales). Cost if wrong: ~40 lines of chunk merge if a 64K per-layer quantize OOMs on the smoke pod |
| 12 | T2: `KINDS` dispatch table in `retrieve` | `retrieve` is an if/elif over `arm["kind"]` (ruler.py:262); `build_arm` raises NotImplementedError("L2") for `quant_faithful` (frontier.py:232) | PR-L2-11: add the `quant_faithful` branch + the `build_arm` kind; converting to a table is optional (ponytail: only if it shortens the diff) |
| 13 | T4 test: `make_press(load_arm("snapkv_k0.15"))`; `PyramidKVPress` "if 0.5.1 ships it" | kvpress 0.5.1 ships `PyramidKVPress(compression_ratio, window_size=64, kernel_size=5, beta=20)`; `_evict_factory` picks SnapKV/EA by the config NAME prefix (frontier.py:261); existing YAMLs `snapkv_k0.1`, `ea_k0.1/0.25/0.5`, `think_c0.5` use `press: {keep}` / `{ratio}` | PR-L2-12: `press:` gains an explicit `family:` key (snapkv \| pyramidkv \| expected_attention \| think \| think_snapkv); `_evict_factory`/`_press` dispatch on it with the name-prefix fallback kept for the archived arms; new files `snapkv_k0.10/0.15/0.25`, `pyramidkv_k0.10/0.15/0.25`, `ea_k0.10/0.15`; `ea_k0.1` and `ea_k0.25` KEEP their stems (config parity golden + w11 pod configs name them) — the plan's rename to `ea_k0.10` is dropped (legacy `ea-k0.25` rows key the archive) |
| 14 | T4: baselines table "Wilson from hits/n" for ShadowKV (`w15-confirm`) and `ea-k0.25` (`w11-goalA-ruler`) | both archives carry `hits: null, n: null` (pre-Week-18 rows); ShadowKV's key is `shadow-r64` | PR-L2-13: point estimates as archived, `n` shown as "—" where unrecorded, no Wilson invented; rows keyed by the legacy names the archive carries; `paper-v1-tables.md` golden block appended (glob order: `table_baselines.md` sorts after `table8.md`) |
| 15 | T1 (plan) test: `git grep -i palu` hits only 3 files | hits: accounting.palu_footprint (+6 tests), frontier press_type "palu"/`palu_rank_ratio`/`palu_group`, tables.py legacy row keys, golden legacy_arm_kwargs.json, test_svd_oracle test names | PR-L2-14: rename the code identifiers (`palu_footprint` → `lowrank_footprint`, press_type/keys → `svd_oracle`/`oracle_*`, test names); the ARCHIVE keys stay (`legacy_name`, tables.py v1 row map, golden JSON key — edited by hand only for the renamed inner keys, documented as D-014 did). Allowlist = oracle module, its YAML, tables.py, the golden JSON, the test. G2 line 5 is met as amended: hits = docstring + archive keys + DECISIONS |
| 16 | T5 (plan): ss2 pod = "Mistral-7B-v0.3 and Qwen2.5-7B at 16K … all three families at 32K" in ONE `ss2_families.yaml` | `PodCfg` is one model per pod | PR-L2-15: three pods `ss2_families_{mistral,qwen}` (ruler_inhouse_16k + _32k) and `ss2_families_llama` (32k only; 16K exists in the w19 archive), one prereg; arm order cheap-first; no `full` needed (the contrast is paired r64 vs kivi2_faithful on identical prompts) |
| 17 | T5 (plan): ss2 runs on the cycled filler ("same needles as a1") | the filler-realism reading may RETIRE the cycled filler before ss2 launches | PR-L2-16: the ss2 prereg states the conditional: launch only after D-005 closes; if the cycled filler is retired, an amendment moves the tasks to `ruler_v2_*` before launch |
| 18 | T6 (plan): boot.sh `pip install git+<repo>@<commit>` guarded by POD naming an ojakv arm | boot.sh has no per-pod pip hook; `POD` is the pod name, not an arm list | PR-L2-17: the guard reads the pod YAML's arm list (`python -c` over `load_pod`) — or simpler, boot.sh installs OjaKV when `configs/pods/$POD.yaml` contains `ojakv` (grep) |
| 19 | `_footprint` bills the LIVE tracked rank (D-015) | true (frontier.py:326); `quant_faithful` branch missing | T3 adds it exactly as the plan says |
| 20 | `POST_V1` allowlist | tests/test_config_parity.py:50 — every new arm stem must be added | every task adding arms adds its stems |
| 21 | `pod.py launch/harvest/check` | launch refuses a dirty tree / unpushed SHA / prereg not a strict ancestor; harvest rebuilds env.txt from the ENV block; the non-shrink guard is trials-only (pod.py:481) | Task 1 owner: `--max-hours` + boot.sh `timeout` + self-destruct; the non-shrink guard generalization goes to the task that next touches `harvest` (else L6) |
| 22 | `[stage]` prints (harness follow-up) | runner prints none | Task 2 (it touches runner.py) adds one-line `[stage]` prints at model/corpus/haystack load |
| 23 | watchdog BUDGET_ITERS expiry | ends the loop WITHOUT destroying (watchdog.sh:70) | Task 1 (touches the watchdog for `caffeinate -s`): expiry destroys every instance in pods.txt |
| 24 | prereg/filler_realism.md names `w18_boot.sh`/`w21.sh`, `meta-llama/Llama-3.1-8B-Instruct`, w19 line files, "SKIP … dropped", $0.60–1.10/h, credit $23.99 | boot.sh + pod.py path; the repo runs `unsloth/Meta-Llama-3.1-8B-Instruct` everywhere (the reference rows too); records are trials.jsonl; a raised trial is an `error` row (R29 fails the pod); measured rates $0.40–0.74/h (D-011); credit $97.75 | Task 1 amends (append-only, dated) — see PR-L2-18..21 |

### Per-task self-consistency
- T1 (filler pod): `ruler_inhouse_16k_wikitext` already encodes the prereg's task line (filler wikitext = WikiText-2 test via `load_corpus_sentences`, four tasks, 6×2, chunk 4096, no depths); the five arms exist as YAMLs whose `legacy_name`s are the prereg's row keys. Consistent.
- T2 (gen v2): tests reference `tok`, `load_corpora(..., fixture=FIX)`, `make_trial_v1` moved from ruler — consistent after PR-L2-6/7; `test_v1_builder_is_untouched` imports a pin helper that does not exist → the implementer computes the v1 sha pin once with the `tok` fixture (the cycled path is already pinned bit-for-bit by test_w10_ruler_filler).
- T3 (KIVI): `acc.quant_footprint(t, n, *, nbits, group, residual_length, scale_words)` matches the plan's call. `make_kivi` = `make_quant_cache(scheme="kivi", group=32, residual=128)`. Consistent after PR-L2-10.
- T5 (presses): test `[SnapKVPress, ThinKPress]` order for the composed press — kvpress `ComposedPress(presses: list)`; paper order SnapKV then ThinK. Consistent.
- Shared files across tasks: frontier.py (T3 `quant_faithful` footprint; T5 `_press` family dispatch) — sequential, no conflict; ruler.py (T2 moves the v1 builder to gen.py; T3 adds a retrieve branch) — T2 before T3; config.py (T2 only); pod.py (T1 `--max-hours`; T2 `prepare`); tests/test_config_parity.py `POST_V1` (T3, T5, T7) — append-only.

## Pre-flight rulings (PR-L2-*)
- PR-L2-1..17 as in the table. Cost if wrong: each is a file-local change (≤ 40 lines) except PR-L2-5 (a subclass; reverting means re-recording three manifests, which is why it is chosen).
- **PR-L2-18** (D-005 design): launch the full five-arm design as pre-registered (option (a)); the decisive-arms-only option (b) saves ≈ 2.4 GPU-h ≈ $1.5 at the measured rates, not worth an arm-set amendment; the two KIVI arms are the descriptive "is the drop gist-specific" baselines the prereg wrote them to be. Cost if wrong: ≈ $1.5.
- **PR-L2-19** (harness-consistency control): a second, cheap pod `filler_realism_cycle` — the r64 arm alone on `ruler_inhouse_16k` (cycled filler, ≈ 2.4 h ≈ $2) — replicates the archived w18-g1 row (1.00/1.00/1.00/0.58) on the L0 harness. Reason: the reference rows came from `w10_ruler.py` on an older transformers; the L0 runner's parity with it rests on CPU bit-identity tests, never on a GPU re-run. Its pre-registered reading: a replication differing by > 0.25 on any task = harness drift, and the diagnostic is inconclusive until it is resolved. Cost if wrong: $2.
- **PR-L2-20** (budget): the prereg's per-sample rates stand (r64 ≈ 2.1 min measured, 3.0 budgeted; D-011's 5.2 min is the r256 rate, no r256 arm here); boot overhead re-budgeted at 60 min (the Table-4 Llama pod's HF download stalled ~1 h); `gpu_budget_h` = the §9 kill bar = 18.3 h (2× the 9.1 h point); `--max-hours` = the same bar enforced on the pod; rates $0.40–0.74/h → $7–14 ceiling, $4–7 expected; credit $97.75.
- **PR-L2-21** (self-destruct): `pod.py launch --max-hours H` (default `gpu_budget_h`) → `-e MAX_HOURS=H`; boot.sh wraps the run in `timeout` (expiry → `RUN_FAILED` marker, exit 124 named) and, after the final marker, sleeps a grace period then destroys itself with the instance-scoped key (`CONTAINER_API_KEY`/`CONTAINER_ID`, verified against vast.ai's docs by the implementer; failure is harmless — the watchdog stays the destroyer). Grace default 2 h (`ponytail:` ceiling: a watchdog asleep longer than the grace loses the rows since its last poll; `caffeinate -s` is the primary fix, this is the cap on idle billing ≈ $1). Watchdog: `BUDGET_ITERS` expiry destroys every instance in pods.txt; run under `caffeinate -s -i`.

## Log
2026-09-19 02:35: worktree created (lane/L2-generator-v2 @ 1b57656); venv + dumps symlinked; editable install re-pointed; .pth unhidden; baseline 469 passed / 87 s.
2026-09-19 02:50: pre-flight table + PR-L2-1..21 written; Task 1 brief written; Task 1 dispatched (opus) from BASE 1b57656.
2026-09-19 03:20: Task 1 implementer DONE_WITH_CONCERNS @ d3858e6 (opus; 82b3a36 L2.1a configs+prereg Amendment 1, d3858e6 L2.1b launch --max-hours + boot.sh timeout/self-destruct + watchdog expiry destroy; make test 474 passed / 76 s; boot.sh 8084 B; vast.ai docs: CONTAINER_API_KEY/CONTAINER_ID). Concerns: bar 18.3 h stated as 2x the original 9.13 h point (1.9x the re-derived 9.7 h) — accepted (ruling: the bar stays 18.3, the prereg states both); a citation nit in A1.4 (addenda numbering) → fix round if the reviewer confirms; watchdog-asleep-past-grace trade-off documented (L6 note). Review package written; task reviewer (opus) dispatched on 1b57656..d3858e6.
2026-09-19 03:40: Task 1 review (opus): APPROVED, spec ✅, no Critical/Important; Minors #1–#7 (A1.4 rate-citation misattribution → appended dated correction; `vastai destroy` output to /dev/null; `nan` passes the max-hours guard; `timeout` returns 137 when KILL was needed → comment clause; `vastai` pip unpinned on the shared line → separate install; `timeout: False` assertion; test placement accepted). Ruling R-L2-1: Minors #1–#6 are taken NOW in one short fix round (fresh sonnet) + scoped re-review before the launch — the pods run from this head and #2/#5 are credential/env hygiene on billed hardware; #7 accepted as is. Cost if wrong: ~15 min.
2026-09-19 04:00: Task 1 fix round 1 DONE @ 4e65d40 (sonnet; 6/6; make test 474 passed; boot.sh 8548 B). Scoped re-review (sonnet) dispatched on d3858e6..4e65d40.
2026-09-19 04:15: Task 1 fix round 1/5 (5 addressed, 1 open — #3: +inf passes 'not (hours > 0)'; commits d3858e6..4e65d40). Fix round 2 (sonnet) dispatched: math.isfinite guard + test.
2026-09-19 04:25: Task 1 fix round 2 DONE @ 56e89ee (sonnet; isfinite guard + inf tests; covering file 44 passed). Scoped re-review (sonnet) dispatched on 4e65d40..56e89ee.
2026-09-19 04:35: Task 1 fix round 2/5 (1 addressed, 0 open; commits 4e65d40..56e89ee). Task 1: complete (commits 1b57656..56e89ee, review clean after two fix rounds). Waiting on the full make test/check on 56e89ee before push + launch.
2026-09-19 04:50: LAUNCHED filler_realism (51553610, offer 50441080, $0.449/h, launch SHA 56e89ee, manifest 870e4cf) and filler_realism_cycle (51553637, offer 51163394, $0.471/h, launch SHA 870e4cf, manifest 74a57ca); watchdogs pids 81336/81466 under caffeinate -s -i; D-005 addendum + STATE committed fbf7476 and pushed; pod monitor running (15-min poll). Task 2 (generator v2) dispatched (opus) from BASE fbf7476.
2026-09-19 05:35: Task 2 implementer DONE_WITH_CONCERNS @ 9bbdcab (opus; e653689 L2.3a, 9bbdcab L2.3b; make test 498 passed / 77 s; check green on the Table-4 manifests; the two in-flight launch manifests fail check until harvest — expected, CI red on the lane until then). Concerns: gen.py 338 lines (197 code); vt mirrors RULER's 'VAR B = VAR A'; vt root takes the trial's family; arxiv = neuralwork/arxiver (CC-BY-NC-SA); pg19's first train docs are the KJV Bible (4.3M chars) + a compilation → seed-0 pg19 haystacks are Bible verses. Ruling R-L2-2: materialize() skips docs > 2,000,000 chars (compilations/scripture at the head of PG-19 train) — goes into the fix round; the fixture golden is unaffected. Task reviewer (opus) dispatched on fbf7476..9bbdcab.
2026-09-19 05:45: POD SIGNAL (51553610, real-text filler): `full` arm complete in ≈ 30 min — single 1.00 / multikey 1.00 / multivalue 0.92 / **vt 0.08** (1/12). The uncompressed ceiling collapses on vt with WikiText-2 filler → per the prereg's own §Arms text no vt drop is attributable to compression; the prereg's "Control considered and skipped" clause pre-committed to buying the cycled-`full` cell in this case. Ruling R-L2-3: the cycle-control instance 51553637 (stuck "loading" 47 min, never ran — the Table-4 v2 image-pull symptom) DESTROYED at 05:40; Task 1b (sonnet) appends prereg Amendment 2 (full joins filler_realism_cycle; vt excluded from the real-text decision rule, family 8 → 6; +30 min, bar 7.0 h) and re-sizes the pod; relaunch after. Cost if wrong: ≈ $0.5. The real-text pod runs on unchanged.
2026-09-19 06:10: Task 1b DONE @ a5cd89c (sonnet; Amendment 2; cycle pod arms [isvd_r64_h256_seed, full], bar 7.0; stale launch manifest removed; 498 passed). Cycle pod RELAUNCHED: 51559661 (offer 49157741, $0.67/h, rel 0.999), launch SHA a5cd89c, manifest a7e407b; watchdog pid 45946 under caffeinate; D-005 addendum 2 committed 072cea9 + pushed; monitor restarted on 51553610 + 51559661. Task 2 review (opus) still running.
2026-09-19 06:30: Task 2 review (opus): APPROVED, spec ✅ on every ruling (5703/5703 archived [trial] lines parse identically; Hub pins resolve; licences match). Important #1 — the brief's own source formula (`source = haystacks[trial % 4]`) confounds code family with the source pair in every 2-haystack design (family × source counts 4/4/2/2 vs 2/2/4/4). Important #2 — pg19's head docs are the KJV Bible + a compilation (implementer concern 5). Minors #3–#11.
Ruling PR-L2-23 (supersedes the source line of PR-L2-7): `source = haystacks[(h + depth_idx + H·(code_idx // n_families)) % len(haystacks)]` — the family never enters the source choice; each (depth, family) sees all four sources in g1/g2; the golden (task-owned, created this task) regenerates. Cost if wrong: a one-line formula + golden regen. Ruling R-L2-2 stands for #2: `materialize` skips docs > 2,000,000 chars. Ruling R-L2-4: Minor #3 (vt root takes the trial's family) KEPT — `code_family` stays truthful per row and the design stays uniform across tasks; stated in the v2 task YAML `doc:` as a deviation from official vt (numbers only); L3's Gate-1 prereg may reverse it. Fix round 1 takes #1, #2, #4 (space requirement parity), #5 (own error), #8 (refuse unknown design keys — a config trust boundary), #10 (comment); accepts #6, #7, #9, #11 as is (deferred minors for the final review: #7 import inversion if `prepare` on a laptop ever hurts).
2026-09-19 07:05: Task 2 fix round 1 DONE @ 8c8553b (opus; 7/7 items; make test 502 passed / 69 s; golden 20/30 entries moved by the source formula; smoke design sources 2/4/4/2 — accepted, harness validation only). Scoped re-review (sonnet) dispatched on 072cea9..8c8553b (the fix commit alone follows the Task 1b commits).
2026-09-19 07:20: Task 2 fix round 1/5 (7 addressed, 0 open; commits 072cea9..8c8553b). Task 2: complete (commits fbf7476..9bbdcab + 8c8553b, review clean). Deferred minors (final review): #6 task YAML loaded twice in load_task; #7 haystacks.py imports gen (torch) — invert if 'prepare' on a laptop hurts; #9 subprocess test for the prepare subparser; #11 vt variable names from one alphabet permutation; _indices computed twice per trial. Pushed. Task 3 (faithful KIVI) dispatched (opus) from BASE 8c8553b.
2026-09-19 07:40: POD SIGNAL (51553610): isvd_r64_h256_seed on real-text filler — niah_single acc 0.25 (n=12) vs the archived cycled row 1.00, with full = 1.00 on the same cell → Δ = 0.75 > 0.25: the decision rule FIRES on the first cell of the family (retirement of the cycled generator from every headline claim), subject to the cycle-control replication on 51559661. Owner pushed a notification.
2026-09-19 08:05: Task 3 implementer DONE_WITH_CONCERNS @ 972d709 (opus; kivi.py 67 lines; 10 tests; make test 511 passed / 70 s). Concerns: (1) kivi2_streaming.yaml NOT relabelled — the live results/filler_realism manifest pins its hash and doc: is in the hash → accepted; harness follow-up for L6: an arm named by a live manifest has a frozen doc: (exclude doc from the hash with a one-time re-hash, or accept the freeze); kivi4_streaming relabelled (no live pin). (2) the plan's outlier test slice included the outlier channel — committed with the non-outlier slice (the plan's evident intent), ratio 0.284 < 0.5. (3) PRE-EXISTING, untouched: run_ppl bills every kind AFTER scoring, so eviction presses' kept fraction in the ppl axis includes the scored window (≈ 3% over-bill at 16K, flattering ours) — L6/final-review item, must be fixed before any Phase-2 ppl table; longbench.generate swallows quant/press_quant into a TypeError. (4) D-006 paragraph in the report → DECISIONS at the next docs commit. Task reviewer (opus) dispatched on 8c8553b..972d709.
2026-09-19 08:30: Task 3 review (opus): APPROVED, spec ✅ (post-hoc state verified against transformers 5.8.0 QuantizedLayer.update; positions/lengths/fold correct; live-manifest condition applied with evidence). No Critical/Important. Task 3: complete (commits 8c8553b..972d709, review clean). Deferred minors (final review — TAKE #1 and #5 in the final fix wave): #1 empty residual as a 4-D (1,H,0,D) tensor vs upstream's 1-D empty (invisible at R=128; one-line fix); #5 no test of the residual fold after post-hoc (make_kivi(residual=4) + 4 decode steps); #2 kivi4 doc wording adapted ('4 bits'); #3 faithful make lambda re-spells _quant_factory; #4 RESIDUAL BILLING CONVENTION differs between the two KIVI arms (streaming billed its configured R=128 fp16 tokens though post-flush it holds 0; faithful billed the actual T mod R = 0 at pod sizes) → CODE_AUDIT/paper caption note; #6 report omitted w19_sysfix_latency naming kivi2_streaming; #7 pre-existing run_ppl post-window billing of eviction presses + longbench TypeError → L6. D-006 to DECISIONS at the next docs commit. Task 4 (svd_oracle rename) dispatched (sonnet) from BASE 972d709.
2026-09-19 08:45: POD SIGNAL (51559661, cycle control): isvd_r64_h256_seed on the CYCLED filler — niah_single acc 1.00 (n=12) = the archived w18-g1 row (1.00). The L0 harness replicates the archive on the first task; the real-text 0.25 is the filler, not harness drift (Amendment 1 A1.3 reading, first task).
2026-09-19 09:05: Task 4 implementer DONE @ 3c08e1a (sonnet; 8 files; make test 512 / 69 s; tables diff-clean). Task reviewer (sonnet — a mechanical rename diff) dispatched on 972d709..3c08e1a.
2026-09-19 09:25: Task 4 review (sonnet): APPROVED, spec ✅ (zero leftover identifiers; golden edit minimal; allowlist exact; no live manifest pins the oracle YAML). Task 4: complete (commits 972d709..3c08e1a, review clean). Deferred minor: lowrank_footprint docstring phrasing. D-004 evidence paragraph → DECISIONS at the next docs commit. Task 5 (presses + baselines table) dispatched (opus) from BASE 3c08e1a.
2026-09-19 09:40: POD SIGNAL (51553610): isvd_r64_h256_seed real-text niah_multikey acc 0.08 (n=12) vs archived cycled 1.00 (full real-text = 1.00) → Δ 0.92. Two of the three rule-bearing tasks now fire.
2026-09-19 10:10: Task 5 implementer DONE @ 364c80c (opus; 22 files; make test 526 passed / 77 s; tables diff-clean; check: no hash failures). Concerns: the brief's 'ratio=1.0 → equals evict_footprint' was wrong (ratio = pruned fraction) → test pins ratio=0.0 (correct reading); eviction kept count now the MEAN over layers (byte-identical for existing arms; required for PyramidKV's per-layer budgets — layer 0 alone would bill 0.29× vs the real 0.150×); pre-existing ppl-axis post-window billing of press arms noted again (archive ea-k0.1 0.131× not 0.100× at 16K) → L6; think_c0.5 doc edit safe (no live manifest names a press arm); venv iCloud .pth twin removed again. Task reviewer (opus) dispatched on 3c08e1a..364c80c.
2026-09-19 10:40: Task 5 review (opus): NEEDS FIXES — Critical #1: the three pyramidkv_k* arms raise on every q>1 forward after prefill (transformers 5.8 builds ONE causal mask from layer 0's key length — `create_causal_mask`/`DynamicLayer.get_mask_sizes` — and PyramidKV's deeper layers hold fewer keys; reproduced on the tiny model: "size of tensor a (70) must match b (454)"), i.e. `ruler._decode(block=True)`, longbench's block decode and `frontier._score_window` all fail → error rows on every pod task; the diff's PyramidKV test stopped at prefill. Minors: #1 a test that runs nothing (rename); #2 table_baselines silently overwrites a duplicate archive row; #3 suite time 77 s (92 s under load). Ruling R-L2-5: fix round 1 (fresh opus) — per-layer-budget presses (`family: pyramidkv`) decode token-by-token (`block=False`, the arm dict carries `per_layer_budget: True` set by `_press`) in ruler + longbench; `run_ppl` REFUSES such an arm with a named ValueError (a 512-token window scored in one forward needs the single mask; per-token scoring is a Phase-2 option) — the smoke pod is retrieval-only; a test drives a PyramidKV arm through `retrieve` with the pyramid active (keep 0.5 on the tiny model: layers keep 448/64) and one pins the ppl refusal; minors #1 and #2 taken; #3 → cleanup lane. Cost if wrong: ~30 lines.
2026-09-19 11:00: Task 5 fix round 1 DONE @ e53feda (opus; Critical + 2 minors; probe now passes; make test 529 / 73 s (wall 79 s); concerns: the mechanism holds under sdpa (the runner pins sdpa) — eager still trips at q=1 (outside the pod path); a composite naming family pyramidkv would block-decode (none exists)). Scoped re-review (sonnet) dispatched on 364c80c..e53feda.
2026-09-19 11:15: POD SIGNAL (51553610): isvd_r64_h256_seed real-text niah_multivalue acc 0.00 recall 0.06 (n=12) vs archived cycled 1.00 (full real-text 0.92) → Δ 1.00. All three rule-bearing tasks fire for the r64 arm: single 0.25 / multikey 0.08 / multivalue 0.00 (vt pending, descriptive).
2026-09-19 11:25: Task 5 fix round 1/5 (3 addressed, 0 open; commits 364c80c..e53feda). Task 5: complete (commits 3c08e1a..364c80c + e53feda, review clean). Deferred: a composite naming family pyramidkv would block-decode (none exists; latent); eager attention at q=1 still trips (runner pins sdpa); suite 73–79 s. Pushed. Task 6 (ss2 pods + prereg) dispatched (opus) from BASE e53feda.
2026-09-19 12:10: Task 6 implementer DONE @ 9df8c6f (opus; 3 pods + prereg + tests; make test 531 / 74 s; no _diag4096 needed: measured 416/928 diag rows per 16K/32K r64 sample on the live pod). Budget asked: 54.6 GPU-h expected / 109 at the 2× bars → $22–40 expected, $44–81 ceiling. Concerns: Holm family 8 at 16K (Mistral+Qwen only) / 12 at 32K; watchdog .raw re-appends the saturated tail every poll (L6 one-liner); 32K single-shot never measured (OOM = error row). Ruling R-L2-6 (for the fix round, after the review): ss2_families_llama ALSO runs ruler_inhouse_16k — the faithful KIVI-2 vs r64 contrast on the paper's headline cell (Llama 16K) is the panel's #1 gap and the archived Llama 16K single-shot cell is the G=64 mixin, not the faithful arm; the 16K family becomes 12 as the lane text says; ≈ +6 GPU-h. Task reviewer (opus) dispatched on e53feda..9df8c6f.
2026-09-19 12:30: POD SIGNAL (51553610): r64 real-text vt 0.00 (descriptive; full 0.08). r64 real-text row complete: 0.25 / 0.08 / 0.00 / 0.00 vs cycled 1.00 / 1.00 / 1.00 / 0.58. Next: the q4 cell.
2026-09-19 13:00: Task 6 review (opus): NEEDS FIXES — Critical #1: §4 branches 2 and 3 overlap on (adj p < 0.05, KIVI-favoured); Important #2: §3/§7(f) predict the guard never fires, but the live filler pod's diag rows show the repair firing in essentially EVERY 64-absorb window at bf16 (fixed_k 100%, fixed_v 99.2%; pre-repair max orth_err_k at the 1e-3 threshold, V max 8.1e-3, never near abort) — the prereg must say so; RELAY to L6/CODE_AUDIT: bug_cache.py's comment "at the defaults the fix never fires and behaviour is bit-identical" is false on GPU/bf16; Important #3: Holm family when a cell is excluded (state removal, m = 11); Important #4: the R-L2-6 pass (Llama 16K) itemised — budget becomes 60.6 h point / 121.2 at the bars ($24–45 / $49–90; the bar total exceeds the $97.2 credit at the top rate → D-003 top-up); Minors #5–#11. Ruling R-L2-7: fix round 1 (fresh opus) takes Critical #1, Important #2–#4 and Minors #5–#11 in ONE in-place edit of the prereg — the file is lane-created, unlaunched and unmerged, so the pre-launch fix round is the in-house precedent (L1 Task 6 fix round); append-only binds from the launch commit. Cost if wrong: an amendment section instead.
2026-09-19 13:40: Task 6 fix round 1 DONE @ ddf3f67 (opus; 11/11 items; every number recomputed; my 'exceeds the credit at the top rate' claim was wrong — 121.2 h × $0.74 = $89.7 < $97.2; the prereg says it exceeds the credit net of the filler pods' bars). Scoped re-review (sonnet) dispatched on 9df8c6f..ddf3f67.
2026-09-19 14:10: Task 6 fix round 1/5 (11 addressed, 0 open; commits 9df8c6f..ddf3f67; the credit sentence substituted with the arithmetically correct one — blessed). Task 6: complete (commits e53feda..ddf3f67, review clean). Pushed. Task 7 (OjaKV) dispatched (opus) from BASE ddf3f67.
2026-09-19 14:30: CYCLE POD DONE (51559661, ALL_DONE 07:35, ≈ 2.75 h, ≈ $1.9): r64 cycled 1.00/1.00/1.00/0.67 (archive 1.00/1.00/1.00/0.58 — every task within 0.25 → harness replicates, A1.3 holds); full cycled 1.00/1.00/1.00/1.00 (vt ceiling 1.00 on cycled vs 0.08 on real text → the vt collapse is the WikiText × v1-vt interaction, A2.6). Watchdog destroyed the instance but its harvest FAILED ('python: command not found' — the watchdog calls bare python; L1's harvests were re-done by hand for other reasons) → harvested by hand at 4ded444 (96 trials, 0 errors, check OK; the pod ran at a5cd89c which includes Task 2's [trial] fields: prompt_sha256 identical across the two arms on all 48 paired keys — the pairing invariant verified on a real pod). Harness follow-up (final fix wave): watchdog uses .venv/bin/python when present. Credit $93.8.
2026-09-19 14:50: Task 7 BLOCKED (opus; no code): OjaKV's released code is a replacement Llama model for transformers 4.44 + flash_attn, unpackaged, unlicensed, needs an unshipped calibration checkpoint, ranks floored at 0.47× stored state. Ruling R-L2-8: no adapter is written in L2; D-017 OPEN recorded (recommendation A: their harness on its own pod at their ratios, Phase 2); G2 line 7 OPEN; the smoke pod runs without an OjaKV arm; Task 8's brief amended accordingly. Docs commit cd658ec (D-004 evidence, D-006, D-017, D-005 addendum 3).
2026-09-19 15:00: Task 8 (smoke pod + prereg) dispatched (opus) from BASE cd658ec.
2026-09-19 15:45: Task 8 implementer DONE @ ee38e9f (opus; 40 arms; 84 GPU-h point / 168 bar → $34–62 / $67–124; halves pre-registered: 28 cheap arms 36.4 h, 12 gist arms 48.8 h; make test 534 / 75 s). Concerns: live-pod rates above the prereg's (r64 2.9–3.7, q4 3.8–4.5 min/sample — inside the 18.3 h bar); watchdog .raw ≈ 33 GB at the bar + BUDGET_ITERS 25 h → launch preconditions (final fix wave: per-poll dedupe + .venv python); retrieve prefills full/press/composite single-shot regardless of chunkable (documented). Task reviewer (opus) dispatched on cd658ec..ee38e9f.
2026-09-19 15:50: POD SIGNAL (51553610): q4 cell (bugSseed-r64-h256-q4) real-text niah_single 0.00 (n=12) vs cycled 1.00 → the second rule-bearing arm fires on its first task.
2026-09-19 16:20: Task 8 review (opus): NEEDS FIXES — Important #1: prereg line ~211 cites a hardware observation that does not exist (the real-text pod runs a pre-L2.3b SHA, no `sha=` on its [trial] lines; kivi2_singleshot has not run) — the evidence wanted IS on disk in the cycle pod (§2a); Important #2: the per-poll burst bound under-counted (≤ 5 completions per 150 s at the 36 s floor → ≤ 2,725 rows, 11× under 30,000 / 1.8× under the 5,000 fallback; decision unchanged). Minors: §1 prefill sentence contradicts §3; .raw growth = upper bound; assert chunk 4096; a line on partial-death mechanics. Ruling: fix round 1 (sonnet) takes all six.
2026-09-19 16:50: Task 8 fix round 1 DONE @ 5786618 (sonnet; 6/6 — the chunk assert already existed; make test 534 / 72 s). Scoped re-review (sonnet) dispatched on ee38e9f..5786618.
2026-09-19 17:15: Task 8 fix round 1/5 (6 addressed, 0 open; ee38e9f..5786618). Task 8: complete (commits cd658ec..ee38e9f + 5786618, review clean). ALL TASKS DONE (T7 ruled, no code). Whole-branch review (opus) + ponytail-review (opus) dispatched in parallel on 1b57656..5786618 (package excludes results/ data, the fixture corpus and the goldens).
2026-09-19 17:50: ponytail-review (opus) on 1b57656..5786618: 25 findings, net −249 possible (ponytail-review.md). Ruling R-L2-9 — TAKE in the final fix wave: frontier L244 (quant_faithful make = _quant_factory), L321 (press_family once); ruler L345 (merge quant/quant_faithful branches), L380 (block var); gen L49 (drop the make_trial_v1 alias — the test pins ruler.build_task directly; PR-L2-22's intent, v1 untouched, holds without an alias nobody calls), L86 (Doc = dict[str, Any]), L105 (lru_cache on load_corpora), L284 (drop n_sentences); haystacks L112 (drop the write-only .sha256 sidecar — the manifest holds the digest); runner L235 ([stage] one-liner); pyramidkv YAML docs deduped; the three ss2 pod docs → 4 lines each (unlaunched; hash moves are free); conftest _Enc → transformers.BatchEncoding; test_gen_v2 L110 (the one-source-per-group property), L345 (in-process prepare); test_kivi_faithful _tiny() → conftest tiny_model IF every assertion stays identical (else keep); test_presses _StubTok + the duplicated PyramidKV prefill block; test_pod_manifest L829 (one parametrized resolve test over the six pods), L847 (table-driven, KEEPING the prereg-design pins: arm order, task list, n, filler, chunkable of the quant arms — echoes of gpu_budget_h/design dropped), L332 (loop over the bad hours); test_tables_baselines in-process. KEEP: accounting's ratio==0 identity branch (the documented identity; 5 lines); presses.py `family:` on the nine new YAMLs (explicit config over name-parsing at a trust boundary; the conflict refusal guards it); test_keep_fraction_maps_to_compression_ratio (plan-mandated, 4 lines). Expected ≈ −200.
2026-09-19 18:30: Whole-branch review (opus) on 1b57656..5786618: WITH FIXES — no Critical; Important #1 haystack digests written into the POD-SIDE manifest never reach a harvested manifest (every harvested manifest has dataset_sha256 {}; the Table-4 corpus digests have the same pre-existing gap) → print `[stage] dataset_sha256 <key> <sha>` and parse it in harvest; #2 watchdog bare `python` (the running watchdog pid 81336 will destroy but not harvest 51553610 → manual harvest) + per-poll .raw dedupe — edit via temp+mv (bash reads the script by offset; pid 81336 holds it open); #3 BUDGET_ITERS default 25 h destroys healthy > 25 h pods (smoke 168 h, ss2 40.4 h) → derive from gpu_budget_h; #4 main goes CI-red on merge until the running pod's harvest lands → sequence the merge after the harvest; #5 suite 86.6 s pytest / 95 s wall at the 90 s gate → L6 trim (the ponytail cuts remove two subprocess tests). Minors #1 kivi2_streaming YAML `#` comment label (comments are outside the hash), #2 = T3 deferred #1/#5, #3–#9 accepted/L6. G2 map: lines 2, 3, 8 met; 5 met as amended; 1, 4, 6 open pending harvests/launches; 7 open (D-017). Verification: check OK on the 4 harvested manifests, tables diff-clean, figures OK, ruff/mypy clean, forbidden 0, TRIAL_RE 5532/5532 archived lines, synthetic 2400-row smoke harvest → check OK. Ruling R-L2-10: ONE fix dispatch = Important #1–#3 + Minor #1–#2 + the ponytail cuts of R-L2-9, in three commits; then one scoped re-review; the merge waits for the 51553610 harvest (finishing menu presented with that sequencing).
2026-09-19 19:30: Final fix wave DONE @ 423ecd0 (opus; c437e75 A harness, dd79c38 B kivi+label, 423ecd0 C ponytail −190; 538 passed / 75.6 s pytest / 82 s wall; watchdog edited via mv, pid 81336 keeps the old inode → 51553610 harvest by hand). Scoped re-review (opus) dispatched on 5786618..423ecd0.
2026-09-19 20:15: Final re-review (opus): 5/5 ADDRESSED, no new breakage; BUDGET_ITERS traced (7.0→600, 18.3→600, 40.4→1018, 168→4081; missing/0.0→600); watchdog inode evidence confirmed. Final review CLEAN at 423ecd0; pushed. L6 clean-clone gate running on 423ecd0 (make check expected to fail on results/filler_realism until its harvest — re-run after).
2026-09-19 20:45: L6 gate at 423ecd0: env/test(538, 112 s cold, 77.8 s warm)/tables/figures/tree/forbidden all PASS; check fails only on results/filler_realism (launch-only) → re-run after harvest. STATE entry committed d650017. Waiting on 51553610 (q4 vt + two KIVI-2 arms ≈ 2.5 h).
2026-09-19 21:10: POD SIGNAL (51553610): q4 row complete 0.00/0.00/0.00/0.00 (cycled 1.00/1.00/1.00/0.50). KIVI-2 chunked on real text: single 1.00 (cycled 1.00), multikey 0.50 (cycled 0.67, Δ 0.17 < 0.25) — the quantization baseline holds where the gist arms collapse → the drop is gist-specific, not benchmark-wide. multivalue/vt + the single-shot arm remain (≈ 1.5 h).
2026-09-19 21:40: REAL-TEXT POD DONE (51553610, ALL_DONE 10:32 EDT, 6.8 h ≈ $3.1; watchdog destroyed, harvest failed on bare python as expected → harvested by hand: 240 trials, 0 errors, check OK; commit 97fd76e). READING (Fisher + Holm over 6): r64 0.25/0.08/0.00 (vt 0.00) vs 1.00/1.00/1.00/0.58 — Holm-adjusted p 3.4e-4/1.9e-5/4.4e-6; q4 0/0/0/0 vs 1/1/1/0.50 — 4.4e-6 each; KIVI-2 chunked 1.00/0.50/0.50/0.08 (no drop > 0.25 on the rule tasks); single-shot 1.00/0.83/0.42/0.17 (multivalue +0.42, p 0.089); full 1.00/1.00/0.92/0.08. D-005 CLOSED (9ac5eef): cycled filler RETIRED; v1 in-house tables "diagnostic only"; ss2 preregs amended to ruler_v2_* before launch. Pushed. Note: my 'pairing 48 of 48' check on the real-text pod is vacuous (pre-L2.3b SHA → sha None everywhere); the cycle pod is the pairing evidence. L6 gate re-running on 9ac5eef. Total D-005 spend ≈ $5.3; credit $92.5.
--- L6 gate at 9ac5eef
head=9ac5eef docs(plan): D-005 CLOSED — cycled filler retired from headline claims (r64 0.25/0.08/0.00, q4 0/0/0 on real text; KIVI-2 holds; cycle control replicates the archive)
make env rc=0 2s ::  + yarl==1.25.1
make test rc=0 144s :: 538 passed, 15 warnings in 112.92s (0:01:52)
make tables rc=0 1s :: tables: diff-clean vs paper-v1
make figures rc=0 2s :: [wrote docs/paper/figures/coldstart.pdf|png]
make check rc=0 8s :: results/filler_realism: OK (240 trials, 0 errors) results/filler_realism_cycle: OK (96 trials, 0 errors) results/hygiene_table4_llama: OK (0 trials, 0 errors) results/hygiene_table4_qwen_r128: OK (0 trials, 0 errors) results/hygiene_table4_qwen_r256: OK (0 trials, 0 errors)
untracked/modified after targets: 0
forbidden-word grep (src configs scripts docs/paper prereg, text files):
       0
GATE PASS
2026-09-19 22:00: L6 GATE PASS at 9ac5eef (clean clone outside iCloud: env 2 s; test 538 passed (112.9 s cold / 77.8 s warm at 423ecd0); tables diff-clean; figures OK; check OK on all five live manifests; tree clean; forbidden 0). Ledger copied to docs/plan/cleanup/l2-ledger.md; GATES §G2 ticked; STATE done entry; finishing menu presented.
