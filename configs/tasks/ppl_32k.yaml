name: ppl_32k
generator: ppl
ctx: 32768
doc: >-
  Teacher-forced perplexity at 32K: prefill the context with the arm's compression active, then
  score a frozen 512-token continuation window against the compressed cache, averaged over 4
  sampled windows. v1 read WikiText-103 TRAIN (a known defect recorded in CODE_AUDIT). The
  Week-18 G4 pod ran the same protocol at 8 windows instead -- see `ppl_32k_w8`.
n_trials: 1
seeds: [0]
