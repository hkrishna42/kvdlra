name: w18_g3_qwen
model: "Qwen/Qwen2.5-7B-Instruct"
dtype: bfloat16
doc: >-
  Week-18 G3: eviction measured inside the same grid (the 'eviction cannot enter' wording needed
  measured points), multikey included, 16K and 32K. Pre-registered bar: ~$25. v1 had no
  pre-registration: `prereg: null` records that, and it is the one field a re-run of this pod
  must fill before launch (scripts/pod.py check enforces the commit order from L0 on).
prereg: null
arms: ["ea_k0.1", "snapkv_k0.1", "evict_surprise_h256"]
tasks: ["ruler_inhouse_16k", "ruler_inhouse_32k"]
image: "pytorch/pytorch:2.11.0-cuda12.8-cudnn9-runtime"
gpu_budget_h: 0.0
