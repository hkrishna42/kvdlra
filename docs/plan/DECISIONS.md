# DECISIONS — append-only, dated. Gate outcomes carry the evidence path. OPEN = the owner (Hari) decides; the orchestrator records options + a recommendation and continues on everything not blocked. Closing an item = a new dated entry, never an edit.

## 2026-09-11

### D-001 OPEN — Is arXiv v1 live?
- Evidence: commit 9baf571 (2026-09-06, "arXiv package delivered") changed only docs/week19-handover.md; paper/arxiv-v1.tar.gz exists locally (gitignored); a web search for the exact title on 2026-09-11 returns no arXiv listing. → v1 appears assembled but NOT posted.
- Options: (a) not posted → the L7 corrections land in paper/main.tex only, no v2 is needed, and v1 is not posted as-is (it carries the unsupported "tracker load-bearing" sentence and the framing CODE_AUDIT refutes). (b) posted → the L7 diff becomes an arXiv v2 within the week.
- Recommendation: (a). Until you confirm, L7 proceeds under (a) and notes the v2 path in its report.

### D-002 OPEN — Kernel option: (iii) Triton tile-wise reconstruct-inside-attention vs (i) post-RoPE tracking at r=128
- Recommendation (ICML2027_PLAN §2 Gate 3; L4 brief): (iii) primary — it preserves the pre-RoPE design exactly as shipped; (i) as a parallel one-row accuracy experiment (`isvd_postrope_r128`, Llama 16K, n=24). L4 formalises this in docs/adr/0001; the ADR stays OPEN until you accept it.

### D-003 OPEN — GPU split between L3 (Gate 1 v2) and L4 (kernel)
- Recommendation (KICKOFF Part E §5): Gate 1 v2 first — it decides which paper exists; kernel prototypes on a shared card at batch 1. Gate 1 v2 is budgeted ≤ 50 GPU-h; credit is $23.85, so a top-up (~$60–90 at A100 rates) is required before the G1 v2 launch commit.

### D-004 OPEN — `palu-r0.5`: real Palu port vs `svd_oracle` as an explicit static-low-rank upper bound
- Recommendation: rename to `svd_oracle_r0.5` now (a settled fact; L2 §5). Decide on a real-Palu port only if Gate 1 selects Branch A/B — it matters for the Phase-2 tables, not for the gates.

### D-005 OPEN — Launch the filler-realism diagnostic pod (Day-1 milestone; L2 §1)
- Spec: in-house four tasks, Llama-3.1-8B, 16K, n=12, real-text filler; arms = isvd r64-h256 (the paper's r64 configuration), its q4 cell, KIVI-2 streaming, KIVI-2 single-shot, full. Reading pre-registered in prereg/filler_realism.md (committed before the launch commit): a drop > 0.25 on any task for the r64 or q4 arm retires the cycled-filler generator from every headline claim.
- Cost: ~2–4 A100-hours ≈ $3–6 of the $23.85 credit. Spends money → needs your go; the orchestrator does not launch pods.
- Recommendation: go, once prereg/filler_realism.md is committed and L2 confirms `--filler` accepts a real-text source (today `--filler wikitext` draws WikiText-2 test sentences — real text, but not the PG-19 chapter the brief names).

### D-001 evidence addendum (2026-09-11, orchestrator; still OPEN for the owner)
- L7 searched paper/ for an arXiv ID, abs URL, report number or submission receipt: none; every `arXiv:` string is a citation in refs.bib; main.tex's "arXiv v1 preprint" header is intent, not a record. Combined with 9baf571 (tarball assembled) and the empty title search, the recommendation stands: (a) not posted, no v2.

### D-005 addendum (2026-09-11, orchestrator; OPEN) — cost revised, reduced-arm option
- L2prep's estimate from harvested per-trial timings: 5 arms × 4 tasks × n=12 = 9.1 h base, 18.3 h at the 2× safety factor → **$11–20** (credit $23.85). The earlier "$3–6" omitted tasks-per-cell.
- Option (a) full design, 5 arms: $11–20, most of the credit. Option (b) decisive arms only — r64 config, q4 cell, full (the rule is written on r64/q4; KIVI-2 chunked is already known to be worse under chunked prefill and its single-shot variant is a Gate-2 question, not a filler question): ≈ 5.3 h base / 10.6 h at 2× → **$6–12**.
- Recommendation: (b) now; the two KIVI arms join the ss2_families pod (L2 §4) where they belong. Prereg file to be amended to (b) before launch if you choose it; the pod-name/watchdog fixes L2prep listed are done at launch time by L2.

### D-002 evidence addendum (2026-09-11, orchestrator; OPEN — ADR ready for acceptance)
- ADR 0001 drafted (lane/L4-kernel @ e432df3): (iii) primary, (i) parallel accuracy row `isvd_postrope_r128`, (ii) rejected (GQA: 4× the key-side FLOPs of (iii); the pair factorization is per query head, reconstruction per KV head). Cost model regenerable from docs/adr/0001-cost-model.py; Week-3 gate reachable in principle (resident KV 0.60 GB vs full 4.29 GB at 32K; roofline 10.7 ms vs measured 188 ms reconstruct).
- Three concerns the owner should weigh before accepting: (1) `BugStreamingLayer.lazy_initialization` raises above batch 1 — Gate 3's batch ≥ 4 criterion needs a batched cache first (scope for L4 Week 2–3); (2) weights + full KV at 64K batch 4 = 50.4 GB does not fit an A100-40GB — that contrast moves to H100 or to "max batch that fits"; (3) the traffic model under-predicts the measured reconstruct path by 6.6–16× (full KV by 2.1–2.3×), so absolute kernel timings are not predicted, only the floors.
- Recommendation: ACCEPT (iii) primary + (i) row, with concern (1) added to the L4 milestones as the first Week-2 task.

## 2026-09-13

### D-007 OPEN — The repository lives inside an iCloud-synced folder (~/Desktop)
- Evidence: `brctl status` shows com.apple.CloudDocs syncing Desktop; during L0's test runs macOS "conflicted copy" duplicates appeared (`scripts/tables 2.py`, `tests/test_records 2.py`, plus `.mypy_cache`/`__pycache__` twins). They are untracked and were deleted, but a sync race can also resurrect stale file contents.
- Options: (a) move the clone out of iCloud (e.g. `~/src/kv-dlra`) and re-create the worktrees — the vast.ai pods clone from GitHub, so nothing else depends on the path; (b) exclude via a `.nosync` folder name (breaks every path in docs/scripts); (c) keep and mitigate (implementers commit by explicit path; orchestrator deletes `* 2.*` files at every checkpoint).
- Recommendation: (a) at the next quiet point (after L0 merges). Until then (c) is in force.

## 2026-09-14

### D-008 CLOSED (orchestrator ruling R13, recorded for the owner) — G0 "Dockerfile + uv.lock"
- uv.lock stays gitignored (the repo marks it platform-specific); reproducibility = pyproject exact pins + Dockerfile + `scripts/pod.py check` diffing env.txt against the pins. GATES.md G0 amended on the lane (96e1398, merged). Reopen if you want a committed lock for the pod image.

### D-009 CLOSED (orchestrator rulings during L0 Phase B, recorded for the owner — reopen any line)
- R34: `src/kvdlra/baselines/turbo_press.py` (+ its test) KEPT although it was a deletion candidate — Phase 2's composition Pareto (ICML plan §3.4) needs TurboQuant as a pure quantizer; L2 adds its arm YAML. Listed under "Kept despite being a candidate" in deletions.md.
- R35: `hydra-core` dropped; `omegaconf` is the direct pin (configs are plain structured configs, no hydra CLI). Two zero-caller accounting helpers and `seed_everything`'s never-passed parameter deleted.
- R29: `scripts/pod.py check` FAILS whenever a pod's manifest counts errors > 0 (`CHECK FAIL errors: N trials raised`); no tolerance knob. An errored pod becomes citable only by an L6 ruling per case. Cost if wrong: one flag to add.
- R36/R37: tests/ stays outside the forbidden-word scope (tests pin the words); skipped `[diag]` lines are counted into the manifest (`diag_skipped`) and fail `check` when > 0 — L1's diagnostics must emit rows under 400 chars or reuse the `part=` splitter.
- R38: `kvdlra.eval.persist` stays archive-only (its rows are parsed, not produced) until L4 wires it as a runner generator.
- R39: the watchdog's ROWS filter keeps `[error]` lines so a watchdog harvest counts the same errors a full-log harvest does.

### D-010 OPEN — the gitignored litter was parked, not deleted
- The L0 plan said `rm`. I moved the files instead to `~/Desktop/kv-dlra-litter-2026-09-14/` (2.7 MB): handover.md, explanation_week_{1_2,3_4,5_6}.md, next-session-prompt.md, compass_artifact_*.md, dashboards/ (two HTML dashboards), results/gpu_logs/ (30 raw `vastai logs` tails, Weeks 11–12 + the 1B/8B runs — paper-source-map.md cites none of them, so they exist nowhere else), results/scratch/ (9 smoke-run JSONs). Reason: irreversible deletion of files that live only on this disk is the owner's call; the repo outcome is identical (ignore lines dropped, tree clean, nothing under src/scripts/Makefile/configs references the paths).
- Decision: `rm -rf ~/Desktop/kv-dlra-litter-2026-09-14` when satisfied, or keep it outside the repo. Nothing depends on it.

### D-007 addendum (2026-09-14) — L0 has merged; this is the quiet point
- Only the main checkout remains as a worktree (the L0 one is removed); lane branches L2/L4/L7 have no worktrees. Moving the clone out of iCloud is now `mv` + re-creating worktrees on demand. Recommendation unchanged: (a).

### D-007 addendum 2 (2026-09-14) — iCloud also breaks the venv's `.pth` files
- Finding: every dot-prefixed path under the clone carries the macOS `hidden` flag (UF_HIDDEN; 67 in the repo, 65,660 under `.venv/`) — iCloud Desktop sync sets it. Python 3.12.13's `site` skips hidden `.pth` files ("Skipping hidden .pth file" under `python -v`), so the editable install `_editable_impl_kvdlra.pth` and `_virtualenv.pth` were silently ignored: `import kvdlra` failed from any directory outside the repo. Tests, `make tables` and the pod scripts never noticed because `scripts/_paths.py` and the test conftest prepend `src/` themselves.
- Fix applied locally (not a repo change): `chflags nohidden .venv/lib/python3.12/site-packages/*.pth` → import works. iCloud may re-flag files written later (a future `uv pip install -e` re-creates the `.pth`), so this is a symptom patch. The scratchpad clone used for the L6 gate (outside iCloud) never had the problem.
- Recommendation strengthened: (a) move the clone out of iCloud. No Makefile workaround is added — a macOS-only `chflags` in `make env` would paper over the sync hazard the owner is deciding on.

## 2026-09-17

### D-011 OPENED and AUTHORIZED — Table-4 re-run pods (L1 Task 6: `hygiene_table4_qwen`, `hygiene_table4_llama`)
- Owner, 2026-09-17: "Credit is topped up so you can go ahead and launch pod if needed you have all the permissions to do so." Pod launches inside the approved lanes are authorized; each launch is still recorded here with pod name, prereg SHA, launch SHA, budget, and the harvest outcome.
- Plan for L1: launch the two Table-4 pods (≈ 6 GPU-h total, ≈ $5–8) only after Task 6 commits `prereg/hygiene_table4.md` AND the lane passes its whole-branch review + fix round + L6 clean-clone gate, from the pushed lane head (`scripts/pod.py launch` refuses an unpushed SHA and a prereg that is not a strict ancestor). The merge of L1 into week7 remains a separate decision (stops at the finishing menu unless the owner says "merge L1 when green").
- Not launched under this authorization without a further plan step: the 8B KV-dump pod for the Week-3 rank sweep (8B dumps cannot come back through the log channel; the study must run on the pod and emit rows — a design step for L1 Task 4b's report / L3), and the L2 filler diagnostic (D-005; L2's Task 1, after L1 merges and the lane rebases).

## 2026-09-18

### D-012 CLOSED (orchestrator ruling, recorded for the owner) — the orthonormality guard: what was measured, what ships
- The L1 plan's synthetic ratchet (n=512, cap 256, block 16, rank-40 signal + 4 outlier channels ×1e3, bf16-rounded) does NOT ratchet on the shipped incremental-SVD step: max ‖UᵀU−I‖_F = 6.6e-4–7.3e-4 over 1400 blocks (seeds 0–2; growth ∝ √t). The pre-Week-7 augmentation (plain residual QR, no re-orthogonalization) reaches 44–45 within 60 blocks. CODE_AUDIT.md:171's `audit_q4c.py` is not in the tree; CODE_AUDIT.md:172 — the ratchet through the production layer on Qwen r256, floor off (6e-4 → 1.5e-3 → 3.7e-3 → 35.1 at 8K/16K/32K/36K tokens) — is the finding that stands. Evidence: docs/plan/cleanup/l1-ledger.md (Task 1 Step 0), tests/test_orth_guard.py (pre-W7 vs shipped contrast; injection; cache tripwire).
- Shipped defaults: repair (thin QR + core re-diagonalization) above `orth_fix_tol = 1e-3`; abort (`OrthonormalityError`, recorded as `error`) above `orth_abort_tol = 1e-1`; periodic QR (`qr_every`) OFF. Spec §1 / ICML 0.1 asked for periodic QR every 64 absorbs by default — refused: it changes `u_k` on every stream and breaks the r64 golden; the periodic form is the Table-4 factor (`_qr64` arms). On the golden run the guard never fires (max 6.0e-5).
- Consequences: the oja/fd swap arms now raise instead of returning a divergent number; spec §4's "the exact block that crashed the swap pod" is not recoverable (the log holds the LAPACK message, no tensor) — the crash class is reproduced synthetically; spec §5's `isvd_r64_h256_seed_oja_tuned.yaml` became `oja_r64_h256_seed.yaml` (schedule plumbed) + `oja_r64_h256_seed_tuned.yaml` (D-014).

### D-013 CLOSED (recorded ruling, reopenable) — perplexity protocol
- `TaskCfg.corpus` (default `wikitext-103`, the archived v1 corpus: no archived number changed; every pod's config hash moved by the schema field only, no live manifest affected). New corpora: `wikitext-103-test` (288,937 Llama-3.2-1B tokens → 14 non-overlapping windows at 16K+2048, 7 at 32K) and `pg19-val` (2,968,224 tokens under the 3M loader cap → 160 / 84). PG-19 validation carries the 32-window claim (`ppl_16k_pg19val`, `ppl_32k_pg19val`); WikiText-103 test runs at 14/7 with the ceiling stated. No overlapping windows (paired-bootstrap independence). A load-time guard refuses more windows than a corpus supplies; `load_pod` refuses two corpora at one context length (a corpus axis in `check`/records is deferred until a two-corpus pod is wanted). `[pplw]` and `ppl=` lines carry ` corpus=` (optional regex groups; archived logs parse with None); `dataset_sha256` = sha256 of the token ids the windows were cut from. `scripts/tables.py ppl` reports bits/token, paired 95% bootstrap CI and TOST(±0.05 bits) vs `full` on per-window differences; Holm over a named family is applied by hand.

### D-014 CLOSED (recorded ruling) — Oja: rank pin fixed, schedule re-tuned, parity golden edited by hand
- `oja_step` pinned its rank at the seed block width (a rank-16 basis billed as 64 in the W20 swap) and its `n_seen` froze at tier saturation; both fixed (rank grows to `rank_cap` via the shared augmentation; `n_seen` = the monotone tokens-seen). Every Oja number changes; the W20 cell was already void (untuned).
- Re-tuned by `kvdlra.eval.recon.tune_oja` on the 1B dumps (doc63 + doc718 held out, r=16, layer 8): the surface falls AWAY from the Week-2 (20.0, 0.03) point; keys (5.0, 0.3), values (5.0, 0.1) (results/recon_1b/provenance.json, tuned at r=16 / layer 8 on doc63 + doc718, reused at every rank); (20, 0.03) scores 0.821 vs 0.405 on keys. `configs/arms/oja_r64_h256_seed_tuned.yaml` carries the key schedule; `oja_r64_h256_seed.yaml` stays the void-W20 arm at (20.0, 0.03); L3's Gate-1 v2 `oja_tuned` arm starts from the tuned file.
- `tests/golden/legacy_arm_kwargs.json`: the eight `tracker: bug` values rewritten to `isvd` and the oja arm's entry gained the schedule knobs (by hand; the generator was deleted in L0; documented in tests/test_config_parity.py).

### D-015 CLOSED (recorded ruling) — memory billing uses the live tracked rank
- `_footprint` bills the mean over all gist layers of the live basis widths (K and V averaged; exact by linearity of every rank term in `stored_state_numel`), not the configured cap. Byte-identical for every floor-off arm at the cap (all archived arms); a fresh floor-on ratio (e.g. `isvd_r256_f0.01`) is NOT comparable to the archived Week-17 row, which billed the cap. Evidence: tests/test_effective_rank_billing.py, tests/test_accounting.py (measured K/V asymmetry 21 vs 23 on a collapsed layer). The retrieval and longbench axes' `ratio=` move likewise for floor-on arms.

### D-011 addendum (2026-09-18, orchestrator) — Table-4 pods LAUNCHED under the owner's authorization
- `hygiene_table4_llama`: vast.ai instance 51394691 (offer 51163391, A100 PCIe 40 GB, $0.40/h), launch SHA ce48b29 (prereg first commit 35a8890, a strict ancestor), manifest commit 17d31a0; `hygiene_table4_qwen`: instance 51401786 (offer 50441077, A100 SXM4 40 GB, $0.40/h), launch SHA 17d31a0, manifest commit 54cf11f. Both from the pushed lane branch `lane/L1-harness-hygiene`; watchdogs running on the owner's machine (harvest + destroy on ALL_DONE; credit floor $6).
- Early signal from the first Llama sample (`isvd_r256_noguard`, 16K): per-layer ‖UᵀU−I‖ up to 1.01 with the guard off (layer 0: 0.15 / 0.21 for K/V; effective rank 128 of 256) — the production-layer ratchet CODE_AUDIT:172 reported on Qwen also shows on Llama at 16K. The tolerance and qr64 arms will show whether repair alone restores the perplexity; outcome recorded here after harvest (G1 line 8).
- Note for L6: `pod.py launch` echoes vast.ai's `instance_api_key` into the orchestrator's console (instance-scoped) — redact it in a later lane.

### D-011 addendum 2 (2026-09-18 04:35, orchestrator) — both Table-4 pods KILLED under the prereg's §9 overrun rule; design re-sized before relaunch
- Measured: 5.2 min per 16K perplexity sample on the r=256 arms (A100 40 GB): 1024 absorbs × 32 layers × 2 streams ≈ 65,000 core SVDs (272×272) per sample at ~4 ms each on the GPU — inherent to the incremental-SVD arms at `absorb_block: 16`, not a defect. The 32-window design therefore costs ≈ 11 GPU-h (Llama, 4 arms) and ≈ 37 GPU-h (Qwen, 11 arms + retrieval), against the pre-registered bar of 2 + 4 h (an unmeasured estimate, flagged as such in the Task-6 report). §9: "a pod still running past its budget is a pod to kill and diagnose" — killed at 2.2 h (llama; ~1 h of it a stalled anonymous Hugging Face download) and at boot (qwen). Spent ≈ $0.8; credit $123.05. The watchdog's 25 h `BUDGET_ITERS` cap ends its loop WITHOUT destroying the instance (status `BUDGET_EXPIRED`) — an L6 hardening item.
- Evidence kept from the killed Llama run (`isvd_r256_noguard`, 10 samples): results/hygiene_table4_llama_killed_51394691/diag.jsonl (320 rows). Per-sample max ‖UᵀU−I‖ over layers: 1.01, 2.88, 1.01, 1.01, 1.01, 1.01, 1.01, 1.01, 2.47, 1.01 — the guard-off r256 basis loses orthonormality (error ≈ 1–3) in at least one layer of every 16K sample on Llama-3.1-8B; CODE_AUDIT:172's Qwen observation generalizes.
- Relaunch plan (amended prereg, committed before the new launch commits): n = 16 windows on `ppl_16k_pg19val_w16` (paired CI / TOST(±0.05 bits) remain decidable at the effect sizes at stake); Qwen split into `hygiene_table4_qwen_r128` and `hygiene_table4_qwen_r256` (each with `full`) so the two groups run in parallel; the retrieval-neutrality secondary (`ruler_inhouse_16k` on the r128 arms) moved to a follow-up pod; decision rule unchanged. Budget ≈ 4.7 h (llama) + ≈ 7 h (qwen_r256) + ≈ 3.5 h (qwen_r128) ≈ 15 GPU-h ≈ $6.5. The owner may order the 32-window run later; the amendment states this.

### D-011 addendum 3 (2026-09-18 05:20, orchestrator) — Table-4 RELAUNCHED under prereg Amendment 1 (16 windows; three pods in parallel)
- `hygiene_table4_llama`: instance 51408895 (offer 50937082, A100 SXM4 40 GB, $0.54/h), launch SHA 95e1280, manifest commit cac2035. `hygiene_table4_qwen_r256`: instance 51408903 (offer 38565864, $0.60/h), launch SHA cac2035, manifest commit a5f02c2. `hygiene_table4_qwen_r128`: instance 51408911 (offer 50937088, $0.54/h), launch SHA a5f02c2, manifest commit 833128f. Prereg first commit 35a8890; Amendment 1 committed at 0d2263b/b4c1373/95e1280 — all strict ancestors of every launch SHA. Expected ≈ 15 GPU-h ≈ $8.5 at these rates; bars 5.0 / 7.0 / 7.0 h.

### D-011 addendum 4 (2026-09-18 05:58, orchestrator) — two hosts never loaded the image; relaunched on other hosts
- Instances 51408895 (llama) and 51408911 (qwen_r128) sat in vast.ai's "loading … Retrying in 2 seconds" state for 60 minutes with no container and no log (host-side image pull failure — the July flakiness again); destroyed. `hygiene_table4_qwen_r256` (51408903) is unaffected and running (GPU ≈ 85%, first sample's 28 diag rows parsed). Relaunched: `hygiene_table4_llama` → instance 51415024 (offer 51341862, A100 SXM4, $0.67/h), `hygiene_table4_qwen_r128` → instance 51415044 (offer 48658876, A100 SXM4, $0.74/h); the cheaper offers had been rented meanwhile. Launch SHAs = the previous pod's manifest commit (see the launch commits); prereg ancestry unchanged.

### D-011 addendum 5 (2026-09-18 08:05, orchestrator) — every guarded arm aborted at its first absorbs; a harness defect, not the ratchet; pods stopped, fix before relaunch
- Llama (51415024, harvested): `isvd_r256_tol` and `isvd_r256_qr64` both raised `OrthonormalityError` at absorb 1 of sample idx 1 — layer 0, V side ‖UᵀU−I‖ = 0.921 with `eff_rank_v` 93 of 128 (K side 2.2e-4). Qwen r256 (51408903): tol/qr64 at absorb 1, error 0.506; Qwen r128 (51415044): tol at absorb 1 (0.506), qr64 at absorb 4 (0.141). Mechanism: the sample's first 128-token block on one stream is rank-deficient (duplicate value vectors at a PG-19 excerpt's start), the GPU SVD of the seeding core returns non-orthonormal null-space columns, and `_guard_orthonormality` checks the abort tolerance BEFORE attempting the repair — so a one-step numerical event that a thin QR fixes trivially failed the arm, and `run_ppl`'s per-arm catch then skipped the arm's remaining windows. The unguarded arms carry the same defect silently (Llama noguard: NLL 2.2–2.8 nats, no divergence; Qwen r256 noguard: NLL ≈ 10, the divergence) and the floor arms drop the garbage directions by construction.
- Both Qwen instances destroyed at 08:05; spent so far ≈ $5.6 (credit $118.26). Llama's valid records (noguard 16 windows, full 16 windows, diag) kept at results/hygiene_table4_llama_v2_aborted_51415024/ (no manifest.json, so `check` ignores it); the Qwen raw logs kept locally.
- Fix (lane commit before relaunch): (1) the guard repairs first (thin QR + core re-diagonalization) whenever the error exceeds `orth_fix_tol` or a repair is forced, then re-measures, and aborts only if the POST-repair error still exceeds `orth_abort_tol` (an unrepairable basis); the pre-repair error still enters the diag row's window max, so the ratchet trace is preserved. (2) `_svd_core` verifies the returned left factor's orthonormality and re-orthonormalizes it with a thin QR when a driver returns garbage null-space columns for a rank-deficient core (read-only check on the default path; the golden never trips). (3) Prereg Amendment 2 records the cause and voids the v2 guarded-arm cells; decision rule and budgets unchanged; relaunch ×3.

### D-011 addendum 6 (2026-09-18 10:30, orchestrator) — third launch under prereg Amendment 2 (the guard repairs before it aborts)
- Fix on the lane: abf603d (repair-then-abort; `_svd_core` left-factor check) + 3f6e006 (`_LEFT_ORTH_TOL` 1e-2 with a headroom pin; NaN-safe thresholds; fatal-row ranks pre-repair) + e482570 (per-stream NaN-safe abort; NaN-sticky window folds); Amendment 2 at 7e60619 + 103b4a9 (the core repair applies to all arms, event-logged; Qwen v2 evidence at results/hygiene_table4_qwen_r{256,128}_v2_aborted_*/). Reviews: whole-branch-style review of Task 7 (one Critical fixed), scoped re-review (the NaN residual fixed). Suite 465 passed / 62 s; guard set 109.
- Launched from the pushed head e482570: `hygiene_table4_qwen_r256` → instance 51438089 (offer 51163383, A100 PCIe, $0.44/h), launch SHA e482570, manifest commit 1873332; `hygiene_table4_llama` → 51438103 (offer 50937082, $0.54/h — the host that stalled at v2; monitored for a stall), launch SHA 1873332, manifest a2906b3; `hygiene_table4_qwen_r128` → 51438116 (offer 50937080, $0.54/h), launch SHA a2906b3, manifest 4a6c33f. Spent so far ≈ $5.7; expected ≈ $8 more.

### D-011 addendum 7 (2026-09-18 10:40, orchestrator) — r256 v3 host failed its weight download; relaunched
- Instance 51438089 (`hygiene_table4_qwen_r256`) died in the Hugging Face weight download with `httpx.ReadError: [SSL: DECRYPTION_FAILED_OR_BAD_RECORD_MAC]` (host network; `===RUN_FAILED` marker, no samples); destroyed. Relaunched on offer 50441073 → instance 51446637 (launch commit on the lane). The llama and r128 pods are unaffected and scoring; both logged the `svd_nonorthonormal` repair event with the exact v2 abort values (0.921 / 0.506), confirming the diagnosis and the fix on the real hardware.

### D-011 addendum 8 (2026-09-18 23:15, orchestrator) — Table-4 HARVESTED: the guard alone removes 98.4% of the Qwen r=256 divergence; the floor closes the rest. §4 outcome 3(a).
- Records (PG-19 validation, 16 non-overlapping windows of 2048 tokens at 16K context; bits/token = mean over windows; Δ paired per window vs `full`, bootstrap 95% CI; TOST ±0.05 bits): results/hygiene_table4_qwen_r256, results/hygiene_table4_qwen_r128, results/hygiene_table4_llama (`scripts/tables.py ppl --pod <pod> --baseline full`).
- **Qwen2.5-7B, r = 256** (`full` 3.3238): `isvd_r256_noguard` 14.2651 (Δ +10.94 [+10.67, +11.25] — the Week-17 divergence, ppl ≈ 1.97e4); `isvd_r256_tol` 3.4943 (Δ +0.171 [+0.151, +0.197]); `isvd_r256_qr64` 3.4972 (Δ +0.173); `isvd_r256_f0.01_tol` 3.3429 (Δ +0.019, TOST equivalent); `isvd_r256_f0.01_qr64` 3.3426 (Δ +0.019, equivalent). Guard-alone vs floor: +0.151 bits [+0.132, +0.178] (every window +0.11…+0.31) — not equivalent. Periodic vs tolerance form: +0.003 [−0.004, +0.011], equivalent (p_tost 4.3e-9).
- **Qwen2.5-7B, r = 128** (`full` 3.3238): `noguard` 4.0119 (Δ +0.69 [+0.30, +1.33] — the r128 cell ALSO degrades without the guard, contrary to §5's prediction; per-layer ‖UᵀU−I‖ reached 40 with the guard off); `tol` 3.4075 (Δ +0.084); `qr64` 3.4087 (Δ +0.085); `f0.01_tol` / `f0.01_qr64` 3.3617 (Δ +0.038, equivalent).
- **Llama-3.1-8B, r = 256** (`full` 3.4003): `noguard` 3.4216 (Δ +0.021), `tol` 3.4200 (Δ +0.020), `qr64` 3.4206 (Δ +0.020) — all TOST-equivalent to `full`; the ratchet is real on Llama too (‖UᵀU−I‖ up to 1.44, effective rank 66 of 256 with the guard off) but harmless to perplexity at 16K.
- Diagnostics: with the tolerance guard the pre-repair error exceeds 1e-3 in EVERY 16K window on every guarded arm (Qwen and Llama), so the repair fires each window; post-repair maxima 0.08–0.33 (Qwen r256 `qr64` once 3.7) — the tripwire repaired, never aborted. The `svd_nonorthonormal` core repair fired once per pod (the first sample's rank-deficient seeding block: 0.921 on Llama, 0.506 on Qwen) and therefore touched every arm including the controls (Amendment 2 A2.5): reported, and it changed one window of the unguarded arms by < 0.03 nats.
- **Decision rule §4 (as amended):** branch 1 fails (guard-alone is 0.151 bits from the floor arm, > 0.05); branch 2 fails (guard-alone is 0.17 bits from `full`, < 1.0); **outcome 3(a): the orthonormality ratchet is the substrate of the divergence — repairing orthonormality alone removes 10.77 of the 10.94 bits — and the singular-value floor is still needed to close the remaining 0.15 bits at r = 256 (and 0.05 at r = 128).** The paper's mechanism section reports both, in that order: ratchet as the mechanism, the floor as the remaining rank-siphoning residual. Holm over the four pre-registered members (A1.5): m1 tol vs noguard p_tost = 1 (not equivalent — the guard matters; the paired difference is −10.77 bits, CI [−11.08, −10.50]); m2 tol vs floor p_tost = 1 (not equivalent — the floor still adds); m3 qr64 vs tol p_tost = 4.3e-9 (equivalent; survives Holm at rank 2 of 4: 4.3e-9 × 3 < 0.05); m4 Llama tol vs noguard p_tost = 3.6e-23 (equivalent; survives at rank 1: × 4). No member's reading changes under Holm.
- Not decided here (deferred by Amendment 1): retrieval neutrality of the guard at r128 (the in-house task was dropped from these pods — a follow-up pod); 32K.
- Cost: v1 $0.8 + v2 $5.6 + v3 $20.4 = **$26.8** against a $6–8 estimate. v3's overrun is ≈ $12 of IDLE billing: this Mac slept from 12:27 to 22:42 (pmset log), the watchdogs (caffeinate -i prevents idle sleep only) stopped polling, and the three pods — finished by ≈ 13:15 / 14:30 / 17:15 — sat idle until the 22:42 wake harvested and destroyed them. Fix for the next launch: run the watchdog under `caffeinate -s` (or on a machine that does not sleep) and give `pod.py launch` a `--max-hours` that boot.sh enforces on the pod (`timeout`) so an unattended pod self-destructs at its budget. Credit: $97.75.
- `check`: all three harvests pass every rule (`make check` rc 0) after L1.8 recovered env.txt from each pod's own log — the watchdog's `sort -u` had scattered the ENV block across the deduped log, not dropped it, so the record is the pod's own, not inferred. Two harness gaps found on the way, for L6: `harvest`'s non-shrink guard covers trials.jsonl only (a re-harvest from a stale log in the same directory silently shrank the Llama pplw/ppl/diag records from 64/4/1537 to 32/2/578 before being redone from the right log; extend the guard to every record file, and keep only the harvested instance's log in a pod directory); the pod's ENV block should print every pinned package (L1.8 added them for future pods; `triton`/`omegaconf` read `unrecorded` on these three).

## 2026-09-19

### D-016 CLOSED (owner instruction) — "Merge everything to the main branch; one branch"
- Merged into week7 with `--no-ff`: lane/L7-paper-correction (5 paper commits; the pending G7 owner review is superseded by this instruction), lane/L4-kernel (ADR 0001, still OPEN as a decision — D-002), lane/L2-generator-v2 (prereg/filler_realism.md + the pre-L0 `w21.sh`). Cleanup commit: `scripts/pod/w21.sh` removed (it sourced the deleted `w18_boot.sh` and called the deleted `w10_ruler.py`; L2 Task 1 re-expresses the diagnostic as a pod YAML + `pod.py launch`; the prereg's one mention of w21 is amended then), five forbidden-vocabulary phrases in the L7 paper/abstract additions reworded per CLAUDE.md (the v1 text had none), CLAUDE.md's working branch set to main. Merged tree: 469 passed, tables diff-clean, `make check` rc 0, ruff clean.
- `main` fast-forwarded to week7 and pushed; branches deleted: week7 (local + origin), week1-scaffold / week2 / week3 on origin (all strict ancestors of main — nothing lost), the three lane branches (local; never pushed). One branch remains: `main`. All future lanes branch from `main`; STATE/DECISIONS live on `main`.

### D-005 addendum (2026-09-19 03:45, orchestrator) — filler-realism diagnostic LAUNCHED under the owner's standing authorization (D-011, 2026-09-17), full five-arm design + a harness-consistency control
- Design ruled (lane L2 pre-flight PR-L2-18/19/20/21, docs/plan/cleanup/l2-ledger.md at merge): the prereg's full five-arm design (option (a); option (b) would have saved ≈ 2.4 GPU-h ≈ $1.5 and an arm-set amendment), plus a second pod `filler_realism_cycle` — the r64 arm alone on the cycled filler (`ruler_inhouse_16k`) — that replicates the archived w18-g1 row (`bugSseed-r64-h256` 1.00/1.00/1.00/0.58, results/paper-v1/w18-llama + w18-g1-llama) on the L0 runner, so that a drop on real text can be attributed to the filler and not to harness drift between `w10_ruler.py` and `pod.py run` (that parity rested on CPU bit-identity tests only). Both under prereg/filler_realism.md Amendment 1 (pod path `configs/pods/filler_realism{,_cycle}.yaml` ↔ the design table row by row; a raised trial = `error` row, R29; budget re-derived at the measured rates — r64 2.1 min/sample measured / 3.0 budgeted, D-011's 5.2 min is the r256 rate; 60-min boot overhead; bars 18.3 h / 6.0 h; $0.40–0.74/h → ceiling ≈ $7–14 + $2–4, expected ≈ $4–7 + $1–2), first committed 82b3a36, A1.7 correction 4e65d40 — strict ancestors of both launch SHAs.
- `filler_realism`: instance 51553610 (offer 50441080, A100 SXM4 40 GB, $0.449/h incl. storage), launch SHA 56e89ee (lane/L2-generator-v2, pushed), `--max-hours` 18.3, manifest commit 870e4cf. `filler_realism_cycle`: instance 51553637 (offer 51163394, A100 PCIe 40 GB, $0.471/h), launch SHA 870e4cf, `--max-hours` 6, manifest commit 74a57ca. Watchdogs on this Mac under `caffeinate -s -i` (PreventSystemSleep asserted); the pods self-destruct 2 h after their final marker (boot.sh, L2.1b/c). Credit before launch $97.67.
- Harness hardening shipped with the launch (L2.1b–d): `pod.py launch --max-hours` (default = `gpu_budget_h`; refuses non-positive/non-finite) → boot.sh `timeout` (RUN_TIMEOUT marker before RUN_FAILED; harvest records `timeout`) + grace-period self-destruct with vast.ai's instance-scoped key (docs.vast.ai/guides/instances/docker-environment); the watchdog's `BUDGET_ITERS` expiry now destroys every instance in pods.txt. Ceiling stated in boot.sh: a watchdog asleep longer than the grace loses the rows since its last poll — `caffeinate -s` is the primary fix.
- Outcome: recorded here after harvest, closing D-005, with the evidence path results/filler_realism/ and results/filler_realism_cycle/.

### D-005 addendum 2 (2026-09-19 06:05, orchestrator) — the `full` ceiling fell on `vt` (0.08) on real text; cycled-`full` control bought (prereg Amendment 2); cycle pod relaunched
- Real-text pod 51553610, `full` arm (all four cells, ≈ 30 min): single 1.00 / multikey 1.00 / multivalue 0.92 / vt 0.08 (1/12). Per the prereg's §Arms text, no vt drop of the r64/q4 arms is attributable to compression; `vt` is excluded from the real-text decision rule and reported descriptively (Holm family 8 → 6); the "Control considered and skipped" clause pre-committed to buying the cycled-`full` cell in this case → prereg/filler_realism.md Amendment 2 (a5cd89c, committed before the relaunch commit). Hypothesis for the harvest (A2.6): WikiText-2's `= = Title = =` heading lines survive the sentence splitter and collide with the v1 `VAR X = ..` chain — a generator finding, not a compression finding.
- `filler_realism_cycle` instance 51553637 (offer 51163394) never left "loading" in 47 min (no container, no log — the Table-4 v2 host symptom); destroyed 05:40, ≈ $0.35 billed. Relaunched with arms `[isvd_r64_h256_seed, full]`, bar 7.0 h: instance 51559661 (offer 49157741, A100 SXM4 40 GB, $0.67/h, reliability 0.999), launch SHA a5cd89c, manifest commit a7e407b, `--max-hours` 7; watchdog restarted under caffeinate -s -i. Credit $97.2.

### D-004 evidence addendum (2026-09-19, orchestrator; D-004 stays OPEN for the real-Palu question)
- Rename complete in code at 3c08e1a (lane L2 Task 4): `accounting.palu_footprint` → `lowrank_footprint` (docstring keeps the arXiv:2407.21118 citation, drops the method name); `frontier._press`/`_footprint`: `press_type` `"palu"` → `"svd_oracle"`, arm-dict keys `palu_rank_ratio`/`palu_group` → `oracle_rank_ratio`/`oracle_group`; test names; the oracle module's docstring states plainly what it is (Eckart–Young upper bound on the sequence's own K/V) and is not (Palu: no weight decomposition, grouped heads, Fisher rank allocation, fine-tuning, no online computation). Archive keys retained deliberately (`legacy_name: "palu-r0.5"`, tables.py's v1 row keys, the golden's top-level key — the literal strings the paper-v1 records carry); `tests/test_palu_rename.py` makes the confinement CI-enforced. G2 line 5 met as amended: `palu` hits = the disclaimer, the archive keys, DECISIONS. Real-Palu port: deferred to Branch A/B (unchanged recommendation).

### D-006 OPEN — KVQuant arm (2026-09-19, from lane L2 Task 3)
- A KVQuant-style pre-RoPE 2/3-bit dense-and-sparse baseline requires (i) pre-RoPE key quantization — the cache must store keys before the rotary embedding and re-apply RoPE at attention time, which the transformers cache/attention path does not expose; (ii) an outlier sparse store (per-channel/per-token outliers in a sparse fp16 side table added back at dequantization); (iii) a dequantization path without their fused kernel. Estimate ≈ 2 engineer-days + a GPU validation cell. Recommendation: defer to Phase 2 unless Gate 2 turns on the 3-bit operating point; the faithful KIVI arm (L2.2, 972d709: G=32, R=128, fp16 single-shot prefill, post-hoc quantization verified against transformers 5.8's QuantizedLayer) is the 2-bit quantization baseline the panel asked for.

### D-017 OPEN — OjaKV cannot run "end to end from the authors' code at matched bytes" (2026-09-19, lane L2 Task 7, no code committed)
- Findings (task-7-report.md in the lane ledger; every item fetched, none guessed): repo https://github.com/zzbright1998/OjaKV @ 182b03434355b479c6b1e8745c495e888514bb2e (2026-04-16, HEAD of main; no releases, no LICENSE); paper arXiv 2509.21623v2 (Findings of ACL 2026). (1) No setup.py/pyproject — `pip install git+…` fails. (2) The method is a replacement Llama MODEL (`LlamaForCausalLM_EigenAttn`: refactored low-rank k/v projections + Oja-updated bases; the cache is a plain DynamicCache of coefficients) — nothing fits `retrieve()`'s `arm["make"]()` without a model swap. (3) Written for transformers 4.44.0 + flash_attn only (imports absent in 5.8.0; no CPU path). (4) Needs a GPU calibration checkpoint (WikiText-2, `error_budget`) the repo does not ship. (5) Its calibration floors ranks at 45 % of d_h → nearest stored-state ratio ≈ 0.47×, three times the 0.15× target; the paper runs 0.8×/0.6× and has no Llama-3.1-8B RULER row. Also: `--snapkv_topk` is dead (top-k fixed at 1024); full-rank keys of every prefill token stay resident beside the low-rank cache (the paper bills analytically); chunked prefill/window scoring impossible.
- Options: (A) their harness on its own pod (their image, calibration at 2–3 error budgets, their RULER driver at 16K/32K, results emitted through the log; billed analytically from the calibrated ranks with the resident caveat; compared at THEIR ratios 0.5–0.8× via generator v2's official semantics) — ≈ 5–10 GPU-h + a separate boot script; (B) an in-repo `Cache` port ("OjaKV-style", any rank incl. 0.15×, deviations documented) — 1–2 engineer-days; (C) cite only.
- Recommendation: (A) in Phase 2 next to the Gate-1 v2 pods, labelled "OjaKV at its published range"; (B) only if a reviewer insists on a 0.15× row, labelled as our port. Our `oja_*` tracker-swap arms stay what they are (Oja subspace tracking in our harness) and are NOT called OjaKV. Until decided: the L2 smoke pod runs without an OjaKV arm; G2 line 7 is OPEN (not met in L2); "version recorded" = this entry.

### D-005 addendum 3 (2026-09-19 14:30, orchestrator) — cycle-control pod DONE; harness replicates the archive; the vt ceiling collapse is filler-specific
- `filler_realism_cycle` (51559661, launch SHA a5cd89c, ALL_DONE 07:35 UTC-4, ≈ 2.75 h ≈ $1.9): `bugSseed-r64-h256` on the cycled filler 1.00 / 1.00 / 1.00 / 0.67 (archive w18-g1 1.00 / 1.00 / 1.00 / 0.58; every task within 0.25 → Amendment 1 A1.3: the L0 harness replicates the archive, harness drift excluded); `full` cycled 1.00 / 1.00 / 1.00 / 1.00 (the ceiling is intact on cycled filler, so the real-text `full` vt 0.08 is the WikiText × v1-vt interaction of Amendment 2 A2.6, not a model limit). Records results/filler_realism_cycle/ (4ded444; 96 trials, 0 errors, `check` OK; the pod ran a post-L2.3b SHA, so every `[trial]` line carried `sha=`: `prompt_sha256` identical across the two arms on all 48 paired keys — the pairing invariant verified on hardware). Watchdog: destroyed the instance on ALL_DONE but its harvest step failed (`python: command not found` — bare `python` on this Mac); harvested by hand; fix queued (`.venv/bin/python` when present).
- Real-text pod 51553610 so far: `full` 1.00 / 1.00 / 0.92 / 0.08; `bugSseed-r64-h256` 0.25 / 0.08 / 0.00 / 0.00 (vt descriptive). q4 and the two KIVI-2 arms pending; D-005 closes at harvest.

### D-005 CLOSED (2026-09-19 10:32 EDT harvest; orchestrator, evidence-bound) — the cycled-filler generator is RETIRED from every headline claim; all v1 in-house retrieval tables are "diagnostic only"
- Evidence: results/filler_realism/ (instance 51553610, launch SHA 56e89ee, ALL_DONE, 240 trials, 0 errors, `check` OK; 6.8 h at $0.449/h ≈ $3.1) and results/filler_realism_cycle/ (51559661, a5cd89c, 96 trials, 0 errors, ≈ $1.9; plus ≈ $0.35 on the destroyed first cycle host). Total ≈ $5.3 against the amended $5–9 expectation. Reading computed by the prereg's rule (point-estimate drops vs the archived cycled rows; Wilson per cell; Fisher exact + Holm over the family of 6 = 2 arms × {single, multikey, multivalue}, vt excluded by Amendment 2):
  - `full` (ceiling, real text): 1.00 / 1.00 / 0.92 / vt 0.08. On cycled filler (cycle pod): 1.00 ×4 → the vt ceiling collapse is WikiText-specific (A2.6), the other three tasks are intact.
  - `bugSseed-r64-h256` (the r64 configuration), real text: **0.25 / 0.08 / 0.00 / 0.00** vs archived cycled 1.00 / 1.00 / 1.00 / 0.58 → drops +0.75 / +0.92 / +1.00 (Fisher p 3.4e-4 / 9.6e-6 / 7.4e-7; Holm-adjusted 3.4e-4 / 1.9e-5 / 4.4e-6). The cycle-control pod reproduced the archived row on this harness (1.00 / 1.00 / 1.00 / 0.67) → the drop is the filler, not harness drift.
  - `bugSseed-r64-h256-q4` (the sub-cliff cell), real text: **0.00 / 0.00 / 0.00 / 0.00** vs 1.00 / 1.00 / 1.00 / 0.50 → drops +1.00 ×3 (Holm-adjusted 4.4e-6 each).
  - `quant-2bit-kivi` (chunked): 1.00 / 0.50 / 0.50 / 0.08 vs 1.00 / 0.67 / 0.42 / 0.67 → no drop > 0.25 on the three rule-bearing tasks (+0.00 / +0.17 / −0.08); `quant-2bit-kivi#chunk0` (single-shot): 1.00 / 0.83 / 0.42 / 0.17 vs 1.00 / 1.00 / 0.83 / 0.67 → +0.00 / +0.17 / +0.42 (p 0.089): the v1 ss2 "single-shot multi-value edge" (0.83) is itself partly a cycled-filler number.
- **Decision (pre-registered): the rule fires on every one of the six cells** — the cycled generator is retired from every headline claim, and every v1 in-house retrieval table (Tables 1, 2, 3, 6, 7 and the Week-9…17 "100/100/100" rows) is marked "diagnostic only" in the paper. The drop is gist-specific: the 2-bit quantization baseline retains its accuracy on the same prompts (pairing invariant: identical `prompt_sha256` across arms verified on the cycle pod's post-L2.3b records). CLAUDE.md's suspicion ("the in-house filler flatters the gist") is confirmed; the Week-20 official-RULER 0.00 on the q4 cell is now explained by the same mechanism.
- Consequences: Gate 1 v2 (L3) and every later retrieval pod run generator v2 (real documents); prereg/ss2_families.md's filler condition (its branch 3) applies — the ss2 pods are amended to `ruler_v2_*` before launch; the paper's retrieval story is rewritten from generator-v2 numbers only (Phase 3). Not decided here: whether the r64 configuration has ANY real-text retrieval niche at higher rank/tier budgets (a Gate-1 v2 question), or on the `words` code family; the generator-v2 vt task on real text (the v1 vt collapsed under WikiText even uncompressed).
- G2 line 1: MET (harvested; this entry).

### D-007 CLOSED (2026-09-19, owner: "Move the clone out of iCloud now") — the clone lives at `~/src/kv-dlra`
- `mv ~/Desktop/kv-dlra ~/src/kv-dlra` with a clean tree, no worktrees, no live watchdogs and no evicted (`.icloud`) placeholders; `.venv` rebuilt at the new path (`make env`, 2 s from uv's cache — the old console-script shebangs pointed at the Desktop path); verified: `make test` 538 passed, `make tables` diff-clean, `make check` OK on all five live manifests, ruff clean, `import kvdlra` works from outside the repo (the `.pth` files are no longer flagged hidden). Nothing in src/scripts/configs references the path; the pods clone from GitHub. Historical docs (KICKOFF_WEEKS0-3.md, the 2026-09-01 reviews) keep the old path as a record; docs/plan/SESSION_PROMPT_L3.md updated. The Claude auto-memory directory was copied to the new project key. `~/Desktop/kv-dlra-litter-2026-09-14` (D-010) is untouched and still the owner's call.

### D-011 addendum 9 (2026-09-20 08:10, orchestrator) — Gate-1 PRE-FLIGHT pod LAUNCHED (`gate1_preflight`) under the standing authorization for pods inside approved lanes
- Pod `gate1_preflight` (configs/pods/gate1_preflight.yaml at 18102a9): Llama-3.1-8B, `ruler_v2_16k` (generator v2, five tasks, 2 × 3 × 2 design, n = 12, one seed, chunked prefill), arms in order `full`, `isvd_r64_h256_seed`, `nogist_h2423`, `frozen_r64_h256_seed`. Pre-registration `prereg/gate1_preflight.md`, first commit 8db2db2, in-place pre-launch revisions 09dbe2f and 27b5767 (two review rounds); its readings §4: (i) completeness, (ii) the `full` ceiling ≥ 0.9 on every task (a task below it is excluded from the Gate-1 primary family by amendment and repaired in the generator), (iii) the `prompt_sha256` pairing invariant, (iv) the measured min/sample per arm from the manifest's `cell_elapsed_s` (L3.1c) against the re-size triggers. The Gate-1 prereg `prereg/gate1_tracker_swap_v2.md` (first commit 4c8b5d7; revisions ce994e5, 27b5767) was committed before this launch, so the pre-flight's rows enter it only as dated amendments (lane ruling R-L3-3).
- Launch: instance 51722149, offer 31632919 (A100 SXM4 40 GB, $0.668/h, reliability 0.999, 4.4 Gbps, Slovenia; the cheaper California PCIe host family that stalled in D-005 addendum 2 avoided), launch SHA 9e77314 (`lane/L3-gate1-tracker-swap-v2`, pushed), `--max-hours` 17 (= `gpu_budget_h`, the prereg §7 bar over the 8.4 h point), watchdog on this Mac under `caffeinate -s -i`; the pod self-destructs 2 h after its final marker. Expected ≈ 8.4 h ≈ $5.6; ceiling ≈ $11.4. Credit before launch $92.52.
- Outcome: recorded here at harvest (results/gate1_preflight/), with the Gate-1 prereg's Amendment 1 following it before any Stage-1 launch commit. Stage 1 itself waits on D-003 (Stage-1 bar 164 GPU-h at 32 perplexity windows, $74–121).

### D-011 addendum 10 (2026-09-20 14:30 EDT, written 16:05 — the 14:30 append was lost to a failed command chain; orchestrator) — pre-flight pod DONE on the instance but the harvest is PARTIAL (97 of 240 trials): the capture failed, not the pod; reading (i) fails; repair pre-registered before a re-run
- What happened: instance 51722149 (launch SHA 9e77314) ran to `===ALL_DONE===` with 0 error rows (status.txt 14:11 EDT; ≈ 7.5 h of compute after a 20-min boot). This Mac entered "Low Power Sleep" at 06:23 EDT on a 1 % battery (pmset log: `Using Batt (Charge:1%)`) and woke from hibernate at 14:09 when AC was attached; `caffeinate -s` holds only on AC power. The watchdog therefore polled nothing between 06:22 and 14:11; on waking it fetched the log (vast.ai returns a ≈ 4 MB tail — 15,519 lines, 15,381 of them `[diag]` rows), harvested what it had and destroyed the instance. Records lost with the instance: 35 isvd trials, all 60 nogist trials, 48 frozen trials. Cost $6.44 (≈ 1.5 h of it idle between ALL_DONE and the 14:11 destroy).
- What survived (results/gate1_preflight/, committed at 8d10483): the complete `full` arm — single 1.00 / multikey 1.00 / multivalue 0.92 / multiquery 0.92 / **vt 0.75 (9/12)**; isvd single 0.83 (10/12), multikey 0.25 (3/12), multivalue 1/1; frozen vt 0.33 (4/12); `cell_elapsed_s` for 8 cells: `full` 3.2–3.6 s/sample (the L2 rate of 0.6 min was 11× too high), isvd 3.27–3.42 min/sample, frozen 1.58 min/sample (vt cell); pairing invariant: 0 disagreeing / 0 null digests over the captured keys.
- Readings (prereg/gate1_preflight.md §4): (i) completeness FAILS (capture); (ii) the `full` ceiling: **vt 0.75 < 0.9 fires** — pre-registered consequence: vt leaves the Gate-1 primary Holm family by amendment (16 → 12) and v2's vt template is compared with official RULER's before any pod runs it again (the misses spread across both code families and depths — a model ceiling rather than a generator defect is the working hypothesis); (iii) passes on the captured keys; (iv) isvd 3.3 min/sample measured (trigger 4.7: no re-size), frozen 1.58 (one cell), nogist `not measured`.
- Repair, pre-registered as Amendment 1 to prereg/gate1_preflight.md (commit 8eb7f56) before the re-run's launch commit: (a) harness — `pod.py run` replays its compact records (`[trial]`, cell-summary, `[pplw]`/`ppl=`, `[error]` and `[stage]` lines) between `===RECORDS_REPLAY_BEGIN/END===` markers before it returns, so the last log fetch always carries the readings; harvest dedupes exact-duplicate lines (049c66b); the launch procedure states "Mac on AC power" and the cause; (b) a re-run pod `gate1_preflight_rerun` (dc3047f) with the three compressed arms only (`isvd_r64_h256_seed`, `nogist_h2423`, `frozen_r64_h256_seed`; the `full` arm's cells are complete and its reading is decided), same task and seed (byte-identical prompts, verifiable by `prompt_sha256` across the two pods), ≈ 6.8 h point at the measured rates, bar 14 h, ≈ $4.6–5.0 expected / $9.5–10.4 ceiling — under D-011's standing authorization and the owner's instruction of 2026-09-20 ("continue with the fix wave and launch the re-run on AC"). Readings (i) and (iv) are read on the re-run; (ii) stands from this pod; (iii) across both pods.

### D-011 addendum 11 (2026-09-20 17:55 EDT, orchestrator) — pre-flight RE-RUN launched (`gate1_preflight_rerun`) on the owner's go-ahead ("You have the permission go ahead"), Mac on AC
- Pod `gate1_preflight_rerun` (configs/pods/gate1_preflight_rerun.yaml at dc3047f): Llama-3.1-8B, `ruler_v2_16k`, arms `isvd_r64_h256_seed`, `nogist_h2423`, `frozen_r64_h256_seed` (the `full` arm's five cells stand from results/gate1_preflight/, reading (ii) decided: vt 0.75); pre-registration `prereg/gate1_preflight.md` Amendment 1 (8eb7f56) + its correction (d4ed366): readings (i) and (iv) on this pod, (iii) across both pods by `prompt_sha256`. Harness on the pod: the end-of-run records replay (049c66b) with the SIGTERM handler that keeps it alive at the `MAX_HOURS` bar (4a5f71e); harvest dedupes exact-duplicate lines.
- Launch: instance 51815080, offer 50895886 (A100 SXM4 40 GB, $0.601/h, reliability 0.998, 5.6 Gbps, UK), launch SHA d4ed366 (`lane/L3-gate1-tracker-swap-v2`, pushed), `--max-hours` 14 (= `gpu_budget_h`, 2× the 6.8 h point at the measured rates), watchdog pid 9161 under `caffeinate -s -i` with the Mac on AC (battery 83 %+, charging) and its log kept outside the tree; the pod self-destructs 2 h after its final marker. Expected ≈ 6.8 h ≈ $4.1; ceiling ≈ $8.4. Credit before launch $86.08.
- Outcome: recorded here at harvest with the evidence path results/gate1_preflight_rerun/, then the Gate-1 prereg's Amendment 1b (the pre-flight rows, the vt exclusion 16 → 12, the §9 re-size from `cell_elapsed_s`) before any Stage-1 launch commit; Stage 1 waits on D-003.

## 2026-09-20 (evening) — owner decisions delegated ("Go ahead finish them") and taken

### D-003 CLOSED (owner, 2026-09-20: "Start it; let me know if you are running low, I'll top up") — Stage 1 launches on the current credit
- Stage 1 (`gate1_v2_stage1_llama` + `_qwen`, 41 h point / 82 h bar each, 82/164 GPU-h, $37–61 expected / $74–121 at the bar) launches after the pre-flight re-run harvests and `prereg/gate1_tracker_swap_v2.md` Amendment 1b is committed, on the credit then available (≈ $82 after the re-run). The orchestrator asks for a top-up when the credit falls under $35 with pods running, or when the next launch's bar exceeds the credit; the watchdog's credit floor ($6) is the hard stop. The ss2 pods (121 GPU-h bar) and the smoke pod (168) wait behind Gate 1's reading.

### D-018 CLOSED (owner, 2026-09-20) — `vt` stays DESCRIPTIVE in Gate 1 as pre-registered
- The pre-flight's `full` ceiling on generator v2's `vt` is 0.75 (9/12; D-011 addendum 10), below the pre-registered 0.9; `vt` leaves the Gate-1 primary Holm family (16 → 12) by Amendment 1b and is reported descriptively. A numbers-only `vt` re-run is NOT planned; if the template comparison against official RULER (task in flight, report to `docs/plan/reports/vt-template-comparison.md`) finds a generator deviation, that becomes a separate decision, not a silent change.

### GATES §G3 line 1 — "≤ 50 GPU-h" retired (owner asked "what can be done"; orchestrator's answer taken)
- The 50 was the plan's sizing estimate (41 GPU-h for Stage 1 at rates 2× lower than L2 measured); the scientific constraint is that the bar is pre-registered and enforced (`--max-hours` = `gpu_budget_h`). GATES.md §G3 line 1 now reads "the pre-registered bar (prereg §9) in the manifest". Nothing else changes.

### D-001 CLOSED (delegated) — arXiv v1 was never posted; no v2
- Evidence unchanged (no ID, no receipt, empty title search; the tarball was assembled locally). The corrected `paper/main.tex` (L7) is the only manuscript text; posting is a Phase-3 decision after Gate 1 reads. No action.

### D-002 CLOSED (delegated) — ADR 0001 ACCEPTED: (iii) Triton tile-wise reconstruct-inside-attention primary, (i) post-RoPE tracking as the parallel accuracy row `isvd_postrope_r128`
- `docs/adr/0001-factored-attention-kernel.md` status lines updated to ACCEPTED (this commit). Concern (1) (batch > 1 raises in `lazy_initialization`) is lane L4's first Week-2 task; concern (2) (64K batch 4 does not fit an A100-40GB) is the H100-or-max-batch contrast `prereg/kernel_smoke.md` cell list B names; concern (3) (the traffic model predicts floors only) stands.

### D-004 CLOSED (delegated) — real Palu port deferred to Phase 2, conditional on Gate 1 selecting Branch A/B
- The rename to `svd_oracle` is complete (D-004 evidence addendum); the oracle stays the explicit static-low-rank upper bound in every table. A faithful Palu port is scheduled only under Branch A/B, as a Phase-2 lane item.

### D-006 CLOSED (delegated) — KVQuant deferred to Phase 2 unless Gate 2 turns on the 3-bit operating point
- The faithful KIVI arm (G = 32, R = 128, fp16 single-shot prefill) is the 2-bit quantization baseline the panel asked for; a pre-RoPE dense-and-sparse arm (≈ 2 engineer-days + a GPU cell) is not built unless Gate 2 makes the 3-bit point load-bearing.

### D-010 CLOSED (delegated) — the parked litter moved to the Trash
- `~/Desktop/kv-dlra-litter-2026-09-14` (2.7 MB) moved to `~/.Trash/` on 2026-09-20; nothing in the repository references it; the owner empties the Trash at will (the orchestrator does not permanently delete).

### D-017 CLOSED (delegated) — OjaKV: option (A)
- OjaKV is compared through the authors' own harness on its own pod at their published ratios (0.5–0.8×) in Phase 2, after Gate 1 reads, labelled "OjaKV at its published range"; our `oja_*` tracker-swap arms are Oja subspace tracking in our harness and are never called OjaKV. G2 line 7 stays open until that pod runs.

### D-018 addendum (2026-09-20 19:50 EDT, orchestrator) — the vt template comparison returns Verdict B: a generator deviation, not the model's ceiling; the repair is scoped and deferred past Stage 1
- Report: docs/plan/reports/vt-template-comparison.md (read-only investigation; every claim sourced to `kvdlra.eval.gen`, NVIDIA/RULER's `variable_tracking.py`/`constants.py`, and the pre-flight records). Findings: generator v2's `vt` differs from official RULER in five ways — the mod-1 depth wrap places the chain's definition AFTER its references at depths 0.40 and 0.95 (RULER sorts positions so the value always precedes every reference); v2 omits RULER's mandatory one-shot example; the value may come from the `words` family (RULER: numbers only); chain statements lack the closing period; the filler is real documents. Third-party official-RULER runs put full-attention Llama-3.1-8B at vt ≈ 99.6 at 16K (arXiv:2602.05191 Table 4), so the pre-flight's 0.75 (string-match-all partial credit 0.867) is attributable to the generator. NVIDIA publishes no per-task vt cell for this checkpoint; no completion text is stored, so which variable each miss dropped is unrecoverable.
- Decision (within D-018): Stage 1 runs `vt` on the unrepaired generator and reports it DESCRIPTIVELY, its rows read only against the pre-flight ceiling; no `vt` row from before the repair is ever pooled with one from after it. The repair — definition-first ordering, the one-shot example, numbers-only values for vt, the period — is a scoped Phase-2 item validated by one `full`-arm vt cell (≈ 1 GPU-h) before any pod reads vt as a decision; it changes `ruler_v2_*`'s config hash, so it lands as new task files, never by editing a task a manifest pins. The three quotable sentences for Amendment 1b are in the report's last section.

### D-011 addendum 12 (lane L4; 2026-09-21 07:20 EDT, orchestrator) — kernel-smoke pod LAUNCHED (`kernel_smoke`: the on-pod correctness precondition, then cell list A) under the standing authorization for pods inside approved lanes
- Pod `kernel_smoke` (configs/pods/kernel_smoke.yaml at a691d44): Llama-3.1-8B, bfloat16, arms `full`, `isvd_r64_h256_seed`, `isvd_r64_h256_seed_kernel` (prereg §3's order; the knob `decode_attention: kernel`, Amendment 1 A1.1), a `pre_run` step first (the Triton kernel's own gpu tests, tests/test_kernel_triton.py + the gpu params of tests/test_kernel_reference.py, handed over base64 and recorded as `pre_run.txt` + `manifest.pre_run_rc` — Amendment 1's correction, R-L4-27/29/30), then the tasks `kernel_check_16` (the §4 precondition on the pod's own model: 16 pg19-val windows of 4096 tokens at offsets i × 131072, 32 greedy tokens each, token-exact ≥ 14/16 + per-layer max|Δ| < 1e-2 on live 8B K/V — A1.2) and `latency_16k_32k_64k` (batch 1, 16K/32K/64K, 64 steps / 8 warm-up). Pre-registration prereg/kernel_smoke.md, first commit 71c9be6 (lane/L3-gate1-tracker-swap-v2, a strict ancestor), Amendment 1 at 17b4e7f and its correction at 3fafd14 / 438b45f (all append-only: the arm and knob, the check axis and its records, the record/renderer, the re-sized budget 2.25 h point / 5.0 h bar = 2.2×, the pre_run hook and its three refusals).
- Correctness precondition evidence before launch (§4): CPU — tests/test_kernel_reference.py (random 8B shapes 4.66e-3 / 5.15e-3 and the 1B dump layer 8 at 2.75e-3, all < 1e-2 in bf16; 6fe058c) and tests/test_kernel_path.py (16/16 tiny prompts greedy-token-exact, max logit diff 1.6e-7, fp32; 99842f2..a2f0d10). The 8B halves run on this pod first: the pre_run gpu tests (Triton vs reference max ≤ 2e-3 ∧ rms ≤ 1e-4 at n_splits == 1, 4e-3 across splits; vs reconstruct < 1e-2 — R-L4-21/26) then the kernel_check axis → results/kernel_smoke/kernel_check.jsonl. A failed precondition refuses the Week-3 gate; the latency rows are then measured and reported, never cited (A1.2; renderer rulings R-L4-24/25).
- Launch, two attempts: (1) offer 36878336 (Slovenia host 224078, $0.734/h, R 0.999) → instance 51903328 created with the API reply `success: False` and intended state `stopped`; `vastai start` answered "Required resources are currently unavailable, state change queued" → destroyed at 07:18 EDT, never booted (billing expected $0; confirmed at harvest from the credit column). (2) offer 44397667 (A100 SXM4 40 GB, Czechia host 3497, $0.801/h + storage = $0.815/h, reliability 0.995, 3.9 Gbps) → instance 51903816, `success: True`, state running at 07:19 EDT; launch SHA a691d44 (lane/L4-kernel, pushed), `--max-hours` 5 (= gpu_budget_h), watchdog pid 54645 under `caffeinate -s -i` with the Mac on AC (its log in this session's scratchpad); the pod self-destructs 2 h after its final marker. Expected ≈ 2.25 h ≈ $1.8; ceiling ≈ $4.1 at the bar. Credit before launch $80.80.
- Outcome: recorded here at harvest with the evidence path results/kernel_smoke/ and the table docs/paper/tables/kernel_smoke.md (`make kernel_smoke`); the Gate-3 Week-3 outcome under D-002 per prereg §10; GATES §G4 line 3's b = 1 half as amended. Cell list B (batch 4; the H100-or-max-batch contrast) stays the owner's amendment (D-002 concern 2).

### D-002 addendum — Gate-3 WEEK-3 OUTCOME (2026-09-21 08:55 EDT, orchestrator, lane L4): `kernel_smoke` harvested; the pre-registered verdict is **REFUSED** (the correctness precondition was not met under the bar as written); every latency row measured and reported, none cited
- Pod `kernel_smoke`, instance 51903816 (offer 44397667, A100 SXM4 40 GB, Czechia, $0.815/h), launch SHA a691d44, prereg prereg/kernel_smoke.md (first commit 71c9be6; Amendment 1 at 17b4e7f; corrections at 3fafd14, 438b45f, bcc5f04). Harvest results/kernel_smoke/ (`manifest.json` status ALL_DONE, errors 0, pre_run_rc 0, wall_clock_s 4290.3; `latency.jsonl` 9 rows; `kernel_check.jsonl` 16 rows; `pre_run.txt`; `env.txt`); `scripts/pod.py check results/kernel_smoke` → OK; table docs/paper/tables/kernel_smoke.md (`make kernel_smoke`). Wall clock 1.19 h (the check axis 1,448.7 s = 0.40 h against its 0.45 h allowance); cost $1.27 (credit $80.80 → $79.53).
- **The pre_run gate (the kernel's own tests on CUDA, Amendment 1's correction): PASSED** — `7 passed, 1 skipped (the 1B-dump test; the tree is gitignored on the pod), 8 deselected in 20.08 s`, pytest 9.1.1, rc 0: Triton vs the torch reference max|Δ| ≤ 2e-3 ∧ rms ≤ 1e-4 at n_splits == 1 (B = 16, the full tile loop) and ≤ 4e-3 across 16 splits and for batch independence; vs reconstruct-then-attend < 1e-2 on the random 8B shapes (3 params); the no-middle and refusal cases; a CUDA tiny-model end-to-end decode (rulings R-L4-21/26; the `backend` attestation exists from 32b9a0a — this pod's records predate it and harvest as null).
- **The §4 precondition on the 8B model (bf16): NOT met on both clauses** — greedy token-exactness **12/16** (bar ≥ 14/16; the misses: prompts 3 at step 13, 6 at 15, 8 at 4, 14 at 8); the worst per-layer max|Δ| on the first kernel decode step **1.464e-2** (prompt 7, layer 26; bar < 1e-2), with prompts 2 (1.233e-2), 10 (1.003e-2) and 12 (1.405e-2) also over it, layers 26/31 dominant; the other twelve prompts 5.6e-3 … 9.4e-3. Per §4 ("a kernel whose correctness precondition has not passed is not measured for speed: a fast wrong kernel is not a result") and A1.2, **the Week-3 gate reads REFUSED**; the rows below are measured and reported, not cited.
- **The reading of the miss (recorded; the rule for this pod does not move):** the two failing clauses are disjoint — every over-bar prompt (2, 7, 10, 12) is token-exact and every diverging prompt (3, 6, 8, 14) is under the bar; an arithmetic error would put them on the same rows. The compare is a bf16 kernel output against an fp32 reference over the bf16 store, so its absolute Δ carries one bf16 rounding of |out| (Amendment 1 correction 3 (v), written before these rows: 1e-2 ≈ one bf16 ulp at |out| ≈ 2); the CPU calibration the bar came from had |out| < 1 (2.75e-3 on the 1B layer, 4.7e-3 / 5.2e-3 random), and the fp32 tiny-model check (16/16) never exercised bf16-vs-bf16 greedy divergence at near-ties. The pre_run gate at 2e-3 / 1e-4 against the reference is the kernel's implementation evidence, and it passed. What would falsify this reading: a layer whose Δ exceeds a few bf16 ulps of its own |ref|max, or a mismatch at a step whose reconstruct-path top-2 logit gap is large — neither is in this pod's records (no |ref|max, no logits), so there is no re-reading, only a re-run under an amended bar.
- **Latency, batch 1 (the archived Week-20 rows beside them, prereg §2 (a)):** 16K — full 24.74 ms / kv_peak 2.05 GB (archived 25.89 / 2.05, agree); reconstruct 126.03 ms / 3.25 GB / spikes 4 (archived 103.25: +22 %, reported); kernel 39.19 ms / **kv_peak 0.83 GB** / kv_resident 0.83 / mean 88.0 / max 1,992 / spikes 5. 32K — full 30.13 / 4.08 (archived 27.49 / 4.08, agree); reconstruct 248.30 / 6.45 / spikes 3 (archived 188.27: +32 %); kernel **56.12 ms** / **kv_peak 1.08 GB** / kv_resident 1.08 / mean 68.6 / max 232.6 / spikes 4. 64K — full 42.23 / 8.14 (archived 36.97: +14 %); reconstruct 605.27 / 12.84 / spikes 0 (archived 507.94: +19 %); kernel **88.31 ms** / **kv_peak 1.61 GB** / kv_resident 1.59 / max 263.1 / spikes 4. The two Week-3 conditions on the pod's own 32K rows: memory 1.08 < 4.08 GB (margin 73 %, not marginal) — met on measurement; speed 248.3 / 56.1 = **4.4×** ≥ 3.0 (3.35× against the archived 188.27) — met on measurement; spikes ≤ 8 on both arms. At 64K: 605.3 / 88.3 = 6.9× and 1.61 GB against full KV's 8.14 GB (0.20×); the kernel is 2.1× slower than full KV at 64K (88.3 vs 42.2 ms). **None of it is cited: the verdict is REFUSED on the precondition.** The kernel arm's p50 carries the one-row `FactoredMiddle.cat` copy (≈ 1–1.5 ms/token; removed in 32b9a0a) — a conservative handicap. The analytic stored footprint beside the measured peak: 0.302 / 0.556 / 1.064 GiB at 16K / 32K / 64K (0.151 / 0.139 / 0.133× of full); the kernel arm's kv_peak 0.83 / 1.08 / 1.61 GB is the peak of a path that materializes no n×T middle (the remainder is the dense tiers and the attention workspace).
- **Deviations named:** the reconstruct rows sit +22 / +32 / +19 % over the archived Week-20 rows (a different host, driver 580.105, and torch 2.11 — reported per §2 (a); `full` agrees within 10 % at 16K/32K and is +14 % at 64K); the kernel arm's spikes (5 / 4 / 4, maxima 1,992 / 233 / 263 ms) are not the absorb-event rebuild (the kernel path materializes none) and not per-absorb Triton recompiles (every length-dependent argument is a runtime scalar, triton_kernel.py:39-41) — a first-touch specialization or another cause, to be separated from the 16-step absorb lattice by a log-only companion line on any re-run; the check axis ran 0.40 h against the 0.45 h allowance.
- **GATES §G4:** line 1 met (D-002); line 2 — the CPU halves met (random shapes, the 1B dump, 16/16 fp32) and the pod's pre_run gate met, the 8B bf16 halves NOT met (12/16; worst 1.464e-2) — open; line 3 — the b = 1 cells exist, `check` OK, the memory and 3× conditions met on measurement, NOT ticked (the verdict is REFUSED) — open; the b = 4 half is cell list B (the owner's amendment, D-002 concern 2).
- **Pre-registered next step (§4: "a fix is a later commit and a later pod"):** nothing on this pod is re-run with a knob changed. The drafted path is Amendment 2 (for the owner: a relative per-layer bar of 4 bf16 ulps of |ref|max, a near-tie margin M = 2⁻⁵ × the step's top reconstruct logit for token mismatches with a $0 tiny-model bf16 calibration first, a spike diagnostic; a Task 11 recording the fields; a re-run pod ≈ $1–2, wall clock ≈ 1.2 h measured here) or, alternatively, recording this REFUSED as the Week-3 outcome and deferring the re-run to Phase 2 — **the owner decides**. D-002 concern 3 stands (the traffic model predicts floors only); the systems bars were met on measurement at 3–7× and 0.26–0.40× of full KV's peak.

## 2026-09-21 (owner, next-session §0 decisions)

### §0(a) The orchestrator TAKES OVER the L3 items
- The stalled L3 session (idle since 02:24 EDT on its Monitor ALL_DONE; not wakeable) was released by the owner ("I take over") and confirmed closed. This session committed the `gate1_preflight_rerun` harvest, wrote and task-reviewed Amendment 1b, ran the L6 gate and merged L3 into main (STATE 2026-09-21; merge 62140f9).

### §0(b) Kernel re-run: option (A) — extends the D-002 addendum of 2026-09-21 (kernel_smoke REFUSED)
- Commit Amendment 2 + Task 11 + `configs/pods/kernel_smoke2.yaml` and re-run cell list A (≈ $2). Approved: the relative per-layer bar `2⁻⁶` (4 bf16 ulp of `max|ref|`), the near-tie margin `M = 2⁻⁵ × top reconstruct logit` with its κ > 8 revision branch, the five `KernelCheckRecord` fields, the label `kernel_smoke2`. Executed in lane L4b. (C) rejected on the record (instance 51903816 stored neither `max|ref|` nor any logit → not re-readable); (B) declined (leaves every later kernel pod an unreadable absolute bar; the paper's systems paragraph unwritten).

### §0(c) Credit: top up ≈ $100 at Stage 1's launch — extends D-003
- D-003 ("let me know if you are running low, I'll top up"). Credit $79.48 at handover; Stage 1 $37–61 expected ($74–121 at the bar), `kernel_smoke2` ≈ $2, the `isvd_postrope_r128` row ≈ $10–20. The top-up is requested at the Stage-1 launch step, not at the $35 floor.

### §0(d) Cell list B (batch ≥ 4 at 64K): max-batch on the A100 class first — extends D-002 concern 2 (ADR 0001)
- By a dated amendment to `prereg/kernel_smoke.md`, after the re-run's precondition reads. H100 (a second GPU type) deferred to Phase 2 with a "kernel tested on two GPU types" limitation line.

### D-011 addendum 14 (2026-09-21, orchestrator) — Stage 1 LAUNCHED (both pods) from main a659f38, under D-003 ("Start it") + D-011
(Addenda 12–13 are the L4 lane's kernel_smoke / kernel_smoke2 addenda, merged with L4 later; this is the next number free on main.)
- Stage 1 (`gate1_v2_stage1_llama` + `gate1_v2_stage1_qwen`, `gpu_budget_h` 82.0 each) launched from main **a659f38** (the L3 merge; a strict descendant of Amendment 1b's commit; `pod.py launch`'s prereg-ancestor + pushed + clean-tree checks passed). `--max-hours` 82 (= `gpu_budget_h`). Credit before launch **$154.47** (topped up from $79.48 — it covers the 164 GPU-h bar's $74–121 with headroom, so D-003's ask-condition "bar > credit" is not met and no separate top-up was requested). Mac on AC; watchdogs under `caffeinate -s -i` (pids 75417 / 75418), logs in this session's scratchpad; each self-destructs 2 h after its final marker.
  - `gate1_v2_stage1_llama`: instance **51958204**, offer 42079724 (A100 **PCIE** 40 GB, Germany, reliability 0.9967, $0.734/h; host CUDA 13.0, image cuda12.8).
  - `gate1_v2_stage1_qwen`: instance **51958463**, offer 41257399 (A100 **PCIE** 40 GB, Japan, reliability 0.9916, $0.892/h; host CUDA 13.2).
  - **A100 SXM4 40 GB was scarce at launch** (one verified offer, which churned out). These are single-GPU inference pods, for which PCIe is identical to SXM4 in the science — retrieval hits and perplexity are deterministic in the model/prompt/seed; only wall-clock differs, and the 82 h bar is 2× the point — so PCIe was chosen. Neither host is the California PCIe family 5116338x the handover warns against; the manifests record the actual GPU.
- **Aborted first attempt (per the launch/kill rule):** `gate1_v2_stage1_llama` was first launched on offer 50895886 → instance **51950407**, then destroyed at ≈ 2 min (≈ $0.06) because `pmset` showed the Mac on **battery** (90 %) and a ~41 h watchdog holds only on AC (`caffeinate -s`; the pre-flight lost data to battery sleep — D-011 addendum 10/11). The owner then confirmed AC (charging), and both pods were relaunched on AC.
- Expected ≈ 41 h wall each (the two run in parallel), $37–61 total; $74–121 at the 82 h bar. Outcome recorded at harvest (results/gate1_v2_stage1_{llama,qwen}/) → `make gate1` renders the table UNREAD → the five-reviewer panel (G1-verdict lane) → `gate1_verdict()` decides the branch → DECISIONS + GATES §G3/§G5.

### D-011 addendum 13 (2026-09-21, lane L4b) — kernel_smoke2 LAUNCHED (the Amendment 2 re-run of the Week-3 kernel-correctness precondition), under the owner's §0(b)-A + D-011
- Pod `kernel_smoke2` (`configs/pods/kernel_smoke2.yaml`, byte-for-byte `kernel_smoke.yaml` with `name: kernel_smoke2`; `prereg: prereg/kernel_smoke.md`): Llama-3.1-8B bf16, arms `full` / `isvd_r64_h256_seed` / `isvd_r64_h256_seed_kernel`; the `pre_run` gpu-test gate first, then `kernel_check_16` (§4 precondition read under **Amendment 2**'s relative bar — `rel ≤ 2⁻⁶ ∧ attributable-count ≥ 14 ∧ no errors`, near-tie margin `M = 2⁻⁵·kernel_logit_for_ref_argmax`, κ = 8) and `latency_16k_32k_64k` (cell list A, batch 1).
- SHAs: prereg first commit **71c9be6** (the machine-checked ancestor); Amendment 2 **2cb8f97**; launch **0984a5d**. `git merge-base --is-ancestor 2cb8f97 0984a5d` → **YES** — Amendment 2 is a strict ancestor of the launch SHA (satisfying A2.6's "governs only pods launched after its own commit"). L6 clean-clone gate green at 0984a5d (make test 684 passed, tables diff-clean, tree clean, forbidden 0; the two pre-flight-dir `check` failures are pre-existing lane-base artifacts, resolved at the L4 merge).
- Launch: instance **51969128**, offer 47508314 (A100 **SXM4** 40 GB, Iowa US, reliability 0.9961, $0.6955/h; host CUDA 13.0, image cuda12.8), `--max-hours` 5 (= `gpu_budget_h`), Mac on AC (100 % charged), watchdog under `caffeinate -s -i` (pid 7227), log in this session's scratchpad; self-destructs 2 h after its final marker. Expected ≈ 1.2 h ≈ $0.8 (kernel_smoke measured 1.19 h / $1.27); ≤ $4 at the 5 h bar. Credit before launch **$151.40**.
- Outcome recorded at harvest (results/kernel_smoke2/) → `make kernel_smoke2` → the D-002 addendum "Gate-3 WEEK-3 OUTCOME, re-run" (the rule decides: `rel ≤ 2⁻⁶ ∧ attributable ≥ 14 ∧ no errors`, then the two gate conditions at batch 1) → GATES §G4 lines 2–3. Instance 51903816's records are never re-read.

### D-002 addendum — Gate-3 WEEK-3 OUTCOME, re-run (2026-09-21 evening, lane L4b): kernel_smoke2 harvested; Amendment 2's relative bar READS the gate — still REFUSED, but by a single low-|ref| outlier, and the near-tie hypothesis is confirmed
- Pod `kernel_smoke2` (instance 51969128, offer 47508314 A100 SXM4 40 GB Iowa, $0.6955/h), launch SHA **0984a5d** (Amendment 2 **2cb8f97** a strict ancestor), prereg prereg/kernel_smoke.md Amendment 2 (κ = 8). Harvest results/kernel_smoke2/ (`scripts/pod.py check` OK, 0 errors; pre_run rc 0 / 32 rows — the Triton gpu gate PASSED again; kernel_check.jsonl 16 rows; latency.jsonl 9 rows); table docs/paper/tables/kernel_smoke2.md (`make kernel_smoke2`). Wall ≈ 1.8 h; ≈ $1.3. Instance 51903816's records are untouched.
- **The precondition, under Amendment 2's relative bar (`rel ≤ 2⁻⁶ ∧ attributable-count ≥ 14 ∧ no errors`): NOT met — but now readable, not ambiguous.**
  - **Token clause MET: 16/16 effective (0 attributable, 5 near-tie mismatches).** The five greedy divergences (prompts 0, 1, 3, 6, 7) are bf16 near-ties — reconstruct top-2 gaps 0.0 / 0.0 / 0.125 / 0.0625 / 0.0625 ≪ M = 2⁻⁵·top-logit ≈ 0.45–0.58. **The near-tie hypothesis of the morning's REFUSED is CONFIRMED: the kernel is greedy-token-exact; the token mismatches were never kernel errors.**
  - **Relative per-layer clause MISSED by one prompt: `rel 1.868e-2 (4.78 u) > 2⁻⁶ (4 u)` at prompt 10, layer 8, ref_max 0.437.** The other 15 prompts are within the 4-u bar (next-worst prompt 8 1.523e-2, prompt 7 1.415e-2, both < 1.5625e-2). Layer 8 is the worst layer for 3 prompts (0: 2.79 u, 3: 2.21 u, 10: 4.78 u), so it is **not a systematic layer-8 kernel error** (which would fail most prompts there); it is one prompt at a **low-magnitude middle layer** where the 4-u bar (calibrated at |out| ≈ 0.9) is naturally tighter because relative error rises as |ref| falls. The absolute worst |Δ| stays 1.464e-2 at layer 26 (reported), whose large |ref| keeps its ratio in-bounds — the late-layer case A2.2 predicted would pass, and it did.
- **WEEK-3 GATE (batch 1): REFUSED** — the precondition requires `rel ≤ 2⁻⁶` on every prompt; prompt 10 fails at 4.78 u. Per §4 the systems rows are measured, not cited: memory @32K kernel kv_peak 1.08 < full 4.08 GB (margin 73.5 %); speed @32K reconstruct/kernel 188.9/57.6 = **3.28× ≥ 3.0**; both PASS on measurement. At 64K kernel 85.2 ms vs full 37.8 ms (2.3× slower — the Week-5 throughput bar needs batch ≥ 4, cell list B). Kernel-arm spikes 5/4/4 (16K max 3688 ms, a first-touch outlier), below §4's > 8 refusal, so the p50s stand.
- **The reading:** Amendment 2 did its job — it converted the morning's ambiguous REFUSED (both clauses missed under an absolute bar read at an unmeasured scale) into a precise one: the kernel is greedy-token-exact and within 4 u on 15/16 prompts, with a single borderline outlier (prompt 10 layer 8, 4.78 u) at a low-|ref| layer. The evidence points to a **near-correct kernel**, not a broken one. Whether prompt 10 layer 8 is a small real numerical variance at low |ref| or the 4-u bar needing a per-|ref| floor is a **Phase-2 systematic-debugging item**; **no fix on this pod** (§4). Until it resolves, the memory/speed claim is not yet citable, but it is one narrow correctness step away rather than blocked on ambiguity.
- **GATES §G4:** line 2 now READ (token half MET; relative half missed 1/16) — the REFUSED is attributable, not ambiguous; line 3 systems conditions MET ON MEASUREMENT, gate REFUSED on the precondition. Cell list B (batch ≥ 4) and the prompt-10 layer-8 investigation are Phase 2.

### D-002 addendum — re-run outcome, CORRECTION (2026-09-21 late, lane L4b, from the whole-branch review): the kernel_smoke2 spike cause is UNDETERMINED (its A2.4 companion line was filtered before harvest); numeric corrections
- **The 16K kernel-arm spike (max 3688 ms) has no recorded cause; "a first-touch outlier" in the re-run addendum above is WITHDRAWN.** A2.4's spike-diagnostic companion line (`[latency spikes ctx=… arm=… steps=…]`, `src/kvdlra/eval/latency.py`) — the pre-registered discriminator between first-touch JIT (indices clustered in the first steps) and the 16-step absorb lattice (indices recurring on a 16-lattice) — was dropped by `scripts/pod/watchdog.sh`'s row-admission filter (`ROWS`), which admitted `[latency …]` lines only when they carried `ms/tok=`. It never reached results/kernel_smoke2/'s log and the instance is destroyed, so the discriminator was never read. The filter is repaired here (`^\[latency spikes` added to `ROWS`, pinned in `tests/test_pod_manifest.py`) so the next latency pod captures it; **the spike cause on kernel_smoke2 stays undetermined, deferred to that pod.** Not a gate input: kernel-arm spikes 5/4/4 are below §4's > 8 refusal, so the p50s stand and the WEEK-3 verdict (REFUSED on the precondition) is unchanged.
- Numeric corrections to the re-run addendum: the near-tie margin M over the five mismatches is **0.4375–0.5625** (not "0.45–0.58"); kernel_smoke2 wall **1.61 h** (manifest `wall_clock_s` 5793) at $0.6955/h ≈ **$1.12** (not "≈ 1.8 h / ≈ $1.3").
- Provenance (A2.7's stated commit order is superseded, per the L4b calibration-first ruling): the actual order was Task 11 (**a571bbd**, incl. the bf16 calibration that measured κ = 8) → Amendment 2 (**2cb8f97**) → the heading fix (**0984a5d**), not A2.7's "Amendment 2, then Task 11". The ruling is sound — both κ branches were pre-specified in the draft at 503c754 (a CPU-only tiny-model calibration that never saw an 8B number), so committing Amendment 2 with the measured κ peeks at nothing.
