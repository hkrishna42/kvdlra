name: isvd_r256_f0.01_tol
kind: bug
chunkable: true
doc: >-
  Rank-256 gist with the singular-value floor at 1e-2 and the default tolerance guard: the
  Week-17 fix, re-run alongside the guard. This is isvd_r256_f0.01 at the shipped defaults; a
  separate file only so the pod can widen the diagnostic window. prereg/hygiene_table4.md.
cache:
  rank: 256
  coord_budget: null
  recent_window: 32
  absorb_block: 16
  n_sink: 4
  retention: "fifo"
  min_sv_frac: 0.01
  diag_every: 4096
